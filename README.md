# Qeynos Audio Suite

A suite of CLAP/VST3 audio plugins (built on [nih-plug](https://github.com/robbert-vdh/nih-plug))
plus FL Studio automation tools, built autonomously. See `PRD.md` for the design and
execution playbook, `STATUS.md` for current progress, and `SPECS.md` for per-plugin
DSP specs.

CLAP bundles install to `%LOCALAPPDATA%\Programs\Common\CLAP\Qeynos\` (per-user, no
admin; FL Studio ≥ 2024.1 scans it). VST3 installs alongside only if the optional
admin junction exists (see `CHECKPOINTS.md`).

## Plugins

| Plugin | Type | Summary | Docs |
|---|---|---|---|
| Qeynos Template | Utility | Hello-gain reference (Phase 0 gate); one smoothed gain + peak meter | — |
| GRIT | Distortion | Sidechained distortion — envelope- and waveshape-driven saturation, 4x oversampled, auto-gain, dry/wet | [docs/GRIT.md](docs/GRIT.md) |
| EMBER | Spectral | Spectral fader / temporal smoother — per-bin STFT state machine, 8-band attack/decay curves, phase-vocoder tails, freeze (+ **Freeze Mix** to blend live↔frozen); reports 2048-sample latency | [docs/EMBER.md](docs/EMBER.md) |
| IMPACT | Instrument | Kick drum synth (MIDI) — exponential pitch/amp envelopes, sine/tri body, band-passed click + 3 embedded PCM transients, sub osc, waveshaper drive; phase-continuous declicked retrigger, key-track, Length macro | [docs/IMPACT.md](docs/IMPACT.md) |
| TRACER | Distortion | Pitch-tracking multiband saturation — MPM f0 detection locks a time-varying LR4 crossover tree to the note; per-band waveshaper drive (2x OS), Smart Frequency, constant-color, confidence freeze, MIDI mode | [docs/TRACER.md](docs/TRACER.md) |
| OVERSEER | Mastering | ONE bundle, TWO plugins: **Node** channel strip (EQ, RMS comp, tanh sat, M/S width) + **Master** bus (EQ, 3-band LR4 multiband comp, 2 ms lookahead limiter w/ reported latency, BS.1770 LUFS meter). Master remote-controls Nodes over a same-DLL bus (override badges, local steal-back). **ENRICH:** Nodes auto-classify their instrument type from the audio (AUTO + LEARN), the Master infers a session THEME and can gently assist the master chain (ASSIST knob, 0 = display-only); type/theme-filtered thematic preset banks. Caveat: FL "Make bridged" severs the link | [docs/OVERSEER.md](docs/OVERSEER.md) |
| DRIFT | Filter | Infinity filter — endless Shepard-tone filter sweep. N (2–8) octave-spaced TPT bell filters glide up/down over a log-freq range (free Hz or BPM-synced), wrapping at the edges with a raised-cosine gain window ⇒ seamless endless rise/fall. Shared resonance, depth, stereo phase offset, dry/wet. Zero latency (minimum-phase) | [docs/DRIFT.md](docs/DRIFT.md) |
| WIRE | Lo-Fi | Codec degradation — a real **Opus** round-trip (pure-Rust `opus-rs`, 48 k internal) abused as an effect: bandwidth low-pass + **crunch** (bit/SR reduce) → encode (Bitrate 6–128 kbps, Voice/Music, FEC) → **packet-loss** dropouts (click-free concealment) → decode → re-encoding **regen** generation-loss loop → width/mix. 20 ms frames, latency reported + dry PDC-aligned. Codec runs in the audio thread (~0.3 % RT) | [docs/WIRE.md](docs/WIRE.md) |
| OUROBOROS | Delay | Recursive feedback processor — a delay loop (1 ms–2 s, free/BPM-synced) through a **reorderable 3-slot effect chain** (granular pitch ±12 st, SVF LP/HP/BP, Hilbert freq-shift, saturate, reverse-granule, bit-crush) → in-loop `tanh` **limiter** → DC blocker → **110 % feedback**. Each repeat is re-processed; past unity it self-oscillates but stays bounded. **Freeze** mutes input + pins 100 % feedback for infinite tails (**Freeze Mix** blends live↔frozen). Zero latency; fractional-smoothed delay glides click-free | [docs/OUROBOROS.md](docs/OUROBOROS.md) |
| SWARM | Granular | Mass granulator — a **10 s stereo capture buffer** sprayed into up to **128 concurrent grains** (poisson or tempo-grid scheduler). Per grain, randomised at spawn: position **spray**, pitch **scatter** (±24 st, free/semitone-quantised), **size** 10–500 ms, Tukey window, equal-power **pan**, **reverse** probability; interpolated buffer reads, density-normalised. Sum → optional **+12 st shimmer** feedback (in-loop `tanh` + DC blocker) that re-enters the buffer to bloom. **Freeze** locks the write head for infinite evolving textures (**Freeze Mix** blends live↔frozen). Voice cap 128 (steal oldest); zero latency | [docs/SWARM.md](docs/SWARM.md) |
| SMUDGE | Spectral | Spectral chaos — an STFT (2048/512) runs four per-frame ops in fixed order, each **exactly bypassed at amount 0**: **1 scramble** (permute bins in ±N neighbourhoods, redraw rate), **2 spectral delay** (per-⅓-oct-band frame delays from a tilt curve, bounded feedback), **3 blur** (per-bin temporal magnitude averaging, τ per band + phase-vocoder advance), **4 smear/stretch** (bin-index remap ×0.5–2, energy-normalised). A **chaos** macro slow-S&H-modulates the op params. Reports 2048-sample latency | [docs/SMUDGE.md](docs/SMUDGE.md) |
| MURMUR | Reverb | Stochastic reverb — an **8×8 Householder FDN** (`suite_core::fdn`, reused by UNDERTOW/SEANCE/CHAMBER) **re-randomised into a new room on every onset**: a band-energy-rise onset detector (or the manual **re-roll** button) draws fresh delay lengths (mutually-prime-ish, scaled by **Size**), diffusion coeff, and damping **Color** into an idle second FDN, then a 50 ms equal-power crossfade swaps to it — click-free, every hit a different room. **Randomness** sets how far draws deviate (0 = fixed nominal room, RT60 = **Decay**). **Freeze** → near-infinite feedback + input duck for an eternal wash (**Freeze Mix** blends live↔frozen). Width, mix; zero latency | [docs/MURMUR.md](docs/MURMUR.md) |
| UNDERTOW | Bass | Kick-to-rumble generator — **sits ON the kick track** and adds a kick-ducked **sub-bass rumble** underneath (hard/melodic techno low-end). **Strip** the click (env-gated transient strip) → **Drive** (suite waveshaper, 2× OS) → small/dark **`suite_core::fdn::Fdn8`** (short delays × **Size**, dark, RT60 = **Decay**; summed mono) → resonant **LP** 90–250 Hz → **key-lockable resonant tune peak** (Note C0..B2, A1 = 55 Hz; Amount) → **ducker keyed by the dry kick** (Depth 0–24 dB, Release) so the rumble breathes around the kick → Rumble/Width/Dry/Trim. **Mono below 150 Hz** (Width only spreads above); zero latency (rumble-muted → dry null) | [docs/UNDERTOW.md](docs/UNDERTOW.md) |
| SNAP | Instrument | Snare/clap generator (MIDI) — a **MODE** blend crossfades three engines: **BODY** (phase-continuous sine/tri @ **Tune** 140–260 Hz with a fast pitch-env shell knock), **RATTLE** (white noise → 3 parallel BP formants ~800/1.5k/3k, own env = snare wires), and a **CLAP engine** of **Taps** (3–5) humanized noise slaps over a **Spread** (8–30 ms) + one longer tail, through BP/LP **Tone** shaping. **Snap** scales the transient click + rattle speed; **Body/Noise** balances the snare; **Decay** is a length macro over every envelope; **Drive** (suite waveshaper, 2× OS) → amp env → **Width** (decorrelated per-channel noise, mono-compatible) → soft clip. Key-track optional (off). Declicked phase-continuous retrigger | [docs/SNAP.md](docs/SNAP.md) |
| SEANCE | Vocal FX | Ethereal vocal machine (ghost vocals) — builds the **formant-preserving PV shifter** (`suite_core::shift::ShiftEngine`, reused by VOXKEY/VOXFIT): cepstral-envelope separation so **Pitch** (±12 st) and **Formant** move independently (Preserve on). Then a BPM-synced **chopper** (Square/Stutter/Ramp/Double/Random gate, smoothed edges), a **shimmer verb** (`suite_core::fdn::Fdn8` with a **+12 st** shifter in the feedback loop, soft-limit + DC block), a **wash** (LP + wow pitch drift), and a **drowned-vocal ducker** (keyed by the dry env — the wet **swells in the silence** between phrases). Three macros: **GHOST** (formant+wash+blend), **DROWN** (size+wet+duck), **CHOP** (density). Mix/Out; +2048-sample latency (auto delay-comp) | [docs/SEANCE.md](docs/SEANCE.md) |
| VOXKEY | Vocal FX | Vocal retuner (autotune) — pitch-tracks a mono vocal (`suite_core::pitch::Mpm`) and snaps it to the nearest tone of a **Root + Scale** (Chromatic/Major/Natural+Harmonic Minor/Phrygian/Dorian/Minor Pentatonic) or a **held MIDI note**, retuning through two formant-preserving **`ShiftEngine`s** (envelope-preserve on, so no chipmunking). **Retune Speed** 0–400 ms (0 = hard-snap autotune artifact), **Amount** 0–100 %, **Humanize** cents drift, **Formant Offset** ±12 st (independent), **Confidence Gate** leaves breaths/silence untouched. Live IN→TGT note read-out. Mix/Out; +2048-sample latency (auto delay-comp) | [docs/VOXKEY.md](docs/VOXKEY.md) |
| VOXFIT | Vocal FX | Vocal character conformer — makes a ripped/foreign acapella **sit** in a different production. Pitch-**independent** formant shift (**`ShiftEngine`** formant-only, ±5 st) → **de-esser** (complementary 5 kHz split, EnvFollower-keyed, threshold/amount/**listen**) → dynamic **harshness tamer** (2–5 kHz bell cut that follows band energy) → **tilt EQ** (complementary shelves @1 kHz, ±6 dB, dark↔bright) → **proximity** low-mid shelf → **air** high shelf → out. The **SIT** macro sweeps a curated dark-mix conform (formant + mild de-ess + presence dip + dark tilt + proximity). Mix/Out; +2048-sample latency (auto delay-comp) | [docs/VOXFIT.md](docs/VOXFIT.md) |
| ASCEND | Instrument | Tension generator (MIDI/transport) — reads host transport and **counts down to the next 8/16/32 (or custom) bar boundary**; one **tension envelope** (curve morph **exp↔lin↔log**) drives, at once, an **SVF filter sweep** (start→end, opening), a **pitch rise** (0–24 st on a tonal **root+fifth** stack, saw↔sine at **Key/Octave**), a **width bloom** (narrow→wide) and a **volume swell** (quiet→full) over blended **filtered noise** (white↔pink) + that stack. At the boundary: an embedded **impact** (IMPACT's `synth_kick` math, low) fires and **auto-cut** silences the sources, re-arming for the next boundary. **Downlifter** mode reverses the envelope (full at the drop, falling away). Transport stopped → a manual **TRIGGER** (or MIDI note; **key-track** = root follows note) runs the same envelope over a **Free Length** (seconds). Live bars-remaining countdown; stereo out, zero latency | [docs/ASCEND.md](docs/ASCEND.md) |
| FLYBY | Spatial | Doppler spatializer — flies a mono source around an **editable closed path** on an XY pad (listener at the origin; drag 4–8 nodes, or load a **Circle / Ellipse / Figure-8** shape). Traversal is phase-driven, **free (Hz) or BPM-synced** (½/1/2/4-bar loops). Per sample the source's distance `r` and azimuth `θ` drive four cues: a moving **fractional-delay doppler** (Catmull-Rom read at `delay = r/c`, **rate-clamped** so sharp corners can't spike the pitch), inverse-distance **level** (`1/max(r,r0)`), distance-dependent **air absorption** (one-pole LP, cutoff ∝ `1/r`), and **equal-power pan** with an optional sub-ms **micro-ITD**, then **Width**. **Size** scales the view/distance, **Doppler**/**Air** the depth of each cue. The delay *is* the effect ⇒ zero latency, `mix = 0` nulls dry. Live moving-source dot | [docs/FLYBY.md](docs/FLYBY.md) |
| CLEAVE | Slicer | Multi-slicer with a **transport-locked step sequencer** — a **2-bar rolling stereo buffer** is captured, sliced (fixed **grid 1/8–1/32**, or **transient** onset detection via **spectral flux + zero-cross backtrack**, sensitivity), and re-played by a **16–64-step** sequencer locked to the host playhead. Each step carries its own lanes — **slice index** (or "as played"), **gate**, **reverse**, **pitch** (±12 st, resample read), **roll** (×2/3/4), **probability**, **level** — edited on a multi-lane **step-grid widget** (Level/Gate/Pitch/Roll/Rev/Prob/On) with a live playhead. **Grain-windowed reads** (raised-cosine 5 ms fades) keep it click-free; **Swing** on the off-steps; **Randomize** (Density) / Clear / Fill. Six pattern presets (Straight Rechop, Rolls & Ghosts, Reverse Accents, Half-Time Flip, Jungle Scatter, Four Flat). Source snapshot latched at each pattern boundary so slices sit on the grid; free-runs an internal clock when stopped. The dry path is zero-latency ⇒ `mix = 0` nulls exactly | [docs/CLEAVE.md](docs/CLEAVE.md) |
| PLUCK | Strummer | Karplus-Strong **strummer** (effect **with MIDI in**) — the **audio input is the exciter**: an onset fires a **STRUM** across **6 KS strings** (fractional **Catmull-Rom delay** + one-pole **damping LP** + **all-pass fine-tune** in the loop), the input's timbre coloring each pluck via a windowed **~500-sample burst** grabbed at the onset; **continuous-drive** feeds the strings constantly. Strings tune from a **dark chord table** (m / m7 / sus2 / m9 / 5th-stack / sus4 on a **Root**), **MIDI-held notes** (voice-assigned low→high, octave-doubled), or a **chromagram key-detect** over the input (`suite_core::stft`, confidence-gated, falls back to chord). **Strum time** 5–80 ms **Up/Down/Alternate** (stagger = strum/5); **Decay** sets sustain, **Damp** brightness, **Vel→Bright** opens damping on hard hits, **Spread** detunes strings (cents), **Stereo Alt** pans them L/R. A small embedded **modal body IR** (2048-tap per SPECS, generated at init) is convolved into the wet path (**Body** amount). Zero latency ⇒ `mix = 0` nulls dry; **Wet Solo** for the pure resonance. Live 6-string activity display | [docs/PLUCK.md](docs/PLUCK.md) |
| SHAPESHIFT | Distortion | XY-**morphing** distortion — **four corners** (A/B/C/D) each pick a waveshaper from an 8-curve bank (**Tube tanh · Tape soft · Diode asym · Hard clip · Sine fold · Tri fold · Cheby-3 · Bit soft**) with a per-corner **gain trim**; an **XY point** sets **bilinear** blend weights (`wA=(1-x)(1-y)…wD=xy`, a partition of unity) so the output morphs continuously between the four characters: `y = Σ wᵢ·shaperᵢ(gᵢ·pre·x)`. A built-in **orbit LFO** rotates the XY point around the user position (**Circle / Figure-8**, **free Hz or BPM-synced** ½/1/2/4-bar, **Radius**, **Phase**) for automatic evolving distortion. The whole morph runs at **4x oversampling** (halfband FIR); the dry path is **delay-compensated** by the oversampler group delay (reported latency) so partial **Mix** doesn't comb-filter. A **Post LP** (SVF) tames fold/crush harshness; optional **Auto-Gain** (300 ms RMS match) holds loudness. Draggable **XY morph pad** with a live orbit dot. Corner-A/-D positions null against that shaper alone; `mix = 0` nulls dry | [docs/SHAPESHIFT.md](docs/SHAPESHIFT.md) |
| CHAMBER | Reverb | Image-source **space simulator** — a **shoebox room** (**W×D×H**, 2–40 m) with a **draggable source & listener** on a top-down floor-plan pad. **Early reflections** are a true **image-source cluster** to **order ≤3** (7/25/**63** mirror images incl. the direct path): per image a shared-delay-line tap at **`r/c`**, gain **`(r_direct/r)^dist × reflectⁿ`** (per-wall-group reflectance from **4 materials** — Concrete / Wood / Curtain / Glass, walls·floor·ceiling), a per-bounce **HF-damp** one-pole, and **equal-power azimuth pan**. The diffuse tail is a **Sabine-tuned FDN** (`suite_core::fdn::Fdn8`, reused from MURMUR): line lengths scale from the room mean-free-path, **RT60 = 0.161·V/ΣSα** (or a manual override), damping from the material HF character, pre-delayed in after the ER window; **ER/Late** balance blends the two. A moving source **doppler-glides** (rate-clamped interpolated delays). The direct path **is** the dry ⇒ zero latency, `mix = 0` passes through exactly. **ER Order = Auto** picks the order from a CPU budget (measured **4.0%** of real-time at order 3 → Auto = 3). Distance, Pre-Delay, Width, Mix, Out | [docs/CHAMBER.md](docs/CHAMBER.md) |
| CARVE | Dynamics | Spectral **ducker** (Trackspacer-style) — a **sidechain** carves its own frequencies out of the main signal. Two STFT roles (2048/512, Hann) run in lockstep: a mono **sidechain** analysis STFT measures per-**⅓-octave-band** energy, and one **main** STFT per channel applies a per-band gain — the more SC energy in a band, the deeper the main's matching band is cut, so it makes a **frequency-matched pocket** instead of ducking the whole level. Reduction = **soft-knee** function of SC band level vs **Threshold**, scaled by **Amount** × **Max Depth** (0–24 dB), shaped by **Sensitivity** (knee width + excess span) and **Tilt** (bias the cut toward lows/highs), then **Attack/Release**-smoothed per band-group at hop rate. **Δ-listen** monitors the carved residual (what's removed); **SC-listen** auditions the sidechain; a live per-band **reduction meter** shows where it's cutting. Reports 2048-sample latency; SC-silent nulls to the STFT identity and `mix = 0` passes the delayed dry through exactly. Aux sidechain input, Mix, Out | [docs/CARVE.md](docs/CARVE.md) |
| NERVE | Modulation | Suite **modulation bus** source — generates 8 control streams (**4 LFOs** with an 8-shape bank incl. S&H, free or **BPM-synced**; **2 env followers** of its own input; **2 random S&H** with slew; **4 hand macros** summed into the LFO streams) and **publishes them to a tier-2 cross-DLL/cross-process bus** (`memmap2` shared file at `%TEMP%\qeynos-bus`, 64 fixed slots, **per-slot seqlock**, heartbeat GC). Any other Qeynos plugin's params can **"listen"** to a stream via its **MOD section** (pick source instance + signal + **depth** ± + **curve**), applied at block rate as an additive normalized offset — the host/automation still see the base value, so it acts like an internal LFO tool. Transparent audio passthrough, zero latency. Cross-plugin modulation needs instances **un-bridged** (FL default) | [docs/NERVE.md](docs/NERVE.md) |
| HALT | Buffer FX | Performance **buffer effect** — a **4-bar circular buffer** records continuously; four **momentary** modes replay it live, each a host-automatable **button** AND **MIDI-note** trigger (**C1..D#1**): **TAPE STOP** (read rate ramps **1→0** over a synced/free **Stop Time** with an exp↔lin↔log **Curve**; **Ramp**/Instant release), **STUTTER** (loops the last **1/4..1/64** division with per-repeat **Decay** + **Pitch Step**; retrigger **Quantize** off/1/16/1/8/1/4 snaps the loop to the beat; period stays exact regardless of pitch), **REVERSE** (reads backward from the trigger point), **HALF-SPEED** (rate 0.5). Multiple held → **last-pressed wins**. Every engage / disengage / mode-change / loop-wrap is a **5 ms equal-power crossfade** (click-free). Transport-synced (free-runs at host tempo when stopped); **inactive or `mix = 0` is a bit-exact passthrough**; zero latency. Mix, Out | [docs/HALT.md](docs/HALT.md) |
| BANDAID | Dynamics | Multiband **transient designer** — an **LR4 3-band** split (two crossovers, ~20–800 Hz and ~800–8 kHz) feeds a per-band **transient detector** (fast **1 ms** − slow **50 ms** envelope difference): the rising overshoot is the **attack** region, everything else the **sustain** region. Each band applies an **attack gain** and a **sustain gain** (**±12 dB** each, ~5 ms-smoothed) and is recombined as a **parallel delta** — `out = x + Σ (g_b − 1)·band_b` — so all-neutral gains **null against the dry input exactly** (regardless of crossover phase). Per-band **solo/listen** auditions one shaped band; a global **Detector** trim scales the envelope times. Punch up kick attacks, tighten room tails, soften hat transients, add snap to a drum bus or bloom to a pad. Zero latency; **Mix = 0** passes the dry through. Xover Low/High, per-band Attack/Sustain, per-band Solo, Detector, Mix, Out | [docs/BANDAID.md](docs/BANDAID.md) |
| PATINA | Lo-Fi | Analog **lo-fi character** — a tape/vinyl chain of small imperfections: **wow & flutter** (a Catmull-Rom **fractional delay** driven by a **0.4 Hz** wow + **~8 Hz** flutter + a slow random walk, one-sided so depth 0 = pure delay), **tape saturation** (`tape_soft`, **2× oversampled**, blended against a clean delay so drive 0 is exact), a **head-bump** low shelf (`x + (g−1)·LP`, 60–120 Hz), an **azimuth** HF phase skew (one channel's highs through a 1st-order allpass), random **dropouts** (Poisson-triggered gain dips, 8 ms-smoothed click-free edges), and a **noise** bed — **hiss** (filtered white) + **hum** (50/60 Hz + harmonics) + **crackle** (sparse band-passed pops) — **keyed** to the input's **RMS** envelope (**Key** 0 = constant floor, 1 = fully gated). One **AGE** macro scales every section together on a curated curve. Every section is an exact identity at 0, so **AGE 0 + all sections 0 nulls** against the latency-matched dry; reports **30-sample** latency (base wow delay + oversampler). Wow/flutter/sat/bump/azimuth/dropout/noise + hum 50/60 + Key + Age, Mix, Out | [docs/PATINA.md](docs/PATINA.md) |
| X-RAY | Analyzer | Shared **cross-plugin spectrum analyzer** (tier-2 bus *consumer*). Every Qeynos plugin now taps its output through a **32-band constant-Q filter bank** (⅓-octave, 20 Hz–20 kHz, ~0.5 % CPU) and **publishes** `{ spectrum, peak, RMS, label, kind }` to its slot on the shared `%TEMP%\qeynos-bus` file at block rate. X-RAY reads **every live slot** with `snapshot_live()` and overlays them as **colored curves** on one log-freq / dB grid, with a **legend** (label · bus id · peak/RMS), **hover** to highlight one instance (others dim), **click** to solo-dim, and **Freeze**. Audio is a **bit-exact passthrough** (it can also **Publish** its own input spectrum). Publishing was retrofitted into the tractable majority of plugins; **OVERSEER / NERVE / _template** are deferred (DEFERRED.md). Un-bridged FL instances (default) all map the OS-wide bus file. Publish, Freeze, Out (0 dB = bit-exact) | [docs/XRAY.md](docs/XRAY.md) |
| CHORALE | Resonator | **Resonator bank** (effect **with MIDI in**) — a bank of **12–24 waveguide resonators** (extended-**Karplus-Strong** loops: fractional **Catmull-Rom delay** + one-pole **damping LP** + **all-pass fine-tune**, the PLUCK cent-accurate tuning solve so the fundamental holds **±10 cents**), each with a **tanh-soft-clipped feedback** path so the high-feedback loops **self-limit** under continuous drive. Unlike a strummer, the **audio input excites every resonator continuously**, so the tuned loops **ring sympathetically**. The bank tunes to a **held MIDI chord** (voice-assigned, octave-stacked), a **selected scale/chord on a Root** (minor/major triads, m7/M7, sus2/4, 5th-stack, minor/major pentatonic, Phrygian, Dorian, Octaves — spread across up to **5 octaves**), or a **chromagram key-detect** over the input (`suite_core::stft`, confidence-gated, falls back to the scale). Each resonator's input gain is optionally weighted by the input's **band energy at its pitch** (**Sympathetic** amount, via `suite_core::spectrum::SpectrumTap`) so the bank sings the source's strong notes. **Spread** detunes alternately ±cents; **Stereo** pans even/odd resonators L/R with width; **Decay** sets ring time, **Damp** darkness. DC-blocked wet; zero latency ⇒ **Mix = 0** nulls the dry input; **Wet Solo** for the pure resonance. Live resonator-activity display. Source, Root, Scale, Count, Decay, Damp, Spread, Sympathetic, Excite, Stereo, Wet Solo, Mix, Out | [docs/CHORALE.md](docs/CHORALE.md) |

## Controls (shared UI)

Every editor uses the same widgets from `suite_core::ui`, so controls behave identically
across the suite: **rotary knobs** (drag up/down; Ctrl-drag = fine ~10×; double-click =
reset; scroll = step), **click-the-value to type** an exact value (Enter commits via the
param's own parser, Esc cancels), and **uniform window scaling** (the whole editor zooms
as one unit with 75/100/125/150 % snap points in a corner size menu, persisted with the
project). Boolean params render as toggles. Full reference: [docs/UI.md](docs/UI.md).
The endgame CONSOLE v2 theme only re-skins these widgets; the interactions stay as-is.

Every editor also carries the shared **preset bar** (`suite_core::ui::PresetBar`): one
dropdown with **FACTORY** and **USER** sections, **Save** / **Save As** / **Delete**, and a
**dirty dot** when the live params diverge from the loaded preset. User presets are saved
as flat JSON under `[Documents]\Qeynos\Presets\<plugin>\` and never touch the audio thread.
Full reference: [docs/PRESETS.md](docs/PRESETS.md).

## Tools (Phase 4)

| Tool | Type | Summary | Docs |
|---|---|---|---|
| W8-VITALGEN | Python + Claude API | Generate/tweak/validate Vital 1.5.x synth presets from natural-language descriptions. Claude fills a constrained parameter subset (osc/filter/env/LFO/FX/macros) merged onto an embedded known-good 1.5.5 base patch; pydantic clamps ranges and rejects bad enums so output always loads. Offline tests run without an API key. Skill: `.claude/skills/vitalgen`. | [docs/W8-VITALGEN.md](docs/W8-VITALGEN.md) |
| W4-SESSION-BOOTSTRAP | Python + FL MCP | One-command FL Studio session bootstrap: a JSON template sets mixer track names + colors, channel→mixer routing, and loop mode via the FL Studio MCP controller (SysEx). Ships TECHNO (dark techno) + DNB (atmospheric dnb) templates; `apply`/`list`, `--dry-run` preview, idempotent, resilient to per-op failures. `tempo` is reported/skipped (no MCP command exists). Skill: `.claude/skills/flsession`. | [docs/W4-SESSION-BOOTSTRAP.md](docs/W4-SESSION-BOOTSTRAP.md) |
| W5-PROJECT-JANITOR | Python + FL MCP | Heuristic auto-name + auto-color for a running FL session via the FL Studio MCP controller (SysEx; transport copied from W4). Scans every channel + user-named mixer track, classifies by whole-word name keyword into 10 categories (KICK red / SNARE orange / CLAP pink / HAT yellow / PERC olive / BASS purple / VOX sky / PAD teal / LEAD green / FX grey; first-match-wins so `bassdrum`→KICK), and emits canonical rename + category recolor ops. Dry-run default, `--apply` to execute, `--only channels\|mixer\|both`, `--json`. Idempotent (clean session → 0 ops); unrecognised names + Master untouched; per-op failures are warnings. Offline gate: 52 checks (classifier incl. 11 ambiguous→untouched, op-list snapshot, mocked end-to-end). Skill: `.claude/skills/fljanitor`. | [docs/W5-PROJECT-JANITOR.md](docs/W5-PROJECT-JANITOR.md) |
| W6-SAMPLE-LIBRARIAN | Python (librosa + soundfile) | Analyze, rename + sort a sample tree. Detects BPM (librosa onset autocorr, files > 1.5 s) + key (chromagram vs Krumhansl, import-copied from voxrip; tonal categories only), classifies each file into a category folder (kick/snare/clap/hat/perc/bass/vocal/fx/synth/loop/other via filename keyword + duration fallback), and renames to `{key}_{bpm}_{name}`, moving into `<dest>/<category>/`. **Dry-run default**, `sort … --apply` to touch files, `--dest`, `--no-recursive`, `--json`. Never overwrites (collision → `_1` suffix), idempotent (leading key/bpm tokens stripped before re-prefix; sorted tree re-plans to 0 moves), and every apply writes an **undo manifest** replayed by `undo <manifest>`. Offline gate: 37 checks on synthesized fixtures (120-BPM click loop, C-major sample, one-shots) + tmp-dir plan→apply→undo round-trip. Skill: `.claude/skills/samplelib`. | [docs/W6-SAMPLE-LIBRARIAN.md](docs/W6-SAMPLE-LIBRARIAN.md) |
| W7-REFERENCE-GAP | Python (pyloudnorm + numpy/scipy) | Compare a reference (mastered) track vs your mix render → a self-contained **HTML report** (inline SVG charts, no CDN). Reports LUFS-I loudness for both + delta, 1/3-octave **spectral balance** diff (each spectrum mean-normalized so it shows tonal balance not level — bars up = brighter, down = duller vs ref), **stereo width by band** (side/total energy, 0=mono ~0.5=wide), and **kick fundamental** detection (parabolic-interpolated low-band peak) → nearest note + cents + tuning suggestion (`--key <root>` adds the move onto the track's key root). Plots are pure inline SVG (design decision: light deps, truly self-contained, crisp at any zoom). CLI: `reference_gap.py <ref> <mix> [--key C] [--out report.html]`. Offline gate: 24 checks on synthesized fixtures (known-LUFS pair, 50 Hz kick, bright-boosted mix, mono vs decorrelated) + HTML generated/self-contained/parseable. Skill: `.claude/skills/refgap`. | [docs/W7-REFERENCE-GAP.md](docs/W7-REFERENCE-GAP.md) |
| W9-VOXRIP | Python (demucs + librosa + rubberband) | Rip a vocal out of any song and conform it to a different track's key/tempo. demucs (htdemucs, CPU) splits `vocals_raw.wav` + `instrumental.wav`; librosa + Krumhansl-Schmuckler report BPM (with half/double alts + confidence) and key of both the song and the vocal; rubberband CLI (`-F` formant-preserving; librosa PV fallback) time-stretches to `--target-bpm` and pitch-shifts by the minimal semitone move onto `--target-key` (relative-mode aware), writing `vocals_conformed.wav` + `REPORT.md`. Torch stays in an isolated CPU-pinned uv env; offline tests need no weights/network. Skill: `.claude/skills/voxrip`. | [docs/W9-VOXRIP.md](docs/W9-VOXRIP.md) |
| W1-RUMBLE-BASSLINE | FL piano-roll script (`flpianoroll`) | Generate offbeat / rolling **rumble-bass** patterns that lock around a 4-on-the-floor kick (dark techno): notes AVOID the beats (kick positions); on-beat notes, when used, are ghosted under the kick. Four rhythm engines (Offbeat 8ths / Rolling 16ths / Gallop / Broken), root-dominant with scale passing tones (b2 / 5th / b7) on fills, two-tier accent/ghost velocities, seeded deterministic humanize (±velocity, ±≤5 ticks), fill-density ghosts. ScriptDialog inputs (root+octave, scale, pattern, bars, lengths, velocities, humanize, fills, seed). Pure `generate_bassline()` tested via a mock `flpianoroll` (12/12 offline). Installed to FL's *Piano roll scripts* folder. | [docs/W1-RUMBLE-BASSLINE.md](docs/W1-RUMBLE-BASSLINE.md) |
| W2-BREAK-CHOP | FL piano-roll script (`flpianoroll`) | Rewrite the **SELECTED** notes into jungle / dnb **chop** patterns (each note = one slice): **permute** (reorder slice content), **roll** (2/3/4 decaying retriggers), **stutter-gate** (shorten + repeat), **reverse** (emulated as a double-time repeat — FL's note API has no reverse flag; documented), all inside a master **Intensity** gate with per-op probability. Operates **in place on the selection** (whole-score fallback), preserves the total span **exactly**, protects the downbeat (**keep-first-beat**), seeded-deterministic + velocity humanize. ScriptDialog inputs (intensity, permute, roll chance/count/decay, stutter chance/gate, reverse chance, keep-first-beat, humanize, seed). Pure `chop_notes()` tested via the **shared** mock `flpianoroll` (16/16 offline). Installed to FL's *Piano roll scripts* folder. | [docs/W2-BREAK-CHOP.md](docs/W2-BREAK-CHOP.md) |
| W3-DARK-PROGRESSION | FL piano-roll script (`flpianoroll`) | Generate a **dark, in-key chord progression** (natural minor / phrygian / harmonic minor) plus an optional **hypnotic arp** riding above the pad (Afterlife / melodic-techno). Chords are built on **scale degrees** (strictly in-key); with **voice-leading** ON each chord is re-voiced to the **nearest-voice inversion** (± octave) minimising summed semitone motion (lowest-bass tiebreak). Presets: **Dark Pop** i-VI-III-VII, **Hypnotic** i-i-VI-VII, **Tension** i-bII-i-VII (phrygian), **Wander** i-v-VI-iv, **Random-seeded**. Seeded **sus2/sus4** colour on non-tonic chords; triad / 7th / add9 voicings. Arp (up/down/up-down/random, 8th/16th, 1–2 octave span, gate %) derives from the current chord's voicing, registered above the pad, seeded velocity + timing (≤5-tick) humanize. Two lanes (chords low, arp high), **appended** at the timeline selection start (or tick 0). Pure `generate_progression()` / `chord_voicings()` tested via the **shared** mock `flpianoroll` (19/19 offline, 44 asserts). Installed to FL's *Piano roll scripts* folder. | [docs/W3-DARK-PROGRESSION.md](docs/W3-DARK-PROGRESSION.md) |

## Building

```powershell
powershell -ExecutionPolicy Bypass -File build.ps1 <crate>   # e.g. grit
powershell -ExecutionPolicy Bypass -File build.ps1 -All
```

Each crate builds (release) → tests → bundles `.clap`+`.vst3` → validates with
clap-validator + pluginval (strictness 8) → installs. Requires `tools/bin/mingw64`
(portable MinGW-w64 binutils; gitignored) — `build.ps1` puts it on PATH automatically.

Offline audition renders are written to `renders/<plugin>/*.wav` by the crate tests.
