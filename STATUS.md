# STATUS

CURRENT: SOUND-PASS complete | STEP: done | ATTEMPTS: 0 | LAST-ACTION: SOUND-PASS SHIPPED — all 27 audible plugins auditioned+improved (NERVE/X-RAY excluded, passthrough). Real DSP fixes: FDN metallic ringing (MURMUR/CHAMBER/SEANCE fractional-delay mod), high-f0 vocal formant combing (shift.rs adaptive lifter), IMPACT+SNAP full re-authoring, OVERSEER paid-Ozone deep-dive (limiter TP/comp attack/classifier/ENRICH), TRACER DC, WIRE/HALT/DRIFT/PATINA/SHAPESHIFT/CLEAVE fixes. Consolidated final sweep caught+fixed a cross-agent SEANCE SIZE-sweep click (mod depth 0.9→0.5). GATES: cargo test --workspace GREEN + build.ps1 -All GREEN 30/30, 0 failures. Verdicts docs/SOUND-PASS.md; before/after WAVs renders/_audition/. NEXT: OVERSEER theme-fallback follow-up (Master infers theme from own mix when no Nodes — CHECKPOINTS-logged), then loop idle.
PUSH-PENDING: no
DONE: BOOTSTRAP, GRIT, EMBER, IMPACT, TRACER, OVERSEER, W8-VITALGEN, W4-SESSION-BOOTSTRAP, HARD-CHECKPOINT-1, DRIFT, WIRE, OUROBOROS, SWARM, SMUDGE, MURMUR, UNDERTOW, SNAP, SEANCE, W9-VOXRIP, VOXKEY, VOXFIT, UI-CORE-FIX, ASCEND, W1-RUMBLE-BASSLINE, W2-BREAK-CHOP, W3-DARK-PROGRESSION, HARD-CHECKPOINT-2, PRESET-SYSTEM, OVERSEER-ENRICH, FLYBY, CLEAVE, PLUCK, SHAPESHIFT, CHAMBER, HARD-CHECKPOINT-3, CARVE, NERVE, HALT, BANDAID, PATINA, X-RAY, CHORALE, W5-PROJECT-JANITOR, W6-SAMPLE-LIBRARIAN, W7-REFERENCE-GAP, PRESET-EXPANSION, BUILT-IN-MANUALS, SUITE-TRIAGE, PEDAL-UI, FINAL-CHECKPOINT
DESCOPED: GRIT Mode C (spectral STFT) → DEFERRED.md; OVERSEER Ozone hosting + tier-2 bus (by spec/design) → DEFERRED.md; W4 tempo application (no MCP command exists) → DEFERRED.md; WIRE true-per-bandwidth Opus internal-rate + real FEC/PLC (opus-rs 12k/24k paths buggy; PRD §5-sanctioned pre-LP approximation) → DEFERRED.md (NOT an attempt-budget descope; WIRE shipped full)

## LOG (append-only: date | item | outcome | how-to-test-in-FL)
2026-07-08 | SOUND-PASS (REVERB/TEXTURE family: MURMUR, CHAMBER, SEANCE, EMBER, SWARM, SMUDGE, OUROBOROS, CHORALE) + PATINA user follow-up | SHIPPED (one commit per plugin). **The #1 hunt — METALLIC RINGING — was real in the three FDN reverbs.** `suite_core::fdn::Fdn8` read its 8 delay lines at STATIC integer lengths, so any sustained tail rang on discrete comb modes (tail flatness → 0.00, dozens of narrowband modes; tin-can, not cathedral). Added **opt-in per-line delay modulation to the shared core** (`Fdn8::set_modulation`: `VarDelay::read_frac` fractional interpolated reads + ~0.1–0.2 ms slow decorrelated per-line wobble that sweeps the comb nulls and smears the modes). `set_delays` re-zeros the LFOs so a room change never adds to its crossfade step; **`mod_depth==0` (UNDERTOW + every existing consumer) is bit-identical** to the old core (`read_frac` == `read()` at integer delay — verified: UNDERTOW + full `cargo test --workspace --release` green apart from an unrelated concurrent-sibling CLEAVE edit). MURMUR enables it fully (tail modes **564→265, −53%**; endless_vaults 51→9). CHAMBER runs a lighter depth capped by its moving-source click guard (**875→560, −36%**; the `set_delays` phase-reset was needed to pass `moving_source_produces_no_click`). SEANCE incremental on the shimmer verb (**492→343, −30%**, tail width also improved). **The other five are GOOD-AS-IS with verified rationale, not defects:** CHORALE resonators sing in near-just tune (2 "Extreme" presets are intentional endless drones); SWARM clouds are dense/diffuse (2 shimmer presets are harmonic by design); EMBER + SMUDGE are spectral-FREEZE holds — the tonal tail is the source's own partials (656 Hz = 6th harmonic of the 110 Hz pad), the IMPACT/SNAP "METALLIC = intended harmonic content" precedent, not FDN clang; OUROBOROS resonant feedback is the plugin's identity (self-oscillates musically, no runaway). Infra: `suite_core::harness::write_wav_stereo` + `tools/tail_scan.py` (ringing-mode summariser) + an `#[ignore]`d `audition_render_musical_sources` per plugin → `renders/_audition/<P>/{before,after}/`. **PATINA (two direct user reports):** (1) "heavy phasing when mix is turned down" — the mix knob blended the wow/flutter-MODULATED wet against a STATIC dry (comb/flange, worst mid-mix). Fixed: blend against the TIME-ALIGNED clean reference (`dry15`), so mix crossfades only the degradation, never a delay-detune → no comb at any mix; wow offset scaled by mix so **mix=0 null still holds exactly**. Regression `partial_mix_does_not_heavily_phase` (mix 0.3 steady-tone envelope swing **8.37 dB → <3 dB**). (2) "just adds hissing / static machine" — `AGE_HISS` 0.5→0.35 + hiss gain 0.5→0.35 (~30% quieter; still ducks to input via `key_amount=1`). All 14 PATINA tests green incl. every null. Committed only my paths (never `git add -A`; the shared tree holds a concurrent OVERSEER agent's uncommitted CLEAVE/CHECKPOINTS work). No Fable, no ultracode. | FL: reopen **Qeynos MURMUR / CHAMBER / SEANCE** on a sustained pad — the tail is now a smooth wash, not a ringing tin-can (compare renders/_audition/{MURMUR,CHAMBER,SEANCE}/before vs after). Reopen **Qeynos PATINA**, load a tape preset and pull **Mix** down to ~30 % — the sound thins cleanly with **no flanging/phasing**, and the hiss no longer dominates the character.
2026-07-08 | SOUND-PASS (VOCAL CHAIN: VOXKEY + VOXFIT + shared shift.rs) | SHIPPED (full; one commit per plugin + one shared). Rendered default + every preset over synth_vocal AND harder synthesized cases (vibrato / breathy / two-note glide / **high-female 400 Hz** / 20 s sustain) → `renders/_audition/{VOXKEY,VOXFIT}/{before,after}/`, auditioned with `tools/audition.py` + direct inharmonic-residual measurement. **The real VOX-suite defect was in the SHARED engine** `suite-core/src/shift.rs`: the cepstral envelope lifter had a **fixed `fft_size/16` (128) quefrency cutoff**, which sits *above* the pitch-period quefrency for f0 ≳ 262 Hz — so on **high female-range vocals** the "envelope" tracked the harmonic comb and the reapplied envelope smeared inharmonic energy (gurgle/metallic). Fix: **pitch-adaptive lifter** — find the pitch-period rahmonic in the cepstrum, pull the cutoff to `0.75·period` (clamped `[32, fft_size/16]`), **frame-smoothed** (0.15/frame one-pole) so a peak-pick flicker can't step the envelope (that stepped SEANCE's shimmer → its `size_sweep_no_click` — caught + fixed). Low/textural material keeps the `fft_size/16` default → SEANCE byte-identical there. **Formant-shift inharmonic residual: f0=400 −4.6→−25.3 dB, f0=500 −6.4→−15.9 dB (≈20/10 dB cleaner); f0≤300 unchanged.** Regression test `high_f0_formant_shift_does_not_comb`; TRIAGE phase-wrap still bounded (`synthesis_phase_stays_bounded`). **VOXKEY**: no DSP change of its own — VERIFIED the TRIAGE fixes held (glide zipper-free 0-clicks; confidence gate no gurgle on breathy 0-clicks; no octave errors) and the high-f0 formant preservation is fixed via the shared lifter. 25 presets kept. **VOXFIT**: (a) **de-esser was killing the air** — the sibilant band was `x−low` = everything >5 kHz, so a strong de-ess pulled 11–18 kHz air down as hard as the sibilance (measured −20 dB De-Harsh Rip, **−33 dB Nyquist Sibilance Kill** — dulled the whole top). Rebuilt as a **3-way complementary split** (low<5k / sib 5–10k / air>10k, `low+sib+air=x`): only sib is ducked, air passes at unity. (b) **de-ess onset click** (infinite-ratio `gr` step, 3 outliers/render ratio≤19.5) declicked with a 0.5 ms `gr` one-pole. VERIFIED TRIAGE held: unity-formant PV bypass nulls <−40 dB (transparent tool); TILT/PROX/AIR settle ~11–30 ms; SIT musical. New air-preservation regression in `deesser_reduces_sibilant_band_only` (measured with an onset-robust band percentile). 22 presets kept. **Gates:** `cargo test -p suite-core -p seance -p voxkey -p voxfit --release` GREEN. NOTE: the full `cargo test --workspace` currently has a CHAMBER failure (`moving_source_produces_no_click`) from a CONCURRENT sibling's uncommitted chamber/fdn edits (chamber uses `suite_core::fdn`, NOT shift.rs — unrelated to the vocal chain); the orchestrator's final sweep must revalidate SEANCE+VOXKEY+VOXFIT together once sibling trees settle. Committed only my paths (never `git add -A`). No Fable, no ultracode. | FL: reopen **Qeynos VOXKEY** on a HIGH female acapella (formant offset ±) — the formants no longer gurgle/comb on high notes; hard-snap glides are zipper-free, breaths don't gurgle. Reopen **Qeynos VOXFIT**, push **De-Ess** hard on a sibilant vocal — sibilance ducks but the **air/sparkle survives** (esses no longer dull the whole top) and there's no per-ess click. Audition renders/_audition/{VOXKEY,VOXFIT}/ before/after WAVs.
2026-07-08 | SOUND-PASS (OVERSEER deep-dive — judged as a PAID Ozone competitor) | SHIPPED (full). **Reconciled a crashed Fable checkpoint (wip 1539b10) + completed the 10-item deep-dive.** The Fable agent added the mud/harsh feature axes + bell-move suggestion fields and wrote `plugins/overseer/src/audit.rs`, but **never declared `mod audit` — the whole audit suite was dead code that had never compiled or run**, and its mud/harsh thresholds were hand-guessed (never triggered on real material). Opus wired it in (`lib.rs #[cfg(test)] mod audit;` + `hound` dev-dep), reconciled the incoherences, finished. **3 real DSP/logic fixes, each with a regression test in audit.rs, all alloc/null/latency/PDC-contract-safe:** (1) **LIMITER true-peak (FIX-1)** — the brickwall limiter was sample-peak only; an fs/4 π/4 tone (sample −2.93 dBFS, true +0.13 dBTP) sailed 1.13 dB over the −1 ceiling. Added **latency-neutral 4x-oversampled true-peak detection in the sidechain** (`dynamics.rs Limiter::process` tp_l/tp_r); the ~22-sample OS group delay stays inside the 96-sample lookahead so audio latency/PDC/null are unchanged. fs/4 now −0.92 dBTP (audition.py agrees; kick +2.01→−1.02 dBTP). (2) **COMPRESSOR attack honesty (FIX-3)** — the RMS detector was a fixed 10 ms, flooring realized attack at ~15 ms t90 regardless of the knob; tied the detector to the attack (`Compressor::configure`, 0.5 ms floor / 10 ms cap) → 0.5 ms now realizes 1.9 ms t90, 5→16, 20→54. (3) **CLASSIFIER reese fix** — a detuned-saw reese trips ~22 onsets/s (beating) and read as KICK; `suite_core::classify::scores` KICK onset is now a BAND (1.2–9/s), BASS tolerates spurious onsets → reese=Bass, kick=Kick, pad(stereo)=Pad, vocal=Vocal. **ENRICH now earns its name (headline):** rewrote `suggest_from_features` to be **type-aware** — bass-domain sources only ever get a starved sub LIFTED (never cut); mud/harsh thresholds recalibrated on MEASURED values (mud>0.34→−4/−6@300, harsh>0.065→−5/−6@3.5k). On a planted-defect multitrack the assist moves MUD, HARSH and SUB toward the clean reference on all three axes (audition.py IMPROVED: deviation-sum 431→384, MUD 14.2→11.5, SUB_WEAK −13.7→−11.9). **E2E (item 8):** 4 stems → per-type Nodes → Master (DARK-TECHNO assist 0.3), gain-staged to the techno window — LUFS −8.6, TP −1.30 dBTP, crest 8.3 retained, sub≫harsh; audition.py raw→mastered IMPROVED. **Meters (item 5):** audition.py LUFS −8.63 vs internal −8.60 (Δ0.03 LU), TP within 0.07 dB. **LIMITATIONS (honest):** ~few-ms RMS-detector attack floor; 4x-OS true-peak leaves ~0.35 dB residual on a drifting near-Nyquist 12 kHz full-scale sine (fs/4 fully controlled); harsh-cut trigger near a clean-vocal baseline (declinable SUGGEST ghost); kick-forward break reads as KICK; mono pad reads as VOCAL (width is the separator — by design). **Gates:** `cargo test -p overseer --release` GREEN (47 tests incl. the wired-in audit suite); `build.ps1 overseer` (bundle + clap-validator + pluginval strictness-8). **classify.rs is a shared module but the changed API (mud/harsh fields, NFIELDS 12→14) is consumed ONLY by overseer's bus.rs/master.rs (both NFIELDS-parameterized) — workspace-safe by construction; every existing suite-core classify fixture is unaffected.** The suite-wide `cargo test --workspace --release` + `build.ps1 -All` final sweep (STATUS CURRENT: "pending final sweep") is the orchestrator's step and was NOT green during this pass ONLY because of a concurrent SIBLING agent's in-progress non-compiling edit (`plugins/flyby/src/lib.rs` — `ITD_BASE` unresolved); my committed tree excludes sibling working-tree edits and references the last-green flyby. Committed only my paths (overseer/*, suite-core/classify.rs, docs/SOUND-PASS.md, STATUS.md) — never `git add -A` (the shared tree holds siblings' uncommitted work). No Fable, no ultracode. | FL: reopen a **Qeynos OVERSEER Node** on a track + a **Master** on the bus. Node: play ~8 s, press LEARN — the SUGGEST row now shows mud/harsh bell moves (e.g. "Mud −4 dB @300 · Harsh −5 dB @3.5k") + APPLY writes them; a bass/kick's starved sub gets a low-shelf LIFT (never a cut). Master: the limiter is now true-peak-safe (push a hot mix; TP stays under the ceiling on inter-sample peaks); a reese classifies as Bass, not Kick. Audition renders/_audition/OVERSEER/ before/after WAVs.
2026-07-08 | SOUND-PASS (low-end / distortion family: GRIT, UNDERTOW, TRACER, PATINA, WIRE) | SHIPPED (full, one commit per plugin): rendered every factory preset + default over genre-right musical sources (`suite_core::testsig`), auditioned each with `tools/audition.py`, judged "would a producer keep this?", fixed the shortfalls with regression tests, re-rendered + `compare`'d IMPROVED. Before/after WAV pairs in `renders/_audition/<PLUGIN>/{before,after}/` for the user's ears; verdict rows appended to docs/SOUND-PASS.md. **2 source-level DSP fixes + 1 P2 resolved, all alloc/null/latency-contract-safe with new regression tests:** (1) **TRACER** — systemic saturation-injected DC (up to −0.06 on hot Reese) fixed with a **wet-only ~10 Hz DC blocker** (`hot_saturation_does_not_inject_dc`); the moving pitch-tracked crossover verified **zipper-free on a glide** (`gliding_pitch_no_crossover_zipper`). (2) **WIRE** — the **P2-deferred dropout re-entry click is RESOLVED**: the post-concealment frame is ramped up from zero over ~2.7 ms in `dsp::run_frame` (drop-free path byte-identical), `dropout_reentry_is_click_free` regression test; worst vocal click ratio 24–40 → 10–15 across the loss bank (DEFERRED.md updated). **GRIT / UNDERTOW / PATINA needed NO DSP change** — verified clean by analysis (GRIT: no aliasing, inharm −94.9 dB on the hottest preset; musical env-follower pump; auto-gain flat within 0.4 LU across 3–18 dB drive. UNDERTOW: 40–90 Hz dominant, mud 20–34 dB down, duck breathes, tails natural. PATINA: 0 clicks, no harsh digital crackle edge, noise subordinate). **Preset banks pruned/re-leveled (judged on output, not param distance):** GRIT 18→18 (replaced fizzy off-brief "Nyquist Screech" → glossy-dark 808 destroyer "Sub Detonator"; declipped "Concrete Slam"), UNDERTOW 19→17 (cut 2 duplicates, declipped/re-leveled 7 over-hot presets into a ~−12 dBFS core, killed two +0.7 dBFS clippers), TRACER 21→20 (cut 1 filler, trimmed 4 clippers), PATINA 18→18 (declipped 5 hot presets, replaced near-dup "Barely There" → "Worn-In Glue", min pairwise dist 0.17→0.31), WIRE 22→20 (cut 2 filler, trimmed 2 hot). Mechanical bank gates kept GREEN (counts adjusted to curated sizes; ≥4-vs-default + pairwise-≥2 duplicate guards retained). **Gates:** `cargo test -p grit -p undertow -p tracer -p patina -p wire --release` GREEN (grit 10, patina 13, tracer 14, undertow 14, wire 11; each + the new `#[ignore]` audition render harness). Method: 5 parallel Opus workers over disjoint crates (none committed; orchestrator re-tested the combined tree GREEN then committed one-per-plugin). No Fable, no ultracode, no descope. | FL: reopen **GRIT / UNDERTOW / TRACER / PATINA / WIRE**, open the PRESET dropdown (shorter, cleaner banks) and audition against the before/after WAVs in renders/_audition/. TRACER hot-saturation presets are now DC-free; WIRE's packet-loss presets no longer click on dropout re-entry (feed a sustained vocal through Dying Stream / Discord Ghost and the gaps are seamless).
2026-07-08 | SOUND-PASS (IMPACT + SNAP generator banks) | SHIPPED (full): **FULL RE-AUTHORING of both drum-generator factory banks, judged preset-by-preset on OUTPUT AUDIO** (user verdict: "some presets completely useless… especially kick/snare generators"). Method: rendered the current banks (`cargo test` render harness), auditioned every WAV with `tools/audition.py` + a new `tools/audition_batch.py` (adds per-preset sub/low/body/lo-mid band-energy readout), FOUND the concrete defects, re-authored around use-archetypes, re-rendered+re-auditioned each until genuinely good. **IMPACT 25→16** (Warehouse Rumble / Wave 808 / DnB Punch / Deep Sub Roller / Character Drive): the old **Distorted** category had its energy at **250-500 Hz with no sub** (MUD/BOXY/DULL honky mid-kicks — the "useless" ones) + off-genre psy/clicky/tom filler; new bank is low-band-dominant on every kick (design rule: keep `fend` low + Tube/Tape not Fold + a sub floor so drive can't collapse the low end). **SNAP 24→15** (DnB Snare / Breakcore Snap / Wave Clap / Techno Rim / Texture Layer): old claps were boxy (200-800 Hz pile, `tone` too low) and several snares dull (no crack HF); raised clap `tone` 0.4→0.6-0.68 and kept snare `balance`/`snap` high for real crack. **DSP fixes (both alloc-free, with regression tests, no null/latency/contract break):** IMPACT gained a **~5 Hz output DC blocker** (`deep_sub_kick_has_no_dc_offset`) — deep-sub presets left ~0.003-0.005 DC; SNAP gained a **note-off declick ramp** (`note_off_deactivation_is_click_free`) — the hard `active=false` cut stepped the running noise floor to zero (a sub -74 dBFS click on short presets). **Flag deltas (before→after):** IMPACT DC_OFFSET 8→0, CLICK 4→0, TRUE_PEAK 2→0, MUD 6→2, BOXY 6→2; SNAP BOXY 8→0, MUD 6→0, CLICK 4→0. Remaining flags all archetype-justified: IMPACT 2 deep-808/industrial presets carry SUB_HEAVY/MUD/BOXY/DULL (intended deep octave sub + a kick has no top); METALLIC on saturated kicks/tuned snares = the harmonic overtone series of the fundamental (verified at integer multiples of `fend`/tune — NOT inharmonic FDN ringing; the clean Sine Sub Layer flags nothing); SNAP HARSH×4 = bright-clap crispness, SUB_WEAK×4 = a snare is not a sub instrument. **Mechanical bank gates kept GREEN** (count assertions lowered to the curated sizes: IMPACT ≥14, SNAP ≥12; the ≥4-vs-default and pairwise-≥2 duplicate guards stay). **Gates:** `cargo test -p impact -p snap --release` GREEN (impact 12 tests incl. new DC test; snap 16 tests incl. new declick test). Before/after WAV pairs in `renders/_audition/{IMPACT,SNAP}/{before,after}/` for the user's ears; verdict rows appended to docs/SOUND-PASS.md. No Fable, no ultracode. | FL: reopen **Qeynos IMPACT** and **Qeynos SNAP** and open the PRESET dropdown — the banks are now shorter, purpose-named, and grouped by use (IMPACT: Warehouse Rumble / Wave 808 / DnB Punch / Deep Sub Roller / Character Drive; SNAP: DnB Snare / Breakcore Snap / Wave Clap / Techno Rim / Texture Layer). Audition against a stock sample: warehouse/808 kicks are sub-dominant with a saturated tail, DnB Punch is tight+punchy, Deep Sub Roller is a clean layer sine, claps are wide+crisp (not boxy), DnB snares crack. Compare the before/after WAVs in renders/_audition/.
2026-07-08 | FINAL-CHECKPOINT | SHIPPED (full, [x]): **full-suite ultracode audit + remediation.** **Verdict:** 2 MAJORS confirmed end-to-end by two independent reviewers, ~12 minors fixed, several flagged-but-unconfirmed items resolved by direct code check, ledgers corrected. All fixes are **paint/GUI-thread only** except ONE tiny wait-free atomic store added to TRACER's audio thread (honest live-pitch telemetry). **CONFIRMED MAJORS:** (1) `suite-core/src/ui.rs` `crt_frame` — child UI clip **replaced** the parent clip (`set_clip_rect(inner)`), discarding a ScrollArea's viewport clip so bay content escaped the scroll viewport (repro: NERVE @150% scroll). Fixed to `inner.intersect(ui.clip_rect())` + **headless regression test** (`crt_frame_child_clip_stays_within_parent`) asserting child clip ⊆ parent clip. (2) CHAMBER `plugins/chamber/src/lib.rs` — floor-plan src/lis x/y were settable ONLY by dragging in the CRT (guardrail #3 violation); added a plain-text SRC X/Y · LIS X/Y row of standard knob/value-entry widgets OUTSIDE the glass (window 720→800 for the row). FLYBY node x/y flagged as the same class **and confirmed** (they ARE automatable params): added a collapsible "NODE X/Y (type-in)" grid + wrapped the editor in a ScrollArea so it never clips. **MINORS fixed (file:line):** flyby `xy_pad` per-frame `request_repaint()` → motion+animation-gated (`suite_core::ui::scope_repaint` helper; also applied to nerve/chorale/carve/cleave/pluck/shapeshift/xray bays — honors CRT-motion pref + ~8fps idle, guardrails #2/#6); `ui.rs` prefs save now temp-file+atomic-rename + re-read/merge (preserves other plugins' entries) + **keeps the old file on parse error** (no wipe) + unit test `prefs_merge_preserves_others_and_survives_corruption`; `slug_from_window_id` restructured so each strip falls back to its own input + test `slug_strip_falls_back_independently`; CRT blinking cursor moved into the bottom frame margin so it never overdraws the last telemetry row / interactive pads, `crt_lines` height 30+16n→38+16n (content no longer clipped); NERVE `lib.rs:594` Env A/B (S5/S6) unipolar streams now mapped directly (bars no longer floored at 50%); CHORALE `crt_frame` 76→86 (energy bars no longer clip); enclosure brand strip + top-highlight inset off the 10px rounded corners (no more floating amber nubs); tick-ring `RING_TICKS` 21→20 so majors land on both arc endpoints (symmetric); OVERSEER Master editor snapshots `bus().live_slots()` ONCE/frame and shares it across the enrich card + CRT census + Node grid (was 3 locks+allocs/frame); TRACER bay PITCH line now shows the LIVE detected f0 + note name via a new `pitch_hz` atomic (was static params only). **CLEAVE overflow math:** 680×560 + grown bay would clip the bottom knob row → window 560→620; suite-wide sweep of no-scroll fixed editors bumped for the +8px `crt_lines` growth (bandaid 500→516, halt 520→536, patina 560→576, _template 220→236, chorale 580→600). **LEDGER corrections:** DEFERRED.md SpectrumPublisher roster 25→**26 + X-RAY** (CHORALE also publishes, verified); OVERSEER tier-2 "how to resume" corrected (the memmap2 bus in `suite-core/src/bus.rs` ALREADY EXISTS — only step left is porting `overseer/src/bus.rs` onto it); WIRE FEC entry de-duped (kept top-level, cross-referenced in TRIAGE-P2). CHECKPOINTS.md gained FINAL CHECKPOINT block (PEDAL-UI skin+THEME toggles+prefs-persist, BUILT-IN-MANUALS '?' panel, SUITE-TRIAGE re-test of the 13 fixed behaviors) + a NERVE-section note that OVERSEER/EMBER/SEANCE/VOXFIT/VOXKEY have NO MOD section (can't be modulation targets). **Gates:** `cargo test --workspace --release` GREEN (63 result groups, 0 failed, +3 new regression tests) + **`build.ps1 -All` GREEN 30/30 crates, 0 gate failures** (clap-validator 0 failed each; pluginval strictness-8 SUCCESS each; all reinstalled). No Fable escalations; ultracode used once (this FINAL CHECKPOINT audit, PRD §8 whitelist item 3). **SUITE COMPLETE — all §7 items ticked.** | FL: this is a polish pass over the shipped suite — reopen any plugins and use the CHECKPOINTS.md "FINAL CHECKPOINT" list: scroll a tall editor (NERVE @150%) and confirm the bay no longer paints over the preset bar; check CHAMBER now shows SRC/LIS X/Y as type-in knobs under the floor-plan; toggle THEME → CRT motion / Console skin and confirm they persist across reopen (Documents\Qeynos\ui_prefs.json); open TRACER on a pitched source and watch the CRT DETECTED line show live Hz + note.
2026-07-08 | PEDAL-UI | SHIPPED (full, [x]): **CONSOLE v2** ("console inside a pedal", user-approved batch-3 mockup) as the suite-wide skin — ALL 28 editors, **paint-only** (every UI-CORE-FIX interaction byte-identical; verified: zero diffs touching from_size/set_latency/process/initialize/ScaledWindow base across the whole item). **Foundation (a4ccc7d, suite-core/src/ui.rs):** (1) pedal **enclosure** (machined body + amber brand strip + bevel + 4 corner screws, ~14 painter primitives) drawn by `ScaledWindow` via a window-id→slug hook (`qeynos-<slug>-window`), so knobs/toggles/enclosure themed **zero-touch** for every plugin; (2) **tick-ring machined knobs** + **footswitch-LED toggles** (paint branch only); (3) **CRT bay** `crt_frame`/`crt_lines` — recessed bronze-black glass, amber phosphor (#ffb000 ≈13:1, dim ≈5.4:1 — guardrail #5), **static** scanlines, cursor blink gated by CRT-motion pref and requesting only ~8 fps repaint (guardrail #6); (4) per-plugin persisted **Console skin + CRT motion** toggles in the size-menu (THEME section), stored in `[MyDocuments]/Qeynos/ui_prefs.json` (NO plugin Params change; Console-off = wholesale minimal-dark fallback — guardrail #2 + THEME-OFF done-bar; host reduced-motion undetectable under baseview ⇒ explicit toggle, documented in docs/UI.md). **Retrofit (7 commits, keepers first, TRIAGE cut-candidates last):** pilots GRIT+_template (a4ccc7d), then 89240c0 ember/impact/tracer/overseer(Node+Master, tier-1 bus untouched), 7de28ac drift/wire/ouroboros/swarm/smudge, 8aea785 undertow/snap/seance/voxkey/voxfit (VOXKEY's live detected→target atomics readout moved INTO the CRT per SPECS), 421c18f flyby/cleave/chamber/carve (FLYBY xy-pad, CHAMBER floor-plan, CARVE reduction meter wrapped in crt_frame with drag behavior + meaning-colors unchanged; CLEAVE's operable step grid deliberately kept OUTSIDE the bay per guardrail #3, bay shows live playhead), 129dc70 halt/bandaid/patina/xray/chorale (X-RAY analyzer = the CRT glass, functional multicolor slot curves + legend hover/click solo intact), 03089c0 murmur/ascend/pluck/shapeshift/nerve. Every bay shows HONEST live telemetry (existing atomics or the params also on the knobs — guardrail #3), ≤5 dense terminal lines (~≤110 px) or the plugin's real scope; no new cross-thread plumbing anywhere. Each batch independently re-verified by me (`cargo test -p <crates> --release` GREEN, 351 tests total) before its commit; usability fix en route: GRIT pilot bay compressed 7→4 lines (460px editor, guardrail-over-theme). **Gates:** `cargo test --workspace --release` GREEN twice (Stage 1 + Stage 3; 63 result groups, 0 failed) + **`build.ps1 -All` GREEN 30/30 crates, 0 gate failures** (clap-validator 0 failed each; pluginval --strictness-level 8 --skip-gui-tests SUCCESS each — every themed editor constructs headless; CLAP dir + VST3 junction + dist/ reinstalled). Method: 6 parallel Opus retrofit batches over disjoint crates (none committed; orchestrator re-tested + committed each), no Fable, no ultracode, no descope — 0 editors on the DEFERRED fallback. docs/UI.md gained the CONSOLE v2 section (architecture, hooks, guardrails, prefs). **NEXT §7: FINAL CHECKPOINT (the only remaining item).** | FL: reopen any Qeynos plugin (rescan not needed — same plugin ids, but all 30 were reinstalled): the editor is now a dark pedal enclosure with corner screws, tick-ringed amber knobs, LED footswitch toggles, and a recessed amber CRT screen showing that plugin's live telemetry (X-RAY's full analyzer, VOXKEY's detected→target note, CLEAVE's step playhead, OVERSEER's theme/LUFS…). Knob drag / Ctrl-fine / double-click reset / scroll / click-to-type / 75-150% zoom / presets / MOD / '?' manual all work exactly as before. Click the top-right **NN%** menu → THEME to toggle **CRT motion** (kills cursor blink) or **Console skin** (instant plain-dark fallback) per plugin — settings persist in Documents\Qeynos\ui_prefs.json.
2026-07-08 | BUILT-IN-MANUALS | SHIPPED (full, [x]): in-GUI usage manual for all 28 plugins. Foundation (cef9bd9): `suite_core::manual::Manual` tolerant `## `-section parser + `assert_manual_covers_params` cross-check helper + `ui::manual_button` ('?' anchored top-right of every editor -> closable/scrollable egui window rendering What It Is / Signal Flow (monospace) / Controls / Recipes). Content: 28 plugin docs extended + '?' wired into every editor via 6 parallel cluster commits (300cccd, b1e615c, 2186b1d, 82dfe70, a56085f, e6352fd), each crate gaining a manual_covers_all_params test (every param name must appear in its manual's Controls section). Final gate: `build.ps1 -All` **GREEN 30/30 crates, 0 gate failures** (clap-validator 0 failed each; pluginval --strictness-level 8 --skip-gui-tests SUCCESS each; CLAP dir + VST3 junction + dist/ reinstalled). Note: the item agent exited before running the final sweep; orchestrator ran it and ticked per LOG-trusted evidence (PRD 1.6). No Fable, no ultracode, no descope. **NEXT §7: SUITE-TRIAGE (user directive 2026-07-08, inserted before PEDAL-UI).** | FL: open any Qeynos plugin and click the **?** button top-right — the manual panel opens; every knob on the panel is documented under Controls, with Recipes for common moves.
2026-07-08 | PRESET-EXPANSION-B | SHIPPED (full — Batch B of 2, **completes PRESET-EXPANSION; §7 box now [x]**): deep purpose-named factory banks for the final **16 plugins** (drift, ouroboros, swarm, smudge, murmur, flyby, cleave, pluck, shapeshift, chamber, carve, nerve, halt, bandaid, patina, chorale), **ONE commit per plugin**, using Batch A's exact pattern. Every bank is genre-aware (dark-techno / atmospheric-dnb / Cynthoni-Sewerslvt vocabulary — e.g. 'Warehouse Rechop', 'Last Train Home'-style, 'Sewer Choir', 'Drowned Meadow', 'Numbers Station') and organized into 4-6 categories via `Preset.category`, which the shared `PresetBar` renders as preset-bar sections. **Quality gate PER PRESET, mechanical, in each crate's presets.rs** (grit-mirrored `count_diffs` over that plugin's `Settings` fields + `bank_meets_expansion_quality_gate`): (1) loads (serde); (2) >=4 params vs default; (3) pairwise >=2 vs every other preset (no near-duplicates); (4) unique names + every preset categorised; render-passes-universal stays enforced by each crate's existing render/`presets_pass_universal` loop (untouched, incl. its >=5/>=6 length checks). Counts before->after: drift 6->16, ouroboros 6->18, swarm 6->18, smudge 6->18, murmur 6->18, flyby 6->17, cleave 6->18, pluck 6->18, shapeshift 6->18, chamber 6->18, carve 6->15, nerve 6->15, halt 6->18, bandaid 6->15, patina 6->18, chorale 6->18. **Total Batch-B factory presets: 277 (pluck landed at 19). GRAND TOTAL A+B: 591.** Special cases handled per directive: **CLEAVE** — the step-grid is persisted host state, not params; presets score only the automatable param space (slice_mode/sensitivity/grid_div/steps/swing/mix/out) via `count_diffs`, keeping the existing pattern+seed convenience keys so a freshly-loaded preset is audible; a `//!` note documents this. **HALT** — mode buttons are live performance state (not stored); presets vary only the persisted character params, 18 distinct combos confirmed. **NERVE** — presets are param-id keyed (applied live via `apply_values`, no runtime `settings_from_preset`); a test-only `settings_from_preset` in the gate's test mod drives `count_diffs`, live apply path unchanged, audio stays bit-exact passthrough. **Method:** 16 parallel Opus authoring sub-subagents (one per crate, each given grit as the reference pattern, editing ONLY its own presets.rs, NOT committing); the orchestrator independently re-ran `cargo test -p <crate> --release` (GREEN for all 16) before each commit, then **`cargo test --workspace --release` GREEN (exit 0, 63 test-result groups ok, 0 failed)**. No preset clipped a render (all authored with Out <= 0 dB + the per-plugin output ceilings). No Fable, no ultracode, no descope, no PUSH-PENDING. build.ps1 per-crate NOT run this batch (presets are embedded data; preset + render tests are the gate per the batch directive — spot build.ps1 auditions deferred to the FINAL CHECKPOINT sweep). **NEXT §7: BUILT-IN-MANUALS.** | FL: reopen any of the 16 plugins and open the top **PRESET** dropdown — the FACTORY list is now split into named category sections (e.g. HALT: Tape-Stop Feels / Stutter Feels / Reverse Sweeps / Half-Time Grooves). renders/<PLUGIN>/ are regenerated by `cargo test` for offline audition.
2026-07-08 | PRESET-EXPANSION-A | SHIPPED (partial — Batch A of 2; §7 box intentionally NOT ticked, Batch B finishes it): deep factory banks for **12 plugins** (grit, ember, impact, tracer, overseer, undertow, snap, seance, voxkey, voxfit, ascend, wire), ONE commit per plugin. Every bank is purpose-named + genre-aware (dark-techno / atmospheric-dnb / Cynthoni-Sewerslvt vocabulary — e.g. 'Warehouse Thump', 'Last Train Home', 'Drowned Ghost Sit', 'Sewer Codec') and organized into 5-6 categories via the existing `Preset.category` field, which now renders as **preset-bar sections**. **suite-core enabler (committed with grit):** `suite_core::ui::PresetBar::show` groups a tagged factory bank into one headed section per category (distinct categories in first-appearance order; untagged stragglers under a plain FACTORY heading; a fully-untagged bank like _template stays one flat FACTORY list — fully back-compat, no public-API signature change). **Quality gate PER PRESET, mechanical, in each crate's preset test** (new `count_diffs` over that plugin's `Settings` fields + a `bank_meets_expansion_quality_gate` test, mirrored from grit): (1) loads (serde parse); (2) differs from default in >=4 params; (3) differs from EVERY other preset in its plugin in >=2 params — the pairwise no-near-duplicate check; (4) unique names + every preset categorised; **rule-4 (render passes universal assertions) stays enforced by each crate's existing render_tests loop over the full bank** (untouched, incl. its >=5/>=6 length checks). Counts before->after: grit 5->17 (Kick-Driven/Vocal/Bus/Texture/Extreme), ember 5->24 (Pads/Fades/Freezes/Rhythmic/Textures), impact 5->25 (6 kick cats, all downward f_start>f_end sweeps), tracer 5->21 (Bass-Reese/Vocal-Formant/Movement/Static-EQ/Extreme), overseer Node 36 (6 types x >=6) + Master 10 (5 themes) — already deep from OVERSEER-ENRICH, its tests upgraded to the >=4/pairwise-2 gate with type/theme filtering intact, undertow 6->19, snap 6->24, seance 6->28, voxkey 7->24 (2 existing nudged to clear >=4-vs-default), voxfit 6->22, ascend 6->22, wire 6->22 (1 existing nudged; full codec feature set, not the Plan-C valve). **Total Batch-A factory presets: 314.** Fixes during work (all preset-side, never assertion-side): undertow 'Basement Pressure' peaked +0.32 dBFS -> pulled out-trim to land -0.68 dBFS (universal ceiling); a few existing presets gained a category tag + a param nudge to satisfy the stricter >=4 gate. **Gates:** each crate `cargo test -p <crate> --release` GREEN, then **`cargo test --workspace --release` GREEN (exit 0, 0 failed, 63 test-result groups ok)** — catches the shared PresetBar change across all 30 crates. build.ps1 per-crate NOT run this batch (presets are embedded data; the preset + render tests are the gate per the batch directive — spot build.ps1 auditions deferred to the FINAL CHECKPOINT sweep). No Fable, no ultracode, no descope, no PUSH-PENDING. Method note for Batch B: 10 of the 12 were authored by parallel Opus subagents (one per plugin, each given grit as the reference pattern, editing only its own crate, NOT committing); grit (pattern + the suite-core UI change) and overseer (dual-bank special case) were done in-session; every subagent result was independently re-tested with `cargo test -p <crate> --release` before its commit. **NEXT §7: PRESET-EXPANSION Batch B** — the remaining ~16 plugins (drift, ouroboros, swarm, smudge, murmur, flyby, cleave, pluck, shapeshift, chamber, carve, nerve, halt, bandaid, patina, chorale). | FL: reopen any of the 12 plugins (**GRIT/EMBER/IMPACT/TRACER/OVERSEER/UNDERTOW/SNAP/SEANCE/VOXKEY/VOXFIT/ASCEND/WIRE**) and open the top **PRESET** dropdown — the FACTORY list is now split into named category sections (e.g. GRIT: Kick-Driven / Vocal / Bus / Texture / Extreme). Audition the new banks; OVERSEER Node/Master filter their banks by the auto-detected instrument type / session theme. renders/<PLUGIN>/ are regenerated by `cargo test` for offline audition.
2026-07-08 | CHORALE | SHIPPED (full, [x]): resonator bank (SPECS "CHORALE", **the LAST Phase 3 plugin — Phase 3 COMPLETE**). Fresh crate `plugins/chorale` from _template, **NO suite-core API change** (reused `spectrum::SpectrumTap`, `stft::Stft`, harness/ui/presets/testsig — no rebuild-all). **Recovery first (PRD 1.6):** the session opened on a dirty tree — X-RAY had shipped (LOG: build.ps1 -All GREEN, all reinstalled) but only committed suite-core/spectrum.rs + plugins/xray + a CHECKPOINTS entry; its **25-plugin SpectrumPublisher retrofit + docs/XRAY.md + STATUS/PRD/README/DEFERRED edits were left uncommitted**. Committed them as `wip(recovery)` (never stash/reset) before starting. **DSP core `dsp::ChoraleCore`** (stereo, shared verbatim with tests): **12–24 extended-Karplus-Strong resonators** preallocated at MAX_DELAY 4096; each = FracDelay (Catmull-Rom, copied from PLUCK/FLYBY) + 2-tap damping LP + all-pass fine-tune, **PLUCK's cent-accurate tuning solve** (`delay_read = period − p_damp − p_ap`) verified **±10 cents**. **Continuously excited** (not strummed): the mono input feeds every resonator at a per-resonator gain; the feedback path is **tanh soft-clipped** so the high-feedback loops **self-limit** under continuous drive without runaway (memoryless ⇒ no pitch shift), and the summed wet is **DC-blocked** (~5 Hz). **feedback_for** maps Decay→per-resonator loop gain (0.3–18 s, clamp 0.80–0.99993). **Tuning sources:** (a) **Scale/Chord** — Root (0–11) + a **12-type table** (m/M triad, m7/M7, sus2/4, 5th, m/M pentatonic, Phrygian, Dorian, Octaves) filled `offset[i%L] + 12·(⌊i/L⌋ % SPAN_OCT=5)` so the bank fans across ≤5 octaves then **wraps+stacks** (duplicates detune under Spread → chorus; also keeps Octaves/Sub presets off Nyquist); (b) **MIDI Held** — held-note bitmap `[bool;128]` drained per block, voice-assigned low→high, octave-stacked, alloc-free fixed-array `sort_unstable`; (c) **Key Detect** — PLUCK's chromagram (`suite_core::stft` 4096/1024, split-borrow, confidence-gated) → minor/major triad on the detected root, **falls back to Scale/Root**. **Spread** = alternating ± cents. **Sympathetic weighting:** an input `SpectrumTap` (32-band constant-Q) refreshes every 2048 samples (block-size-independent); per-resonator input gain = `base·((1−amt)+amt·norm_band_energy_at_pitch)` — the bank "sings" the source's strong bands (cheap, per the prompt). **Stereo** pans even/odd resonators L/R (equal-power) with width. **Null contract:** dry = zero-latency direct copy, `out=(1−mix)·dry+mix·wet`·outtrim, mix smoothed+primed ⇒ **mix=0 nulls < −80 dB** both ch; **Wet Solo** overrides mix. Alloc-free process (24 loops + SpectrumTap + STFT + DC blockers preallocated; ScopedFtz first line). **Params (13):** Source, Root (note-name s2v), Scale, Resonators (12–24 Int), Decay, Damp, Spread (ct), Sympathetic, Excite (0–2×), Stereo, Wet Solo, Mix, Out — floats percentage/rounded string_to_value. **GUI:** PresetBar (slug **chorale**) + **MOD** section (decay/damp/symp/mix listen targets) + a resonator-activity bar display + knob grid. **6 presets:** Sympathetic Am, Phrygian Drone Bed, Glass Choir, Sub Resonance, Wide Shimmer Strings, Tight Body. **Done-bars MET (no Fable, no ultracode, no descope) — 8/8 chorale tests GREEN:** (1) noise-excited A-minor → the **strongest 8** resonator peaks within **±10 cents** (windowed DFT peak search); (2) **decay scales RT** — burst-then-silence tail drop over a fixed window: short-decay > 20 dB AND ≥ long-decay + 12 dB; (3) **MIDI E2+G2+B2** retunes the bank (peaks ±15 ct, E2 ≫ scale-root C2); (4) **mix=0 null < −80 dB** both ch + **Wet Solo** non-silent & far from dry (ignores mix); + **sympathetic weighting** — white noise (constant-Q reads +3 dB/oct) → full weighting raises the top/bottom resonator-energy ratio **>1.6×** the flat case; + universal on all 6 presets (pink+chirp, both ch → renders/CHORALE/); + extremes fuzz (maxed/degenerate finite ≤0 dBFS); + MIDI-block alloc-free guard (thread-local counting allocator). **Debug (all test-side, core right first pass):** initial sympathetic test used a pure sine / then pink noise → both failed because resonators need broadband drive AND a constant-Q RTA reads pink ~flat (X-RAY's own note); switched to white noise + the correct (HF-emphasis) direction. Also capped octave spread at 5 (SPAN_OCT) after seeing the raw stack push high resonators past the probe/Nyquist. **build.ps1 chorale GREEN:** clap-validator **all PASSED / 0 failed**, pluginval **--strictness-level 8 --skip-gui-tests SUCCESS** (44.1/48/96k, blocks 64..1024, **Fuzz Parameters** over all 13 params incl Source=KeyDetect running the STFT + Mono/Stereo buses + process() alloc guard); chorale.clap → CLAP dir, chorale.vst3 → VST3 junction + dist/. No new deps. Deliverables: plugins/chorale/{Cargo.toml, src/dsp.rs, src/lib.rs, src/presets.rs, src/tests.rs}, workspace Cargo.toml member, docs/CHORALE.md, README row. **NEXT §7: Phase 4 automations — W5-PROJECT-JANITOR → W6-SAMPLE-LIBRARIAN → W7-REFERENCE-GAP (Python/uv tools; need FL+MCP for live verification, else build against fixtures + note CHECKPOINTS), then the POLISH endgame (PRESET-EXPANSION / BUILT-IN-MANUALS / PEDAL-UI) + FINAL CHECKPOINT.** | FL: **Manage plugins → Find more** (rescan) if CHORALE isn't listed, then put **Qeynos CHORALE** on an audio track or bus. At **Mix = 0** it's an exact passthrough — raise **Mix** to blend the resonance in. Pick a **Scale/Chord + Root** (or feed a MIDI chord with **Source = MIDI Held**, or **Key Detect** to track the input's key). Play material through it and the bank rings sympathetically; raise **Sympathetic** so resonators only sing where the source has energy. Set **Decay/Damp** for tail+tone, **Spread**/**Stereo** to widen, **Count** for density, **Excite** for drive; **Wet Solo** auditions the pure resonance. Try **Sympathetic Am** under a vocal/drum loop, **Phrygian Drone Bed**/**Sub Resonance** for dark beds, **Glass Choir**/**Wide Shimmer Strings** for lush washes, **Tight Body** for percussion body.
2026-07-08 | X-RAY | SHIPPED (full, [x]): shared cross-plugin spectrum analyzer (SPECS "X-RAY", Phase 3 — tier-2 bus CONSUMER; the PRD §5 "retrofit suite-core wrapper → rebuild-all → revalidate-all → reinstall-all" item). **(1) `suite_core::spectrum` (new module):** `SpectrumTap` = **32 constant-Q TPT-SVF bandpasses** (log-spaced 20 Hz–20 kHz, ≈⅓-oct, Q 4.32 — band count == `bus::NUM_SPECTRUM` so bands map 1:1 onto the slot fields NERVE pre-plumbed); per output sample all 32 `bp²` are integrated, per block each band's RMS is one-pole smoothed (0.5 keep) → published `{spectrum[32], peak, RMS}`. **Chose a per-sample constant-Q bank over a decimated FFT**: no window/OLA/ring state, alloc-free, and cheap enough to leave ON in every plugin — ~320 flops/sample ≈ **<0.5 % of a core @48k** (loose ceiling asserted in `cpu_cost_is_negligible`); constant-Q ⇒ pink reads flat like a standard RTA. `SpectrumPublisher` wraps tap + the NERVE claim pattern (session-scoped `new_instance_id` — NOT persisted, same CLAP state-reproducibility rationale; lazy claim in publish if the bus appeared late; heartbeat `beat` per block; `release()` in Drop is a nicety — heartbeat GC is the real reclaim). 4 spectrum tests GREEN (band-center span, low/high-noise energy lands in the right bands, TWO publishers on one file visible to a reader, CPU). **(2) RETROFIT — 25 plugins publish** (uniform 4-part: field + `init(sr, Generic, "NAME")` in initialize + mono-sum `feed` loop + `publish()` at end of process + Drop release): ascend, bandaid, carve, chamber, cleave, drift, ember, flyby, grit, halt, impact, murmur, ouroboros, patina, pluck, seance, shapeshift, smudge, snap, swarm, tracer, undertow, voxfit, voxkey, wire. **DEFERRED (DEFERRED.md, §1.5 tractable-majority):** OVERSEER (2-plugin bundle + tier-1 override bus interplay), NERVE (already a bus source publishing 8 mod streams — two publish paths in one slot would entangle), _template (kept minimal). **(3) X-RAY plugin (`plugins/xray`):** audio **bit-exact passthrough** (`out_gain(0.0)==1.0` exactly, asserted per-bit over sample corpus) + optional self-publish of its own input (kind `Xray`, **Publish** toggle; off ⇒ `release()` so it drops off the bus). **GUI = the product:** log-freq (20–20k, decade gridlines) / dB (+6…−96) panel; **one polyline per live slot** from `snapshot_live()` at GUI rate, color = golden-angle hue walk of slot index; **legend** rows (swatch · label · bus id · peak/RMS dB); **hover** a row ⇒ that curve bright, others `gamma_multiply(0.16)` dim; **click** ⇒ persistent solo-dim (click again or "clear solo" to release; hover overrides per-frame); **Freeze** holds the last snapshot vector. Params just Publish/Freeze/Out — **PresetBar deliberately skipped** (3 trivial params; documented in docs/XRAY.md). No new deps, no bus layout change (VERSION stays 1 — NERVE pre-plumbed the fields). **(4) DONE-BAR (PRD §4) GREEN — `two_instances_publish_distinct_spectra_and_reader_sees_both`:** two mapped handles in ONE process (two "DLLs") publish 40 blocks of 180 Hz-LP vs 7.5 kHz-HP noise through two SpectrumTaps; an X-RAY-style reader's `snapshot_live()` sees **BOTH** slots, in-half band energy **>4×** out-of-half for each, and `dominant_band(low) < dominant_band(high)`; + bit-exact passthrough test. **(5) GATES (suite-core touched ⇒ full sweep):** `cargo test --workspace --release` GREEN (**61 test binaries ok, 0 failed** — all 26 crates incl. suite-core 90 tests w/ 4 new spectrum, xray 3); **`build.ps1 -All` GREEN — 26/26 crates passed all gates** (clap-validator 0 failed each; pluginval --strictness-level 8 --skip-gui-tests SUCCESS each incl. Fuzz Parameters + process() alloc guard — proving every retrofitted publish path is RT-clean; CLAP dir + VST3 junction + dist/ reinstalled). Deliverables: suite-core/src/spectrum.rs (+lib.rs), plugins/xray/{Cargo.toml, src/lib.rs, src/tests.rs}, 25 retrofitted plugins/*/src/lib.rs, workspace Cargo.toml+lock, docs/XRAY.md, README row, DEFERRED.md entry, CHECKPOINTS.md entry. **FL caveat (also CHECKPOINTS):** the bus file is OS-wide so even bridged instances publish, but keep the suite un-bridged (FL default); a paused/removed publisher drops off the overlay after the 3 s heartbeat staleness — expected. NEXT §7 item: **CHORALE** (resonator bank — 12–24 KS waveguide loops tuned to held MIDI/scale/chromagram; PLUCK's cent-accurate KS tuning trick (frac delay + damping-LP + allpass phase-delay solved so `frac + p_damp + p_ap == sr/f0`) and `suite_core::pitch`/PLUCK's chromagram key-detect are the direct handoffs; per-resonator gain = input band energy at its pitch — the new `SpectrumTap` band energies may serve directly). | FL: **Manage plugins → Find more** (rescan — every Qeynos plugin was reinstalled), then put **Qeynos X-RAY** anywhere (master is natural; it's a bit-exact passthrough) and 2–3 other Qeynos plugins (GRIT/PATINA/MURMUR…) on playing tracks. Open X-RAY: **one colored curve per live instance** on the log-freq grid + a legend with each plugin's name and live peak/RMS. **Hover** a legend row to spotlight that instance, **click** to keep it soloed, **Freeze** to hold a moment, **Publish** off to hide X-RAY's own input curve. Stopped/removed plugins fall off after ~3 s. Un-bridged instances (FL default) recommended.
2026-07-08 | PATINA | SHIPPED (full, [x]): analog lo-fi character (SPECS "PATINA", Phase 3). Fresh crate `plugins/patina` from _template, **NO suite-core API change** (reused `dsp::{Oversampler2x, DelayLine, Svf, OnePole, EnvFollower, tape_soft, db_to_lin, ScopedFtz}` + `testsig::Rng` + harness/ui/presets/modlisten — no rebuild-all). **DSP core `dsp::PatinaCore`** (STEREO, shared verbatim with the mono harness path): a tape/vinyl chain where **every section is an exact identity at amount 0**. Flow: **wow/flutter** (crate-local Catmull-Rom `FracDelay` copied from FLYBY; **0.4 Hz wow + 8 Hz flutter + a slow random-walk** — all **one-sided**, added to a constant `BASE_DELAY`=16 so depth 0 ⇒ constant integer tap ⇒ pure delay; a 1 kHz sine gets audible f0 wobble) → **saturation** (`tape_soft`, **2× OS**; blended as `lerp(clean15, OS_shaped, drive)` where clean15 is a **`DelayLine(15)` of the wow output — NOT the OS's filtered output** — so drive 0 is a *bit-exact* 15-sample delay, not a slightly-filtered one) → **head-bump** low shelf (`y = x + (g−1)·LP(x)`, g=dB→lin(amount·9 dB), 60–120 Hz; g−1=0 at amount 0) → **azimuth** (right channel only: `x + amount·(allpass1(hp(x)) − hp(x))`, HF phase skew ⇒ mono-sum HF loss; identity at 0) → **dropouts** (shared **Poisson** trigger `rand<rate/sr` → random 20–140 ms dip to `1−depth`, **8 ms OnePole-smoothed** edges ⇒ click-free; gain primed to 1) → **+ keyed noise**: **hiss** (white − 0.6·LP(800) tilt, per-channel Rng), **hum** (50/60 Hz fundamental + 3 harmonics, shared phase/correlated), **crackle** (sparse 45/s sign-random impulses through a 3 kHz BP, per-channel), each × level × **key gain** = `(1−key)+key·clamp(env/0.15)` with **`EnvFollower::Rms`** (15 ms/90 ms — RMS not peak per BANDAID handoff) → **AGE** macro (age^1.3 curve **adds** to wow/flutter/sat/dropout-rate+depth/hiss/hum/crackle so age alone degrades even with base sections 0) → **mix vs latency-matched dry** (`DelayLine(LATENCY)`), ±0.999 ceiling. **Latency = `LATENCY = (BASE_DELAY−1)+15 = 30`** samples (the −1: FracDelay writes-then-reads, so an integer read arg of 16 is an *actual* 15-sample delay — caught by an impulse-peak alignment test); reported via `set_latency_samples`; dry compensated by the same 30 ⇒ **age 0 + all sections 0 nulls bit-exactly** vs the latency-matched dry. Wow/flutter add delay ON TOP of base (one-sided) so at partial mix an active wow is an intended dry/wet flange (documented). Alloc-free process (FracDelay + Oversampler2x + DelayLines + SVFs + Rng all preallocated; ScopedFtz first line; stereo path via `split_at_mut`). **Params (17):** Wow, Wow Rate (0.25–4×), Flutter, Saturation, Head Bump, Bump Freq (60–120 Hz), Azimuth, Dropout Rate/Depth, Hiss, Hum, Crackle, Hum 60 Hz (bool 50/60), Noise Key, Age, Mix (all 0–100 %), Out (±24 dB) — floats percentage/`num_s2v` string_to_value. **GUI:** PresetBar (slug **patina**) + **MOD** section (wow/age/mix listen targets) + AGE/MIX/OUT headline row + titled knob groups (WOW/FLUTTER · SATURATION/TONE · DROPOUTS · NOISE incl a **HUM 50/60** toggle). **6 presets:** Worn Cassette, Dusty Vinyl, Old Console Hum, Broadcast Ghost, Gentle Glue Age, Destroyed Tape. **Done-bars MET (no Fable, no ultracode, no descope, FIRST implementation) — 12/12 patina tests GREEN:** (1) **wow** — quadrature-demod the 1 kHz sine's phase track, Goertzel over 0.1–1.2 Hz → **peaks at 0.4 Hz ±0.1**, and 0.4 Hz power scales **>3×** from wow 0.25→0.9; (2) **keyed noise** — 200 Hz burst train, >3 kHz-band RMS **burst > 3× silence at key=1**, burst/silence within **0.5–2× at key=0** (constant floor); (3) **dropouts** — high rate/depth → min 30 ms-window RMS **< 0.45× median**, max output sample-Δ **≤ 1.25× the input's** (click-free); (4) **null** age0+sections0 < **−120 dB** vs 30-sample-delayed dry (also mix=0 with everything maxed < −120 dB), **AGE monotone** composite (THD+>6 kHz floor+0.4 Hz f0-mod) strictly increasing 0<0.33<0.66<1.0; + impulse-latency==30 alignment guard, all-6-preset universal (pink+chirp → renders/PATINA/), stereo+mono extremes fuzz finite ≤ full-scale. **Debug (all measurement-side/1-line, core right first time):** unit-impulse latency probe hit the 0.999 ceiling → probed at 0.5; key release 150 ms didn't fully close in a 0.5 s gap → 90 ms + measure the late 0.35–0.49 s of silence; single-bin Goertzel THD leakage floors age-0 degradation ~0.17 (not 0) → assert AGE *range* (age1 > 4× age0) instead of an absolute-clean bound (the null test already proves bit-exact clean). **build.ps1 patina GREEN:** clap-validator **14 passed / 0 failed / 6 skipped / 1 warning** (cosmetic 406 ms scan-time), pluginval **--strictness-level 8 --skip-gui-tests SUCCESS** (44.1/48/96k, blocks 64..1024, **Fuzz Parameters** over all 17 params + the process() alloc guard; buses Mono + Stereo); patina.clap → CLAP dir, patina.vst3 → VST3 junction + dist/. No new deps. Deliverables: plugins/patina/{Cargo.toml, src/dsp.rs, src/lib.rs, src/presets.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/PATINA.md, README row. | FL: **Manage plugins → Find more** (rescan) if PATINA isn't listed, then put **Qeynos PATINA** on any track/bus/master. Neutral (all knobs 0, Age 0) = transparent (30-sample latency, host-compensated). Load **Worn Cassette** for a subtle tape wobble+hiss, **Dusty Vinyl** for crackle+drift, **Old Console Hum** for 60 Hz hum+bump, **Broadcast Ghost** for a wobbly 50 Hz-hum lo-fi radio, **Gentle Glue Age** (Mix 60 %) to age a mix bus, **Destroyed Tape** for full damage. Turn the **AGE** knob to scale everything at once; set **Noise Key** high so the hiss/hum/crackle only rise with the signal (0 = constant floor); pick **HUM 50/60** for your region; push **Wow/Flutter** for pitch wobble, **Saturation/Head Bump** for warmth, **Dropouts** for the medium cutting out; pull **Mix** back to blend under the dry.
2026-07-08 | BANDAID | SHIPPED (full, [x]): multiband transient designer (SPECS "BANDAID", Phase 3). Fresh crate `plugins/bandaid` from _template, **NO suite-core API change** (reused `dsp::{Svf, EnvFollower, OnePole, ScopedFtz}` + `db_to_lin` + harness/ui/presets/modlisten — no rebuild-all). **DSP core `dsp::BandaidCore`** (mono, one per channel — plugin owns `[BandaidCore;2]`; shared verbatim with tests): **LR4 3-band split** (two crossovers via cascaded Butterworth TPT-SVF `Lr4` copied from TRACER/OVERSEER; low=LP(xLow), mid=HP(xLow)→LP(xHigh), high=HP(xHigh)). Per band a **transient detector** = fast peak env (1 ms attack / **60 ms release** — the long release stops the peak follower from *rippling* on low-frequency content and false-firing the attack detector each half-cycle) − slow peak env (40 ms attack / 150 ms release); `att_w = clamp((fast−slow)/slow, 0,1)` marks the rising onset, **`sus_w = 1 − att_w`** (SPL-style: a steady tone is fully "sustain") the body/tail; per-band `gain_dB = attack_dB·att_w + sustain_dB·sus_w` (±12 dB each), `db_to_lin`, **5 ms OnePole-smoothed** applied gain (primed to 1.0 → no zipper). **KEY: parallel-delta recombination `out = x + mix·Σ_b (g_b−1)·band_b`** — NOT `Σ g_b·band_b`. An LR4 split-sum is allpass-flat (unity mag, 360° phase lag) so a naïve sum would NOT null vs dry; adding only the per-band *difference* means every band's term is **exactly 0** when its gain is 0 dB (`g_b==1`) ⇒ all-neutral output == input **bit-exact** (nulls regardless of crossover phase). Solo overrides: `out = Σ_soloed g_b·band_b` (audition the shaped band, dry bypassed). Final `·out_gain` + ±0.999 ceiling (identity for sub-full-scale ⇒ null preserved; only bites boosted transients). Zero latency (minimum-phase). Alloc-free process (2 Lr4 + 6 EnvFollowers + smoothers preallocated; ScopedFtz first line). **Params (14):** Xover Low (20–800), Xover High (800–8k), per-band Attack/Sustain (6× ±12 dB Linear), per-band Solo (3 bool), Detector (0.5–2 env-time scale), Mix, Out — floats string_to_value (hz_then_khz for xovers, local `num_s2v` for dB/scale, percentage for mix). **GUI:** PresetBar (slug **bandaid**) + **MOD** section (latk/hatk/mix listen targets) + a global knob row (xovers/detector/mix/out) + three per-band **groups** (LOW/MID/HIGH each = ATTACK + SUSTAIN knobs + a highlighted **SOLO** toggle). **6 presets:** Punchier Kick, Tighter Room, Soften Hats, Drum Bus Snap, Pad Bloom, Full Squash-Reverse (solos never stored). **Done-bars MET (no Fable, no ultracode, no descope) — 9/9 bandaid tests GREEN:** (1) neutral (all gains 0, solos off) nulls vs pink+chirp input **< −80 dB** (residual ≈ −inf; parallel-delta proof) AND mix=0 nulls even with extreme shaping; (2) synthetic **kick+tonal-pad** mix → **low-band Attack +12 dB** raises the LOW band onset-to-sustain ratio while MID/HIGH stay **within ±1 dB** of neutral; (3) **mid-band Sustain −12 dB** lowers MID inter-onset RMS only (low/high ±1 dB); (4) attack sweep −12/0/+12 → **monotonic** LOW onset ratio; + solo isolates one band, universal on all 6 preset renders (both from a kick+pad mix → renders/BANDAID/), degenerate/maxed fuzz stays finite ≤ full-scale. **Debug notes (all measurement-side, core was right):** the detector's fast PEAK env rippled on low tones (false attack in the tail) → lengthened fast release to 60 ms + put the pad only in mid/high so the low band is a clean kick transient; a pitch-swept kick (180→55) built low-band energy *late* (in the sustain window) → used a kick body under the 300 Hz low crossover from t=0; and re-filtering the parallel-delta output to isolate a band adds a group-delay **comb** whose per-sample peak is phase-scrambled → measured onset as short-window **RMS** (energy), which averages the comb out and recovers the true per-band gain scaling. **build.ps1 bandaid GREEN:** clap-validator **14 passed / 0 failed / 6 skipped** (fixed a param-conversions fail: `v2s_f32_hz_then_khz` already emits its own unit — dropped the extra `.with_unit(" Hz")` that produced "8.0 kHz Hz"), pluginval **--strictness-level 8 --skip-gui-tests SUCCESS** (44.1/48/96k, blocks 64..1024, **Fuzz Parameters** over all 14 params incl the 3 solos + inverted-crossover extremes + the process() alloc guard; buses Mono + Stereo); bandaid.clap → CLAP dir, bandaid.vst3 → VST3 junction + dist/. No new deps. Deliverables: plugins/bandaid/{Cargo.toml, src/dsp.rs, src/lib.rs, src/presets.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/BANDAID.md, README row. | FL: **Manage plugins → Find more** (rescan) if BANDAID isn't listed, then put **Qeynos BANDAID** on a drum, bus, or full mix. Neutral = transparent bypass. Load **Punchier Kick** and push **Low Attack** for more thump (or pull **Low Sustain** to tighten); **Drum Bus Snap** / **Tighter Room** on a drum bus; **Soften Hats** (High Attack down) tames harsh hats; **Pad Bloom** lifts sustain on pads/keys; **Full Squash-Reverse** flips attack↓/sustain↑ for a reversed-envelope wash. Set the two **Xover** knobs so each band covers what you want to shape, tap a band's **SOLO** to hear its contents, and pull **Mix** back to blend under the dry.
2026-07-08 | HALT | SHIPPED (full, [x]): performance buffer FX (SPECS "HALT", Phase 3). Fresh crate `plugins/halt` from _template, no suite-core change (no revalidate-all needed). **DSP core `dsp::HaltCore`** (stereo, shared verbatim with tests): a **4-bar circular capture** always recording (preallocated **32 s = 4 bars @30 BPM** at SR in `new()`/`initialize`; slower tempos clamp the read window — documented; alloc-free `process`). Four **momentary** modes, each a host-automatable **BoolParam button** AND a **MIDI note** (C1..D#1 = MIDI 36–39, MidiConfig::Basic; plugin ORs button||note into a `[bool;4]` held set): **TAPE STOP** (read rate ramps 1→0 as phase 0→1 over a synced **Stop Time** (1beat/½bar/1bar/2bar) or **Free** seconds; rate=`(1-phase)^γ`, γ from **Curve** = (1/3)·9^curve so 0→exp/0.5→lin/1→log; **Release** Ramp spins the tape back up then auto-disengages, or Instant), **STUTTER** (loops the last **1/4..1/64** slice; **retrigger every loop_len OUTPUT samples** so the period is exact **independent of pitch**; **Pitch Step** ±12 st multiplies the read rate per repeat **clamped ±2 oct** — decoupling period from rate was the fix for a geometric wrap-acceleration → rate→∞ → NaN; **Decay** = per-repeat gain; **Quantize** off/1/16/1/8/1/4 snaps the loop anchor to the beat grid via `ppq_pos`), **REVERSE** (read backward from the trigger point, rate −1), **HALF-SPEED** (rate 0.5). **Priority = last-pressed wins** (per-mode press `serial` counter; highest-serial held mode is active; release falls back to the next-most-recent, then Dry). **Click-free = a dual-reader 5 ms equal-power crossfade** (`sin/cos`): every engage/disengage/mode-change/loop-wrap snapshots the sounding reader into `prev` (keeps advancing coherently) and swaps a fresh `cur`; Dry is itself a reader that returns the live input, so engage & disengage share one path. **Null/latency:** zero latency; while idle (Dry + no fade in flight) the plugin returns the input **verbatim ⇒ bit-exact passthrough**; when active `out=(1-mix)·dry+mix·wet` so **mix=0 nulls exactly**. Transport-synced (tempo drives div/tape length, playhead drives quantize; free-runs at host tempo when stopped). **Fixed en route:** (1) `engage()` re-triggered the reader every block whenever a crossfade was mid-flight (`xfade_left` in the same-mode guard) → scrambled stutter/tape phase — removed, same-mode-held now never re-triggers; (2) the stutter pitch-rate runaway NaN above. **Params (all automatable):** 4 mode buttons + Stutter Div/Decay/Pitch Step + Stop Time/Stop Free/Stop Curve/Release + Quantize + Mix + Out (14). **GUI:** big highlighted mode toggle buttons + knob grid + PresetBar (slug **halt**) + **MOD** section (mix/out/decay listen targets — NERVE retrofit, `modulated_float` over the block snapshot). **6 presets:** Classic Stop 1 Bar, Fast Brake, Stutter 16th Decay, Reverse Sweep, Half Time Groove, DJ Kill (character knobs only; mode buttons are live performance state, never stored). **Done-bars MET (no Fable, no ultracode, no descope) — 9/9 halt tests GREEN:** (1) tape-stop 300 Hz sine glides **monotonically to <50 Hz** within the duration (zero-cross freq, quantization-tolerant, gradual-mid check); (2) stutter 1/8 @120 BPM looped-onset **period == 250 ms ±1 ms** across ≥4 repeats; (3) reverse output **xcorr >0.9** with the time-reversed buffer (and > forward by 0.2); (4) engage+disengage **max sample-delta ≤3× steady-state** (click-free) + universal; (5) inactive **bit-exact** passthrough (`to_bits()` equal, any mix) AND mix=0-while-active nulls <−120 dB through all four modes; + last-pressed-wins unit test; + universal (finite/≤0 dBFS/non-silent, both ch) on all 6 preset renders → renders/HALT/. **build.ps1 halt GREEN**: clap-validator 0 failed, pluginval --strictness-level 8 --skip-gui-tests SUCCESS (44.1/48/96k, blocks 64..1024, incl **Fuzz Parameters** over all 14 params + the process() alloc guard, buses Mono+Stereo); halt.clap → CLAP dir, halt.vst3 → VST3 junction + dist/. No new deps. Deliverables: plugins/halt/{Cargo.toml, src/dsp.rs, src/lib.rs, src/presets.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/HALT.md, README row. | FL: **Manage plugins → Find more** (rescan) if HALT isn't listed, then put **Qeynos HALT** on a drum bus / loop / full mix. Idle = transparent. Automate or click **TAPE STOP** at a phrase end for a tape/turntable brake (load **Classic Stop 1 Bar** for a slow build, **Fast Brake** for a quick halt); hold **STUTTER** for a beat-repeat (pick **Stutter Div**, add **Decay**/**Pitch Step**; turn **Quantize** on with the transport rolling so it locks to the grid — try **Stutter 16th Decay** or **DJ Kill**); **REVERSE** for reverse-fills, **HALF-SPEED** (**Half Time Groove**) for a half-time drop. Drive the four modes from a MIDI clip (notes **C1..D#1**) or automate the buttons; multiple held → last one wins. Everything is a 5 ms crossfade so it never clicks; leave **Mix** at 100% (pull down to blend under the dry).
2026-07-08 | NERVE | SHIPPED (full, [x]): suite modulation bus (SPECS "NERVE"), Phase 3 — the largest remaining architecture item: PRD §3 **TIER-2 cross-DLL/cross-process bus** + the suite-core per-param **"listen" layer** + a suite-wide retrofit. **(1) `suite_core::bus` (new module):** file-backed shared memory via `memmap2::MmapMut` over a **fixed-size file at `%TEMP%\qeynos-bus`** (created at full size before mapping, never grown live; **magic+version+slot-count header** — stale/incompatible files recreated on open). **64 fixed slots**, each { instance_id (0=free, CAS claim/release), plugin_kind tag, 32-byte user label, **8 mod-signal f32s** (the NERVE outputs), **32-band spectrum + peak/RMS** (pre-plumbed for X-RAY), heartbeat block counter + wall-clock last_beat_ms }. **Per-slot SEQLOCK**: writer bumps generation odd→writes→even; readers retry on odd/changed — wait-free, alloc-free, never a torn snapshot. All access through raw pointers into the map as atomics (documented safety; Bus is Send+Sync via atomic-only access); **no locks in process(); fixed-size, alloc-free after init**. **GC = wall-clock heartbeat staleness (3 s)**: stale slots read as dead and are CAS-reclaimed by any claim (no cross-process Arc). RT fast paths `read_mod_fast`/`resolve_instance` (no String/Vec). **5 bus tests GREEN**, incl. **two mapped handles in ONE process** (simulates two DLLs: writer publishes, reader sees values) and the **seqlock torn-read test** (writer thrashing all 8 signals to a common value on a thread over 2M reader iterations → reader NEVER observes mixed values), stale-slot reclaim, header-version-mismatch recreate, fill-to-64-full. Fixed en route: `new_instance_id()`'s `|1` collapsed consecutive ids. **(2) `suite_core::modlisten` (new module) + `ui::mod_section`:** persisted `ModRoutes` = {param_id → (source_instance, source_index 0–7, depth ±1, curve Linear/Unipolar/Squared/S-Curve)}; block-rate, alloc-free `offset_for`/`modulated_float` compute **modulated_normalized = clamp(base + depth·curve(signal))** and feed the plugin's DSP configure — **host param state untouched** (host/automation/GUI see base; modulation is a live additive offset like an internal LFO tool; the plugin's own smoother de-zippers). Audio thread only try_reads the RwLock (GUI-held lock ⇒ that block runs unmodulated). `ui::mod_section` = collapsible MOD editor (source instance picker from live bus labels, S1–S8, depth drag, curve, clear). 4 modlisten tests GREEN. **(3) NERVE plugin (`plugins/nerve`):** 4 LFOs (**8 shapes** incl. S&H/Smooth-Rnd/Exp-Pulse; free 0.01–20 Hz or **transport-synced** 4bar…1/16 via pos_beats), 2 env followers (own input, atk/rel), 2 random S&H (rate+slew), 4 bipolar macros summed into LFO streams → **8 float streams published at block rate** to its claimed slot; **bit-exact audio passthrough** (zero latency); session-scoped bus id (deliberately NOT persisted — a persisted random id fails clap state-reproducibility-flush; routes are session-live, documented). GUI: live 8-stream scopes, per-LFO shape/rate/sync/div/depth knobs, macro/env/S&H rows, PresetBar (slug **nerve**), editable bus label. **6 presets:** Slow Swell Bus, 16th Pump, Chaos Pair, Macro Desk, Breathe, Techno Pump 1/8. 5 dsp tests GREEN (sine span, env rise/fall, macro offset, S&H variety, synced phase-lock). **(4) DONE-BAR (PRD §4) GREEN — `nerve_lfo_modulates_listener_drive_round_trip`:** NERVE core publishing over one bus handle + a GRIT-style drive FloatParam listening over a SECOND handle in one process; effective drive tracks the 2 Hz sine LFO in **shape** (spans ~0→24 dB of the 0.5-depth route) and **rate** (≥3 base-crossings/s), unrouted param bit-equal to base. **(5) RETROFIT — 18 plugins now listen:** GRIT (reference: drive/depth/mix/out) + ascend, carve, chamber, cleave, drift, flyby, impact, murmur, ouroboros, pluck, shapeshift, smudge, snap, swarm, tracer, undertow, wire — each: persisted `#[persist="mod"] mod_routes`, `modulated_float` over the block-rate snapshot pre-configure, `ui::mod_section` after the PresetBar (2–4 curated Float targets each, mix/out/character knobs). **DEFERRED (DEFERRED.md, §1.5 tractable-majority clause): overseer** (2 plugins, 11 configure sites, tier-1 override interplay), **ember** (3 configure sites), **seance/voxfit/voxkey** (no single snapshot/configure choke point). **GATES:** suite-core touched ⇒ **`cargo test --workspace --release` GREEN** (all 25 bins, 0 failed; suite-core 86 incl. bus+modlisten, nerve 7) + **`build.ps1 -All` GREEN — 25/25 crates passed all gates** (clap-validator 0 failed each incl. nerve 13 passed/0 failed; pluginval --strictness-level 8 --skip-gui-tests SUCCESS each incl. Fuzz Parameters + the process() alloc guard; CLAP dir + VST3 junction + dist/ reinstalled). New workspace dep: `memmap2 = "0.9"`. Deliverables: suite-core/src/{bus.rs, modlisten.rs, ui.rs mod_section, lib.rs}, plugins/nerve/{Cargo.toml, src/dsp.rs, src/lib.rs, src/presets.rs, src/tests.rs}, 18 retrofitted plugins/*/src/lib.rs, workspace Cargo.toml+lock, docs/NERVE.md (+ full bus-architecture section + X-RAY handoff), README row, DEFERRED.md entry. **FL caveat (also CHECKPOINTS):** cross-plugin modulation needs instances un-bridged — FL's default is fine; the bus file is OS-wide so even "Make bridged" instances still map it. NEXT §7 item: **HALT** (performance buffer FX — 4-bar circular buffer, tape-stop/stutter/reverse/half-speed momentary modes, retrigger quantize; use `suite_core::testsig::FakeTransport` like CLEAVE). | FL: **Manage plugins → Find more** (rescan), then put **Qeynos NERVE** on any track (e.g. the master or a kick bus for the env followers). Type a label (e.g. "DESK") in its header, load **Techno Pump 1/8** (with the transport RUNNING so the sync locks) or **Slow Swell Bus**. Open any other Qeynos plugin (say GRIT on a bass) → expand its **MOD** section under the preset bar → for DRIVE pick source **DESK**, signal **S1**, drag depth to ~0.4 → the knob's *sound* pumps with the LFO while the knob itself (and automation) stays at its base value. **S5/S6** follow NERVE's own input level (sidechain-style movement), **S7/S8** are random. Un-bridged instances only (FL default is fine).
2026-07-08 | CARVE | SHIPPED (full, [x]): spectral ducker (SPECS "CARVE", Trackspacer clone), Phase 3. Fresh crate plugins/carve from _template. **NO suite-core API change** (reused stft::Stft 2048/512, dsp::{DelayLine, Svf, ScopedFtz}, testsig, harness, ui, presets — no rebuild-all). **DSP core `dsp::CarveCore`** (stereo, shared verbatim with tests): a sidechain STFT carves the main's matching bands. **Two STFT roles per sample in lockstep** (SMUDGE primary/secondary split-borrow): ONE mono **sidechain** analysis STFT (aux mono-summed) + ONE **main** STFT per channel. Per **30 ⅓-oct band groups** (20 Hz–20 kHz, bin→group table at init): SC band energy = Σ(|bin|·4/N)² → e_dB → **soft-knee** `over = softknee(e_dB − Threshold, knee)` → `frac = clamp(over/span,0,1)` → `depth_dB = MaxDepth·Amount·frac·tilt_weight` → **per-band-group attack/release** one-pole on the reduction envelope at **hop rate** (attack when deepening, release when letting go). **Sensitivity** sets knee width (12→2 dB) AND excess span (30→8 dB). **Tilt** ∈[−1,1] biases depth toward lows(−)/highs(+) via a weight in [1−|tilt|,1] (never exceeds MaxDepth). Per-bin gain = `10^(−env/20)` **interpolated across band edges** (log-freq lerp) so no spectral steps; main bins ×= gain, iSTFT/OLA. **Δ-listen** applies the complement `(1−g)` → outputs the carved residual (partition of unity ⇒ normal+Δ sum to the STFT-identity of dry). **SC-listen** passes the sidechain through. **Exact-bypass/null:** when max reduction < 1e-4 dB the per-bin multiply is SKIPPED → STFT identity; SC-silent ⇒ every band env=0 ⇒ unity gain. `out=(1−mix)·dry+mix·wet` with a latency-matched dry `DelayLine(FFT_SIZE)`; reports **2048-sample latency**. Alloc-free process (3 STFTs + fixed [f32;30] gain/energy/env arrays + dry delays preallocated; ScopedFtz first line). **Params:** Amount, Max Depth (0–24 dB), Threshold (−90–0 dB), Sensitivity, Tilt, Attack (1–50 ms), Release (20–500 ms), Listen (Off/Sidechain/Delta enum), Mix, Out — floats smoothed, all labeled string_to_value (added a `num_s2v` leading-number parser for the dB/ms/tilt params so clap-validator param-conversions round-trips). **GUI:** shared PresetBar (slug **carve**) + a live **14-bar per-band reduction meter** (`Arc<Vec<AtomicF32>>`, published from process when editor open, GUI reads) + knob grids. **Done-bars MET (no Fable, no ultracode, no descope, FIRST attempt):** (1) main=full-spectrum pink, SC=band-limited 500 Hz–2 kHz → in-band groups (700–1500 Hz) cut ≥ (MaxDepth−3) dB, clearly out-of-band (<150 Hz, >6 kHz) within **±1 dB** of dry; (2) SC silent → wet nulls vs latency-delayed dry **< −60 dB** (honest STFT round-trip bound), mix=0 **< −80 dB**; (3) Δ-listen + normal output energy sum within **1 dB** of dry; (4) gated SC → reduction-envelope 10→63% (attack) / 90→37% (release) intervals within **±50%** of settings; + tilt-bias test (tilt<0 cuts lows more, tilt>0 highs more), extremes fuzz (finite, ≤0 dBFS), all-6-preset universal. **9 carve tests GREEN; build.ps1 carve GREEN** — clap-validator **15 passed/0 failed/6 skipped/0 warnings**, pluginval **--strictness-level 8 --skip-gui-tests SUCCESS** (44.1/48/96k, blocks 64..1024, **Fuzz Parameters** exercised all params incl the Listen enum + process() alloc guard; buses **Mono + Stereo**, aux sidechain listed); CLAP→CLAP dir + VST3→junction + dist/. **6 presets:** Vocal Space, Kick Vs Bass, Master Bus Tuck, Aggressive Carve, Gentle Glue Duck, Delta Inspector. Deliverables: plugins/carve/{Cargo.toml, src/dsp.rs, src/presets.rs, src/lib.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/CARVE.md, README row. Renders → renders/CARVE/*.wav (6 presets, pink main × band-limited SC pulse train). NEXT §7 item: NERVE (suite modulation bus — the tier-2 CROSS-DLL bus builder per PRD §3: memmap2 shared file in %TEMP%\qeynos-bus + seqlock slots, and the per-param "listen" retrofit into suite-core → rebuild-all/revalidate-all/reinstall-all). | FL: **Manage plugins → Find more** (rescan) if CARVE isn't listed, then put **Qeynos CARVE** on the track you want carved (bass, pad, music bed) and route the ducking source (kick, vocal) into its **sidechain** input via the FL mixer. Load **Kick Vs Bass** on a bass to punch a hole for the kick, **Vocal Space** under a vocal, **Master Bus Tuck**/**Gentle Glue Duck** for subtle glue, **Aggressive Carve** for obvious spectral ducking. Set **Threshold** so only the loud SC parts trigger, **Sensitivity**/**Max Depth** for how hard, **Tilt** to aim the cut low/high, **Attack/Release** for feel. Flip **Listen = Delta** to hear exactly what's being removed while dialing in; the top meter shows which bands are ducking; **Mix = 0** is an exact bypass.
2026-07-08 | CHAMBER | SHIPPED (full, [x]): image-source space simulator (SPECS "CHAMBER", Eigen clone), the last + hardest Phase 2b clone. Fresh crate plugins/chamber from _template. **NO suite-core API change** — reused `suite_core::fdn::Fdn8` (Householder FDN, MURMUR's engine) verbatim as the late field, plus `dsp::{DelayLine, OnePole, ScopedFtz}`, ui, presets, harness, testsig — no rebuild-all. **DSP core `dsp::ChamberCore`** (stereo, shared verbatim with tests): a **shoebox** room (W×D×H, 2–40 m) with a draggable **source + listener**, rendered as two summed stages. **(1) Early reflections = true image-source cluster** to **order ≤3**: images enumerated over `(kx,ky,kz)` with `|kx|+|ky|+|kz|≤order` → **7/25/63** mirror images (3-D L1 ball, incl the direct path `k=(0,0,0)`). Mirror coord `image(k,L,s)= k·L+s` (k even) / `k·L+(L−s)` (k odd). Per image: **one tap into a shared Catmull-Rom `FracDelay`** at `delay=r/c` (c=343 m/s; **read-before-write** so the direct lands at exactly r/c), **gain** `(r_direct/r)^Distance · reflect_wall^(|kx|+|ky|)·reflect_floor^nf·reflect_ceil^nc` (reflect=√(1−α); vertical bounces split floor/ceiling by first-hit), **per-image one-pole HF-damp** from accumulated per-bounce hf-keep (direct = unfiltered passthrough), **equal-power azimuth pan** from the image's horizontal direction. Image targets recomputed at **block/control rate**; per-sample the read delays **rate-clamp** toward target (±0.5 samp/samp → click-free doppler on a moving source, like FLYBY) and gains/pans one-pole smooth (5 ms). **(2) Late field = `Fdn8`**: 8 line lengths scaled from the room **mean-free-path 4V/S** (mutually-prime-ish), **RT60 from Sabine** `0.161·V/ΣSα` (clamped 0.1–12 s, or a manual **RT60** override; 0=Auto), damping tilt from mean material HF character, input **pre-delayed** by the ER window (diag/c)+user Pre-Delay so the diffuse tail crosses in after the image cluster. **ER/Late** = equal-power balance of the two. **4 materials** per wall-group (Walls/Floor/Ceiling): Concrete (α .03, live/bright), Wood (.12, warm), Curtain (.55, dead/dark), Glass (.07, live/edgy) — each an absorption + per-bounce HF-keep pair. **Null contract:** the direct path IS the dry (order-0 image at r/c); zero reported latency; `out=(1-mix)·in+mix·wet·outTrim` with mix/out primed on first configure ⇒ **mix=0 = bit-exact input passthrough** (no PDC — the wet has no lag-0 component, like FLYBY/MURMUR). Wet DC-blocked + safety-clipped; final ±0.999 ceiling. Alloc-free process (63 image slots + shared ER FracDelay + Fdn8 + predelay DelayLines all preallocated; ScopedFtz first line). **§4 CPU RULE applied:** benched mean process()/512-block @48k release — **order1=0.6% / order2=1.8% / order3=4.0%** of the RT budget (63 images) → **well under 30%, so `ER Order=Auto`→order 3** (the 3→2→1+bigger-late-field descope ladder is coded via AUTO_ORDER but unneeded here). **Params:** Width/Depth/Height, Source X/Y/Height + Listener X/Y/Height (positions stored as 0..1 room fractions → room-independent dragging), Walls/Floor/Ceiling material, ER Order (Auto/3/2/1), ER/Late, Distance, Pre-Delay, RT60 (0=Sabine), Width, Mix, Out — floats smoothed, enums block-rate, all labeled string_to_value. **GUI:** shared PresetBar (slug **chamber**) + a custom **top-down floor-plan pad** (drag the amber **SRC** dot or the ringed **LISTEN** cross; direct-path line; W×D grid) + a live readout (Sabine RT60, direct distance/ms, image count) + knob grid. **Done-bars MET (no Fable, no ultracode, no descope):** (1) impulse → first (loudest) arrival == direct `r/c` **±1 sample** (geometry-computed); (2) late-tail **measure_rt60 within ±25%** of Sabine for a **small dead** (curtain-floor, ~short) and a **large live** (wood, ~2.3 s) room; (3) source swept mid-render → max sample-Δ ≤ 2× a static reference (**no click**); (4) **mix=0 null < −80 dB** both channels; + image-count 7/25/63, an extremes/bounds fuzz (40 m order-3 everything-maxed stays finite ≤0 dBFS), all-6-preset universal over pink+chirp, and the CPU bench. **9 chamber tests GREEN; build.ps1 chamber GREEN** — clap-validator **14 passed/0 failed** (fixed the param-conversions all-or-none check: Pre-Delay/RT60 needed unit-tolerant `string_to_value` — a shared `leading_number()` strips the " ms"/" s"/"Auto" suffix), pluginval **--strictness-level 8 --skip-gui-tests SUCCESS** (44.1/48/96k, blocks 64..1024, **Fuzz Parameters** exercised all room/material/order/balance params + the process() alloc guard; buses Mono→Stereo + Stereo); CLAP→CLAP dir + VST3→junction + dist/. **6 presets:** Small Dead Booth, Wood Room, Warehouse, Cathedral-ish, Tight Drum Room, Distant Hall. Deliverables: plugins/chamber/{Cargo.toml, src/dsp.rs, src/presets.rs, src/lib.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/CHAMBER.md, README row. Renders → renders/CHAMBER/*.wav (6 presets × pink+chirp). **HARD CHECKPOINT due after CHAMBER (end of Phase 2b, PRD §1/§7)** — next iteration is the ultracode revalidation sweep. | FL: **Manage plugins → Find more** (rescan) if CHAMBER isn't listed, then put **Qeynos CHAMBER** on any track/bus. Load a preset (**Small Dead Booth** for a tight vocal booth, **Warehouse**/**Cathedral-ish** for big live spaces, **Tight Drum Room** for punchy drums, **Distant Hall** for a far-off wash). On the **floor-plan pad** drag the amber **SRC** dot and the ringed **LISTEN** cross to move the source/listener — the readout shows the live RT60, direct distance and image count, and moving the source while audio plays gives a natural doppler glide. Set **Width/Depth/Height** for the room size, pick **Walls/Floor/Ceiling** materials (Concrete = live, Curtain = dead), balance **ER/Late**, push **Distance** for exaggerated falloff, add **Pre-Delay**, or dial **RT60** to override the physical decay (leave at Auto for Sabine). **Mix** blends dry↔room (Mix=0 = exact bypass).
2026-07-08 | SHAPESHIFT | SHIPPED (full, [x]): XY-morphing distortion (SPECS "SHAPESHIFT", Teuri clone), Phase 2b. Fresh crate plugins/shapeshift from _template. **NO suite-core API change** (reused dsp::{Oversampler4x, Svf, OnePole, DelayLine, tape_soft}, harness, ui, presets, testsig — no rebuild-all). **DSP core `dsp::ShapeshiftCore`** (stereo; shared verbatim with tests): `in → pre-gain → 4x OS → [shaper A/B/C/D] → bilinear XY blend → DC block → post-LP(SVF) → auto-gain → mix → out`. The morph, per oversampled sample: `y = Σ wᵢ·cornerᵢ(gᵢ·pre·x)` with **bilinear weights** `wA=(1-X)(1-Y), wB=X(1-Y), wC=(1-X)Y, wD=XY` (a **partition of unity** ⇒ convex combination ⇒ always bounded; corner A=(0,0) BL, B=(1,0) BR, C=(0,1) TL, D=(1,1) TR). **Local 8-shaper bank** (`Corner` enum, LOCAL to the crate — nothing added to suite-core): Tube tanh, Tape soft (suite `tape_soft`), Diode asym (positive half driven harder → even harmonics, DC-blocked), Hard clip, Sine fold, **Tri fold** (`(2/π)·asin(sin(x·π/2))`), **Cheby-3** (`4c³−3c`, clamped), **Bit soft** (quantise to 6 levels, blend 0.72·q+0.28·c) — each bounded ≤1.05. Per-corner **gain trim** scales the input before the curve. Whole blend runs **inside the 4x oversampler** (GRIT recipe); weights computed at base/control rate from **OnePole-smoothed X/Y** (5 ms, primed to target on first configure so a fresh core sits exactly at its XY from sample 0 — needed for the exact corner-null). **Orbit LFO** (built-in): `orbit_offset(shape, phase, radius)` adds an XY offset around the user point — **Circle** or **Figure-8** (lemniscate of Gerono), **free Hz** or **BPM-synced** (½/1/2/4-bar via `context.transport().tempo`), **Radius** 0–0.5, **Phase** offset; phase advanced per sample, published to the GUI via `Arc<AtomicF32>`. **PDC (GRIT/HARD-CHECKPOINT-1 discipline):** the wet path's 4x-OS group delay is measured (`Oversampler4x::measure_group_delay()` = 22 samp) and the dry path is delayed by the same integer, reported via `set_latency_samples` ⇒ partial mix stays sample-aligned (single coherent peak), `mix=0` nulls dry. **Post:** DC blocker (~5 Hz), SVF **Post LP** (tames fold/crush fizz), optional **Auto-Gain** (300 ms RMS match, ±12 dB clamp — GRIT's pattern), Mix + Out with ±0.999 ceiling. Alloc-free process (per-channel OS/DC/SVF + dry DelayLines preallocated; ScopedFtz first line). **Params:** X, Y, Corner A/B/C/D (8-curve enum), Gain A/B/C/D (±24 dB), Pre-Gain (−12…+36 dB), Orbit on/Rate/Sync/Division/Radius/Shape/Phase, Post LP, Auto-Gain, Mix, Out — floats smoothed, enums block-rate, all labeled string_to_value. **GUI:** shared PresetBar (slug **shapeshift**) + a custom **XY morph pad** (FLYBY `xy_pad` precedent — one click-and-drag rect, screen↔[0,1]² map, begin/set/end-bracketed drag of the user point; corner labels show each corner's shaper; when Orbit is on it draws the orbit path + a live white dot at the published phase) + knob grids. **Done-bars MET (no Fable, no ultracode, no descope, FIRST attempt):** (1) XY hard at **corner A** (0,0) nulls against "shaper A alone, same pre+corner gain" **< −60 dB** (residual ≈ −inf; repeated for **corner D** (1,1)); (2) **centre** XY (0.5,0.5) differs from **every** single corner (Pearson **corr < 0.99** with all four, driven at +15 dB pre so the curves differ in shape not just scale) while bounded; (3) **orbit @ 1 Hz** circle → **THD tracks periodically** (per-40 ms Goertzel THD sequence: (max−min)/mean > 0.4 AND autocorr at 1 s (one period) > autocorr at 0.5 s and > 0.3·zero-lag); (4) partial-mix **single-coherent-peak** (impulse @ mix 0.5, PDC-aligned) + **mix=0 null < −80 dB** vs latency-matched dry. Plus all-8-corners bounded ≤1.05 and an extreme-macro (pre+gains 24 dB, orbit 8 Hz, auto-gain) finite/≤0 dBFS fuzz. **10 shapeshift tests GREEN; build.ps1 shapeshift GREEN** — clap-validator 0 failed, pluginval **--strictness-level 8 --skip-gui-tests SUCCESS** (44.1/48/96k, blocks 64..1024, **Fuzz Parameters** exercised all corners/gains/orbit params + the process() alloc guard; buses **Mono + Stereo**); CLAP→CLAP dir + VST3→junction + dist/. **6 presets:** Warm-Fold Morph, Diode Drive Orbit, Cheby Shimmer, Bit Edge, Tape Corner, Full Chaos Orbit. Deliverables: plugins/shapeshift/{Cargo.toml, src/dsp.rs, src/tests.rs, src/presets.rs, src/lib.rs}, workspace Cargo.toml member + Cargo.lock, docs/SHAPESHIFT.md, README row. Renders → renders/SHAPESHIFT/*.wav (6 presets × pink+chirp). | FL: **Manage plugins → Find more** (rescan) if SHAPESHIFT isn't listed, then put **Qeynos SHAPESHIFT** on any track/bus. Load a preset from the top bar, then **drag the amber dot on the XY pad** — the four corner labels show which shaper each corner uses and the sound morphs between them. Turn **Orbit** on and watch the white dot fly the path (**Circle/Figure-8**, set **Radius**/**Rate**, or tick **Orbit Sync** + a **Division** to lock to the groove). Push **Pre-Gain** or a corner **Gain** to drive harder, pull **Post LP** down to tame fold/crush fizz, **Auto-Gain** keeps the level steady, **Mix** blends back the dry. A sustained synth, bass, or drum bus shows the morph best.
2026-07-08 | PLUCK | SHIPPED (full, [x]): Karplus-Strong strummer (SPECS "PLUCK", Strum clone), Phase 2b. Fresh crate plugins/pluck from _template; **effect WITH MIDI input** (MidiConfig::Basic) + audio input as the exciter. **NO suite-core API change** (reused stft/testsig/harness/ui; no rebuild-all). **DSP core `dsp::PluckCore`** (stereo out, mono/stereo in): **6 extended-Karplus-Strong strings**, each = a **Catmull-Rom fractional delay line** (copy-pasted verbatim from FLYBY's `dsp::FracDelay`) + a **one-pole (2-tap) damping LP** + a first-order **all-pass fine-tune**, all *in the loop*. Loop delay solved so `frac_read + p_damp + p_ap == sr/f0` (all-pass low-freq phase delay subtracted so the fundamental stays in tune while upper partials stretch → tuning verified **±10 cents**). Per-string **feedback derived from Decay** for a target ~60 dB sustain (0.3–12 s), computed per-string so higher strings ring shorter (natural). **Tuning sources (param):** (a) **Chord** — dark table (m/m7/sus2/m9/5th-stack/sus4) voiced low→high on a **Root** (base C2), **Spread** detunes ±cents; (b) **MIDI Held** — up to 6 held notes voice-assigned low→high, extras octave-doubled (held-note bitmap `[bool;128]` drained per block, alloc-free); (c) **Key Detect** — coarse **chromagram** via `suite_core::stft::Stft` (4096/1024, split-borrow closure so it's alloc-free), root+minor/major with a confidence gate, **falls back to Chord/Root** when unsure. **Excitation = the audio input:** an onset detector (fast/slow env + 40 ms refractory) fires a strum; on onset PLUCK captures the **next ~500 samples** of input (Hann-windowed — grabs the actual attack *forward* from the onset, not a stale past window) as the exciter **burst**, then injects it **staggered** across strings — **Strum Time 5–80 ms** split into 5 strides (6 strings/5 gaps ⇒ stride = strum/5), **Up/Down/Alternate**; **Vel→Bright** opens the damping on hard hits; **Continuous** mode also feeds the input into every string at low gain (droning sympathetic mode). **Body:** a **1024-tap modal IR** (sum of 6 decaying body resonances + a direct impulse, L2-normalized, generated at init) convolved into the wet path by **direct FIR** (cheap at this length; benched fine — no FFT conv needed), blended by **Body** amount; causal (tap 0) ⇒ **zero added latency**. **Stereo Alt** pans strings alternately L/R (equal-power); **Wet Solo** outputs pure resonance. **Null contract:** dry = zero-latency direct copy, `out=(1-mix)·dry+mix·wet`·outtrim, mix smoothed in-core (primed to target on first configure) ⇒ **mix=0 nulls < −80 dB** both channels. Alloc-free process (6 KS lines @ MAX_DELAY 4096, body rings, STFT all preallocated; ScopedFtz first line). **Params:** Source, Root, Chord, Decay, Damp, Strum Time, Direction, Exciter Gain, Continuous, Vel→Bright, Body, Spread, Stereo Alt, Wet Solo, Mix, Out (enums via nih `#[derive(Enum)]`; Root Int with note-name string_to_value; floats smoothed). **GUI:** shared PresetBar (slug **pluck**) + a cheap **6-bar string-activity display** (per-string env published via `Arc<Vec<AtomicF32>>`) + knob grid. **Done-bars MET (no Fable, no ultracode, no descope, FIRST attempt):** (1) C-minor trigger → each string's STFT peak within **±10 cents** of its chord fundamental (windowed single-freq DFT peak search); (2) tail RMS drops **>20 dB** over the decay window (2 settings, short+long); (3) strum per-string onset offsets staggered by **strum/5 ±20%** in the configured direction (Up increasing, Down decreasing); (4) **MIDI E2+G2+B2** retunes strings (peaks at those pitches, E2≫C2); + universal on all 6 preset renders (both channels) + mix=0 L&R null < −80 dB. **7 pluck tests GREEN; build.ps1 pluck GREEN** — clap-validator 16 passed/0 failed/4 skipped/1 warning (cosmetic), pluginval --strictness-level 8 --skip-gui-tests **SUCCESS** across 44.1/48/96k blocks 64..1024 (Fuzz Parameters exercised all 16 params incl Source=KeyDetect running the STFT + the process() alloc guard; buses Mono + Stereo); CLAP→CLAP dir + VST3→junction + dist/. **6 presets:** Dark Nylon, Metallic Cloud, Sub Harp, Sympathetic Wash, Staccato Machine, Detuned Dream. Deliverables: plugins/pluck/{Cargo.toml, src/dsp.rs, src/presets.rs, src/lib.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/PLUCK.md, README row. Renders → renders/PLUCK/*.wav. | FL: **Manage plugins → Find more** (rescan) if PLUCK isn't listed, then put **Qeynos PLUCK** on an audio track (drums, a vocal chop, a synth stab, noise) — the input's transients strum the strings. Load **Dark Nylon** for a warm minor chord, **Staccato Machine** for tight stabs, **Sub Harp** for a deep slow arp, **Detuned Dream** for wide shimmer. Pick **Chord + Root**, set **Strum Time / Direction**, dial **Decay/Damp**. Feed PLUCK from a **MIDI** track and set **Source = MIDI Held** to play chords on the strings; **Source = Key Detect** (+ **Continuous** on, low **Mix**) makes the strings track the input's key for a sympathetic wash. **Body** adds woody resonance; **Spread** + **Stereo Alt** widen it; **Wet Solo** auditions the pure resonance.
2026-07-08 | CLEAVE | SHIPPED (full, [x]): multi-slicer + transport-locked step sequencer (SPECS "CLEAVE", Slice clone), Phase 2b. Fresh crate plugins/cleave from _template. **SUITE-CORE CHANGE (PRD §4 deliverable, rebuild-all applied):** promoted ASCEND's crate-local fake-transport pattern into **`suite_core::testsig`** — `TransportFrame { playing, tempo, ppq_pos, bar_pos, bars_per_sample, beats_per_bar }` (plain data struct, host-type-free; the plugin fills it from nih_plug, tests from the driver) + **`FakeTransport`** (sample-accurate synthetic 4/4, configurable BPM + `with_time_sig`, `advance(n)`/`seek_samples`/`frame()`/`frame_stopped()`; 4/4 by default). Used by CLEAVE now, HALT later. **ASCEND keeps its own crate-local `TransportFrame` (untouched — could migrate at PEDAL-UI time; noted).** 2 new suite-core tests (sample-accurate 4/4 advance + beats/sample + 3/4). **DSP core `dsp::CleaveCore`** (stereo, shared verbatim with tests): a **2-bar rolling stereo capture ring** (sized 16 s = 2 bars @30 BPM); at each **pattern wrap** the last 2 bars are **latched into a playback snapshot** (so slice boundaries sit on the musical grid — you hear the previous 2 bars re-chopped) and re-sliced: **Grid** (1/8/1/16/1/32 → 16/32/64 equal slices) or **Transient** (streaming STFT 1024/256 **spectral flux**, adaptive `mean+k·std` threshold scaled by **Sensitivity**, each onset **backtracked to the nearest earlier zero crossing**; coarse-grid fallback if too few — STFT + all scratch **preallocated**, reused via reset() ⇒ alloc-free process). **Step sequencer** locked to the host playhead: internal pattern position advanced **sample-accurately** from `bars_per_sample`, re-synced only on a >0.05-bar discontinuity (seek) so steady playback doesn't jitter at block edges; **16–64 steps** divide the 2 bars; **Swing** delays odd steps ≤½ step. Per step (persisted lanes): slice index or **as-played** (slice at the step's time position), **gate** %, **reverse**, **pitch ±12 st** (resample read), **roll ×2/3/4** (scheduled sub-onsets tiling the step), **probability** (deterministic xorshift draw; 0 = always silent), **level**. **Grain voices** (pool 12, steal-oldest): windowed reads with a **5 ms raised-cosine fade** in/out (no clicks), linear-interpolated fractional reads (± for reverse). **Free-run when the transport is STOPPED = internal clock at the host tempo** (keeps slicing standalone; documented). **Null contract:** dry path is a **zero-latency** direct copy, `out=(1-mix)·dry+mix·wet`, hard ±1.0 ceiling (the unique continuous null-preserving bound) ⇒ **`mix=0` passes input through sample-exact**; the wet is a re-timed creative signal, no PDC (reported latency 0). **Params (automatable):** Slice Mode, Grid, Sensitivity, Steps, Swing, Density, Mix, Out. **Per-step lanes are PERSISTED host state** (`#[persist="grid"] RwLock<StepGrid>`, serde), NOT params (16–64×8 lanes ≫ automation tree / fuzzer) — read into the core each block via **try_read → cached** (RT-safe, no lock in process); automation tradeoff documented. **GUI:** shared PresetBar (slug **cleave**) + **Randomize**(Density)/Clear/Fill + a custom **multi-lane step-grid widget** (LANE selector Level/Gate/Pitch/Roll/Rev/Prob/On re-targets click/drag; bars show the lane value, reversed steps tint blue, roll ticks drawn, white **playhead** published from process via `Arc<AtomicU32>`) + knob rows. **Done-bars MET (no Fable, no ultracode, no descope):** (1) 120 BPM fake transport, grid slice, straight pattern → ≥28/32 onset-detected outputs land on the 16th grid **±5 ms**; (2) reversed step cross-correlates with the **time-reversed** source slice **>0.9** (asymmetric chirp slices; and > forward corr by 0.2); (3) **roll ×3** → exactly 3 sub-onsets tiling the step **±2 ms**; (4) **probability 0** step slot silent (RMS<1e-3, neighbour ≥10×); (5) **mix=0** L&R null vs dry **< −120 dB** (also transport-stopped) + universal on all 6 preset renders (warm cycle, both channels). **8 cleave tests GREEN; 73 suite-core tests GREEN (`cargo test -p suite-core --release`); build.ps1 cleave GREEN** (clap-validator 0 failed, pluginval --strictness-level 8 SUCCESS incl Fuzz Parameters exercising slicemode/griddiv/steps/sens/swing/density/mix/out + the process() alloc guard in both slice modes, buses Mono + Stereo); **build.ps1 -All GREEN — all 19 crates** (CLAP+VST3 reinstalled). New dep: `serde` on the cleave crate (persisted grid). **6 presets:** Straight Rechop, Rolls & Ghosts, Reverse Accents, Half-Time Flip, Jungle Scatter, Four Flat (each = global settings + a compact pattern archetype expanded into the grid on load, à la FLYBY's shape→nodes). Deliverables: suite-core/src/testsig.rs (TransportFrame + FakeTransport + 2 tests), plugins/cleave/{Cargo.toml, src/dsp.rs, src/presets.rs, src/lib.rs, src/tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/CLEAVE.md, README row. Renders → renders/CLEAVE/*.wav. | FL: **Manage plugins → Find more** (rescan) if CLEAVE isn't listed, then put **Qeynos CLEAVE** on a drum loop / break / busy pad. Hit play — the step-grid playhead runs and you hear the 2-bar buffer re-chopped; load **Straight Rechop** for a clean 1:1 re-slice, **Jungle Scatter** for chopped breaks, **Reverse Accents** / **Half-Time Flip** for fills. Set **Slice Mode = Transient** on a break so slices snap to the hits (raise **Sensitivity** for more). Use the **LANE** buttons to paint gate/reverse/roll/probability/level per step; **Randomize** (with **Density**) throws a new pattern; **Swing** adds groove; pull **Mix** down to blend the chop under the dry. Per-step edits save with the project.
2026-07-08 | FLYBY | SHIPPED (full, [x]): doppler spatializer (SPECS "FLYBY", Transfer clone), Phase 2b. Fresh crate from plugins/_template; **no suite-core API change** (no rebuild-all needed). **DSP core `dsp::FlybyCore`** (stereo, shared verbatim with tests): a mono source (L+R summed) flown around a **closed Catmull-Rom loop** of 4–8 nodes (listener at origin), phase-driven traversal (free Hz 0.01–20, or BPM-synced ½/1/2/4-bar from `context.transport().tempo`). Per **16-sample control block**: `path_position` → source (x,y)=node·Size → `r`, azimuth → per-block targets; per **sample** every target is slewed/smoothed (OnePole @10 ms). Four cues: (1) **doppler** = a Catmull-Rom **fractional delay** read at `delay = MIN_DELAY + doppler·(r/SPEED)`; the read delay is **rate-clamped to ±0.5 samp/samp** (read speed 0.5–1.5×) so sharp corners can't spike pitch; SPEED=100 (scaled c) makes musical sizes/rates audibly bend; (2) **distance** gain `R0/max(r,R0)` (R0=0.6, inverse-distance, clamped near origin); (3) **air** = one-pole LP, cutoff `∝ 1/r` (18 kHz near → few-hundred Hz far), blended by Air amount; (4) **equal-power pan** from `x/r` + optional **micro-ITD** ≤0.6 ms opposite-ear (own short frac-delay), then **Width** (M/S). Zero latency (delay IS the effect) → `mix=0` **nulls dry** (no lag-0 coherent-peak check — documented, like OUROBOROS). Alloc-free process (1.2 s delay buffer + ITD lines preallocated); ScopedFtz. **Params:** 8× node X/Y (nested n0..n7, ±2), Nodes(4–8), Speed, Sync, Division, Size(1–30), Doppler, Air, ITD, Width(0–200%), Mix, Out — all smoothed, all labeled string_to_value. **GUI:** custom **XY-pad widget** (follows suite knob input/paint conventions) — draws the path + listener cross-hair + **draggable node handles** (nearest-node grab, begin/set/end bracketed) + a **moving white source dot** (phase published from process via `Arc<AtomicF32>`, request_repaint) + listener→source azimuth line; three **shape buttons** (Circle/Ellipse/Figure-8, placed OFF-CENTRE so even Circle flies by → real doppler); all scalar params on knobs; shared PresetBar (slug **flyby**). **Done-bars MET (no Fable, no ultracode, no descope):** (1) circular sine → f0 (zero-cross on L+R) bends >2% AND autocorrelates at 1-cycle lag > half-cycle (periodic doppler at traversal rate, tracked over 3 cycles); (2) windowed L/R RMS ratio crosses 1.0 ≥ once/traversal (noise, circle → 2×); (3) far point (located from path geometry) has lower spectral centroid (naive Hann-DFT) AND lower RMS than near point (air+distance); (4) Figure-8 @ max speed (20 Hz): max |Δout| ≤ 2.5× a stationary reference (rate clamp holds). Plus universal on both channels for all 6 presets over pink+chirp, mix=0 L&R null < −80 dB, bounded on all 3 shapes, path smooth+closed. **9 flyby tests GREEN; build.ps1 flyby GREEN** — clap-validator 0 failed, pluginval --strictness-level 8 --skip-gui-tests 0 failed (Fuzz Parameters exercised the 16 node params + all scalars + the process() alloc guard; buses Mono→Stereo + Stereo), CLAP→CLAP dir + VST3→junction + dist/. **≥6 presets:** Slow Orbit, Fast Circle 1/2, Figure-8 Wide, Distant Flyover, Subtle Motion, Vertigo (shape+node-count encoded, expanded by settings_from_preset; user presets store explicit node coords). Deliverables: plugins/flyby/{Cargo.toml, src/dsp.rs, src/tests.rs, src/presets.rs, src/lib.rs}, Cargo.toml workspace member, docs/FLYBY.md, README row. Renders → renders/FLYBY/*.wav. | FL: **Manage plugins → Find more** (rescan) if FLYBY isn't listed yet, then put **Qeynos FLYBY** on any track — the top PRESET bar loads Slow Orbit / Vertigo / etc.; on the **XY pad** drag the amber nodes to reshape the flight path (or click **Circle / Ellipse / Figure-8**), watch the white dot orbit; **Speed** or **Sync**+**Division** sets how fast it flies, **Size** how far, **Doppler**/**Air** how strong the pitch-bend / distance-darkening, **ITD**/**Width** the stereo image. A mono synth or vocal shows the effect best.
2026-07-08 | OVERSEER-ENRICH | SHIPPED (full, [x]): OVERSEER v2 — "should KNOW, not be told" (SPECS "OVERSEER-ENRICH — v2", the twice-upgraded top user priority). **New suite-core module `suite_core::classify`** (allocation-free at block rate; heavy stats on the GUI/bus tick per spec): `FeatureExtractor` taps a Node's own INPUT and keeps rolling ~4 s stats via a cheap 11-band SVF filterbank + two envelope followers + the suite `PitchTracker` — low-band(<120 Hz) ratio, spectral centroid+tilt, discrete onset rate (rising-edge detector) + crest, pitch confidence + pitched-frame ratio, 5–9 kHz sibilance ratio, sustain (smoothed-envelope windowed mean/peak — robust to pitch-rate ripple), stereo width, level; publishes a compact `FeatureSummary` (12 scalars) + supports an exact-N-sample LEARN capture that finalises once and FREEZES. `classify(&FeatureSummary) -> (InstrumentType,confidence)` is a rule/score mapper over 13 types (KICK/BASS/RUMBLE/PERC/HATS/SNARE/BREAKS/VOCAL/PAD/LEAD/ATMOS/FX/BUS); onset-driven classes gated on `le(pitched)` so pitched pads/vocals never mislabel as percussion. `infer_theme(node reports + mix analysis) -> (SessionTheme,conf)` (DARK-TECHNO/DNB-BREAKS/AMBIENT/HOUSE-GROOVE/GENERIC). **suite-core API change (rebuild-all applied):** `Preset` gained `category: Option<String>` (serde `default` + `skip_serializing_if` — back-compat, user-preset files unchanged); `PresetBar` gained `.filter(Option<category>)` (extend, NOT fork — surfaces the matching bank first, OTHER below). **Node** (`overseer`): Instrument Type enum param (AUTO default + 13 types) + persisted `LearnPersist` (locked type + ghost suggestions); `NodeCore` runs a `FeatureExtractor` on the input (feature/type/LEARN publishing added to the same-DLL `Slot`), GUI shows guessed type + confidence + colored badge, type dropdown (applies context defaults), **LEARN button** (8 s progress → commit type + context defaults + ghost EQ/comp suggestions from measured low-band excess + crest, with APPLY), preset bar filtered by type. **Master**: `MasterShared` state + own `FeatureExtractor`; aggregates live Node reports + mix analysis (transport tempo, tilt, onset density, DR) → THEME shown on GUI; **ASSIST** knob (0=display, default 30%) + **SUGGEST-ONLY** toggle scale theme-derived nudges (EQ tilt / MB comp glue-vs-punch / limiter drive) — **`apply_assist` is BIT-EXACT identity at strength 0** so assist=0 changes NOTHING in the audio path (null done-bar); **LEARN THEME** (12 s → lock theme), summary card (theme + per-track type badges + assist moves), grid type badges/colors. **Context-tuned defaults** per type (`enrich::context_defaults`: KICK mono-low+fast comp; VOCAL gentle knee+presence; PAD wide+slow glue; etc.). **Thematic banks**: 36 Node presets (≥6 per KICK/BASS/VOCAL/PAD/PERC/BUS, purpose-named — Warehouse Thump, Rumble Bed Glue, Drowned Ghost Sit, Grief Wash, Warehouse Tops, Drum Bus Glue…) + 10 Master theme presets, all `category`-tagged. **Done-bars MET (no Fable, no ultracode, no descope):** all 5 classifier fixtures correct above margin (synth_kick→KICK, sliding saw→BASS, synth_vocal→VOCAL, noise-burst train→PERC, wide pad→PAD); white-noise/silence stay below margin; LEARN captures exactly N samples + commits the window's type even if a different fixture follows (tested at extractor AND over the bus with NodeCore); techno session (kick+rumble+pad streams @130 BPM through the Bus in-process)→DARK-TECHNO; assist@0 audio-null vs base render (< −120 dB); KICK-vs-VOCAL default diff table; every bank preset passes universal render assertions; old projects (no type param) default to AUTO cleanly. **71 suite-core tests + 33 overseer tests green; `cargo test --workspace --release` GREEN (39 bins, 0 failed); `build.ps1 -All` GREEN — all 18 crates (clap-validator 0 failed, pluginval --strictness-level 8 incl Fuzz Parameters exercising the new insttype/assist/suggestonly params + the process() allocation guard; CLAP+VST3 reinstalled).** New dep: `serde` on the overseer crate (for the persisted LEARN/theme state). Deliverables: suite-core/src/classify.rs (+ presets.rs category, ui.rs filter, lib.rs mod), plugins/overseer/{enrich.rs (new), bus.rs, node.rs, master.rs, lib.rs, presets.rs, Cargo.toml}, docs/OVERSEER.md, README. | FL: on a mix, put **Qeynos OVERSEER Node** on each track — the header shows its auto-detected TYPE + confidence (KICK/BASS/VOCAL/PAD/PERC…) and the preset bank filters to that type; press **LEARN**, play ~8 s of the track → it locks the type, applies context defaults, and offers SUGGEST → APPLY. Put **Qeynos OVERSEER Master** on the bus — it reads all the Nodes + tempo and shows the session **THEME** (e.g. DARK-TECHNO) with a summary card of every track's type; raise **ASSIST** from 0 to let it gently steer the master EQ/comp/limiter toward the theme (0 = advisory only, provably no audio change), or **LEARN THEME** over the loudest 12 s to lock it; **SUGGEST ONLY** keeps it advisory. Node/Master preset banks now filter by type/theme.
2026-07-08 | PRESET-SYSTEM | SHIPPED (full, [x]): suite-wide USER preset save/load + shared preset bar, retrofit into all 18 editors (SPECS "POLISH phase → PRESET-SYSTEM"). **Disk tier** in `suite_core::presets`: user presets at `[MyDocuments]\Qeynos\Presets\<plugin>\<name>.json` — **[MyDocuments] resolved via the Windows known-folder API** (`dirs::document_dir` → `SHGetKnownFolderPath(FOLDERID_Documents)`; verified it returns `C:\Users\vigor\OneDrive\Documents`, the OneDrive-redirected path, matching `[Environment]::GetFolderPath('MyDocuments')` exactly — NEVER a literal `%USERPROFILE%\Documents`). Same flat JSON as factory. Fns: `list_user`/`save_user`/`load_user`/`delete_user` (all return `Result`, GUI shows errors inline, never panics) + `sanitize_name` (strips path-illegal `<>:"/\|?*` + control chars, collapses whitespace, trims dots/spaces, idempotent; empty-after-sanitize rejected on save; overwrite allowed). **Preset bar** in `suite_core::ui::PresetBar`: one dropdown with FACTORY + USER sections, Save (overwrite loaded user preset), Save As (inline text field), Delete (user-only, two-click *Confirm?*), dirty dot (amber) when live params diverge from the loaded baseline (abs+rel epsilon). **Threading**: all IO is GUI-thread; loads apply via `ParamSetter` (begin/set/end bracketed, host-visible + undoable), saves snapshot param values on the GUI thread; **nothing added to process()**. **Unification**: two new generic helpers — `snapshot_params(&dyn Params)` (reads every param's plain value via `ParamPtr::unmodulated_plain_value`, keyed by nih-plug param id) + `apply_values(&dyn Params,&ParamSetter,&map)` (writes back via `raw_context` raw ParamPtr setters) — handle ALL user presets with zero per-plugin snapshot code; the bar captures a fresh generic snapshot as the "loaded baseline" after EITHER a factory or user load, so the dirty dot works uniformly. Factory presets keep their pretty-key JSON + the plugin's existing `apply_preset` callback (user presets are param-id keyed — both are "same flat JSON", documented). Per-plugin retrofit = ~8 lines: `PresetBar::new(plugin_id,&factory).show(ui,&*params,setter,|setter,p| apply_preset(&params,setter,p))` replacing the old ComboBox. OVERSEER (two plugins in one bundle) uses ids **overseer-node** / **overseer-master**; _template gained 3 demo presets + the bar. Done-bars MET (no Fable, no ultracode, no descope): disk-tier round-trip (save→mutate→load→exact restore incl. overwrite + delete), name-sanitization (illegal/empty/unicode/idempotent), list sort+ignore-non-json, real-Documents-path write test (leaves a documented sample `[Documents]\Qeynos\Presets\_selftest\Test Save.json`), snapshot/apply plain-value round-trip (Float/Int/Bool/Enum), dirty-logic tests — **61 suite-core tests green**; `cargo test --workspace --release` green (39 test bins); **build.ps1 -All GREEN** = all 18 crates (clap-validator 0 failed, pluginval --strictness-level 8 --skip-gui-tests 0 failed incl Fuzz Parameters + thread-safety + all buses, editor instantiated per validate; CLAP+VST3 reinstalled to CLAP dir + VST3 junction + dist/). New dep: `dirs = "5"` (workspace + suite-core, non-optional; pulls dirs-sys→windows-sys already in tree). Deliverables: suite-core/src/presets.rs (disk tier + tests), suite-core/src/ui.rs (PresetBar + snapshot_params/apply_values + tests), all 18 plugin editors, docs/PRESETS.md, README preset note, Cargo.toml/Cargo.lock. NEXT §7 item: OVERSEER-ENRICH (builds type-filtered banks on this same PresetBar — see NOTES). | FL: reopen ANY Qeynos plugin — the top PRESET bar now lists FACTORY + USER presets; pick a factory preset, tweak a knob (amber dirty dot appears), click **Save As**, type a name, Enter → it's written to `Documents\Qeynos\Presets\<plugin>\`; reopen the dropdown to load it back from the USER section; **Save** overwrites it, **Delete** (two-click) removes it. OVERSEER Node + Master each have their own bar/folder. Files live under your OneDrive Documents.
2026-07-07 | HARD-CHECKPOINT-2 | all gates green (18 crates + 6 py gates), 2 minors confirmed+fixed (SNAP rattle declick, voxrip docstring), 2 findings refuted | FL: rescan for the 8 new plugins since CP1, run the 3 piano-roll scripts live, verify ASCEND gets transport
2026-07-07 | W3-DARK-PROGRESSION | SHIPPED (full, [x]): FL Studio piano-roll script `pyscripts/DarkProgression.pyscript` (SPECS "W3 DarkProgression"; PRD §7). Generates a **dark, in-key chord progression** (natural minor / phrygian / harmonic minor) + an optional **hypnotic arp** above the pad (Afterlife / melodic-techno). Chords are built on **scale degrees** (diatonic thirds → strictly in-key); **voice-leading** ON re-voices each chord to the nearest-voice inversion (± octave) minimising summed semitone motion (lowest-bass tiebreak, first chord root position). Presets: Dark Pop i-VI-III-VII, Hypnotic i-i-VI-VII, Tension i-bII-i-VII (phrygian b2), Wander i-v-VI-iv, Random-seeded (tonic-start walk). Seeded **sus2/sus4** colour on non-tonic chords; triad / 7th / add9 voicings. **Arp** (up/down/up-down/random, 8th/16th, 1-2 octave span, gate %) derives from the current chord's voicing lifted above the pad; seeded velocity + timing (≤5-tick, arp-lane-only) humanize; chords grid-locked. Two lanes, **APPENDED** at timeline selection start (or tick 0) — W1 semantics. Pure `generate_progression()` / `chord_voicings()` (guarded `import flpianoroll`, no FL types) tested offline via the **shared** mock `flpianoroll` (`pyscripts/tests/test_darkprog.py` + `mock_fl.py`): **19/19, 44 asserts** — in-scale, grid boundaries, voice-leading strictly-less-than-root-position + bound, arp⊂chord/rate/gate, determinism, humanize bounds, apply() glue. Installed to `[MyDocuments]\Image-Line\FL Studio\Settings\Piano roll scripts\DarkProgression.pyscript` (byte-for-byte verified). how-to-test-in-FL: open Piano roll on a pad channel, **Tools → Scripting → Dark Progression**, set inputs, OK → in-key pad (grid-locked, voice-led) + optional arp above; APPENDS at selection/tick 0; same Seed → identical. Live-run is a CHECKPOINTS item (FL can't run piano-roll scripts headless). docs/W3-DARK-PROGRESSION.md + README row + CHECKPOINTS entry added.
2026-07-07 | W2-BREAK-CHOP | SHIPPED (full, [x]): FL Studio piano-roll script `pyscripts/BreakChop.pyscript` (SPECS "W2 BreakChop"; Phase 4 Taste-tailored). Rewrites the **SELECTED** notes into jungle / dnb **chop** patterns — each selected note is treated as one **slice**. All ops run inside a master **INTENSITY** gate (0-100 = probability each slot is touched at all; 0 = no change) with per-op probability and a FIXED RNG draw order (touched → permute → roll → stutter → reverse) so output is deterministic per seed: **PERMUTE** (reorder which slice's pitch/velocity plays in each time-slot — the amen shuffle — only among the touched slots, so the pitch multiset is a rearrangement, nothing added/removed), **ROLL** (subdivide a slot into 2/3/4 retriggers that tile the slice span exactly, velocity decaying by Roll-decay each hit), **STUTTER-GATE** (shorten + repeat: 2 sub-slots, each gated to Stutter-gate % of its sub-slot), **REVERSE** (**EMULATED** as a double-time repeat = two full-length retriggers into the next slice — FL's `flpianoroll.Note` exposes NO reverse/porta flag that flips slice playback, reverse is a Fruity-Slicer/audio-clip property not a MIDI-note one; the code still writes a real reverse flag when `REVERSE_AVAILABLE` and `hasattr(note,'reverse')` — DOCUMENTED). **RULES:** operate ONLY on the selected notes (fallback = the whole score when nothing is selected, matching FL script convention, documented); **span preserved EXACTLY** (every slot's output tiles `[slot_start, slot_end]`, and the slice with the **maximum end** — not necessarily the last-in-time — always sustains to its end even under a stutter-gate); **KEEP-FIRST-BEAT** protects the downbeat (earliest bar-aligned selected note, else the earliest) and emits it byte-for-byte unchanged; deterministic per seed; velocities decay / clamp ≥ 0.02, humanize applied to TOUCHED notes only (protected/untouched stay exact). **ScriptDialog inputs:** Intensity % (0-100), Permute amount % (0-100), Roll chance % (0-100), Roll count (2/3/4 combo), Roll decay % (0-95), Stutter chance % (0-100), Stutter gate % (5-100), Reverse chance % (0-100), Keep first beat (checkbox), Humanize velocity ± (0-40), Seed (0-9999). **DESIGN DECISIONS** (in file + docs): SELF-CONTAINED single file (FL script sandbox blocks file access); **REWRITE-in-place** (unlike W1's append) — `apply()` collects the selected note indices, runs the pure chopper, DELETES ONLY those indices (descending so earlier ones stay valid), then adds the chopped output; every unselected note is untouched; the chop logic is the PURE, importable `chop_notes(notes, ppq, ...)` (no `flpianoroll` dependency, plain dicts in/out), FL glue is `createDialog()`/`apply(form)`, `import flpianoroll` guarded so the module imports headless. **REFACTOR:** the mock `flpianoroll` was factored out of `test_rumble.py` into `pyscripts/tests/mock_fl.py` (Note class + score list w/ selection/deleteNote + ScriptDialog stub + `load_pyscript()` SourceFileLoader helper) now that W1 + W2 share it; `test_rumble.py` re-imports it and still runs 12/12. **OFFLINE GATE** `pyscripts/tests/test_breakchop.py` runs **16/16 GREEN** under `uv run --python 3.12` (both the plain-assert runner and `--with pytest pytest`, 28/28 across both piano-roll gates): asserts (1) only selected notes altered — unselected stay byte-for-byte through `apply()` (mixed selection + rewrite + whole-score fallback); (2) span preserved exactly across a sweep of op-mixes/seeds incl. a long LEADING note (max-end ≠ last-in-time) and stutter-heavy tails; (3) rolls subdivide correctly (roll_count notes tiling the slice contiguously with decaying velocity); (4) deterministic per seed (same identical, different differs); (5) keep-first-beat = exactly one unchanged downbeat note; (6) intensity 0 = identity; plus PPQ-independence (96/192/384/960), reverse-emulation (double-time) vs the real-flag path, permutation-stays-within-selection, and empty input. **INSTALL:** copied to `[MyDocuments]\Image-Line\FL Studio\Settings\Piano roll scripts\BreakChop.pyscript` (folder found on disk; byte-for-byte copy verified with `cmp`) alongside RumbleBassline + ComposeWithLLM. Live in-FL menu/run verification is human-only (FL can't run piano-roll scripts headless) ⇒ CHECKPOINTS.md entry added. No fix-attempt budget spent (16/16 first full run), no Fable, no ultracode, nothing deferred. Deliverables: pyscripts/BreakChop.pyscript, pyscripts/tests/mock_fl.py (shared), pyscripts/tests/test_breakchop.py, pyscripts/tests/test_rumble.py (refactored to shared mock), docs/W2-BREAK-CHOP.md, README Tools row, PRD §7 tick, CHECKPOINTS entry. NEXT §7 item: W3-DARK-PROGRESSION. | FL: open the Piano roll on a channel with a sliced break (Fruity Slicer slice-notes) or any note run, SELECT the notes to chop (Ctrl+A = all; or select a region for a partial chop), Tools → Scripting → **Break Chop**, set Intensity + the op chances (Roll/Stutter/Reverse) + Roll count/decay, keep **Keep first beat** on to protect the downbeat, click OK — the selected notes are rewritten as a jungle/dnb chop that re-fills the same span (unselected notes untouched). Same Seed → identical chop; bump Intensity + Roll/Stutter chance for busier mangling, Permute for reorders. Reverse renders as a fast double-time repeat (FL note API has no reverse flag).
2026-07-07 | W1-RUMBLE-BASSLINE | SHIPPED (full, [x]): FL Studio piano-roll script `pyscripts/RumbleBassline.pyscript` (SPECS "W1 RumbleBassline"; Phase 4 Taste-tailored). Generates offbeat / rolling **rumble-bass** note patterns that lock AROUND a 4-on-the-floor kick (dark/hard techno). Kick assumed on the beats (PPQ 0,1,2,3 per 4/4 bar); every pattern AVOIDS the kick positions and any note that does land on a beat is GHOSTED (ghost velocity) so it ducks under the kick. **Four rhythm engines:** Offbeat 8ths (one accented note on the "and" of each beat — 16th idx 2/6/10/14, nothing on beats, 4/bar), Rolling 16ths (continuous 16ths; on-beat 16ths ghosted, the "and" accented, "e"/"a" ghosts, 16/bar), Gallop (per beat: ghost on-beat + accent "and" + ghost "a", 12/bar), Broken (fixed syncopated figure {2,3,6,10,11,14}, never on a beat, 6/bar). **Pitch** root-dominant (all base notes = root, low register clamped to root octave ±1) with occasional **scale passing tones** — b2 (phrygian only) / 5th / b7 — at low probability and ONLY on the extra *fill* ghost 16ths. **Velocity** two-tier (accent on offbeats, ghost elsewhere). **Humanize** seeded + deterministic on velocity (±%) and timing (± ticks, HARD-bounded to ≤5, with clamp of over-range requests). **Fill density** sprinkles extra ghost 16ths into the non-beat gaps (never on a beat). **ScriptDialog inputs:** Root note (C..B), Octave (0-4 → F1 = MIDI 29 default), Scale (Minor/Phrygian/Harmonic Minor), Pattern, Bars (1-8), Note length %, Ghost velocity % (0-60), Accent velocity % (40-100), Humanize velocity ±, Humanize timing ± ticks (0-5), Fill density % (0-100), Seed. **DESIGN DECISIONS** (documented in file + docs): SELF-CONTAINED single file — FL's script sandbox blocks file access (verified vs the machine's ComposeWithLLM.pyscript header) so an adjacent shared `.py` can't be imported; APPEND semantics matching ComposeWithLLM's default add-action, generating from the timeline selection start if one exists else tick 0 (clear the roll first for a fresh pattern); the rhythm/pitch logic is the PURE, importable `generate_bassline(...)` (no `flpianoroll` dependency, returns plain dicts), FL glue is `createDialog()`/`apply(form)`, and `import flpianoroll` is guarded so the module imports headless. **OFFLINE GATE** `pyscripts/tests/test_rumble.py` injects a MOCK `flpianoroll` (Note class + score list + ScriptDialog stub; loaded through `SourceFileLoader` because `.pyscript` isn't a recognized source suffix) and runs 12/12 GREEN under `uv run --python 3.12` (both the plain-assert runner and `--with pytest pytest`): asserts (1) no note starts on a kick position unless the pattern ghosts it at ghost velocity, and accents never land on a beat; (2) all pitches ∈ the selected scale and register ∈ root±12; (3) timing humanize bounded ±5 ticks incl. the >5 clamp; (4) deterministic per seed (same seed identical, different seed differs); (5) base note count (fill_density=0) matches the pattern math for all four patterns × {1,2,4,8} bars; plus PPQ-independence (96/192/384/960), fills-only-in-non-beat-gaps, and the FL `apply()` glue end-to-end (dialog defaults + selection-offset) through the mock. **INSTALL:** the Piano roll scripts folder was FOUND on disk at `[MyDocuments]\Image-Line\FL Studio\Settings\Piano roll scripts` (MyDocuments resolved via GetFolderPath → OneDrive-redirected) and the script was COPIED there alongside ComposeWithLLM. Live in-FL menu/run verification is human-only (FL can't run piano-roll scripts headless) ⇒ CHECKPOINTS.md entry added. No fix-attempt budget spent beyond one test-expectation correction (fills were landing on empty on-beat slots for Broken/Gallop — tightened the generator so fills only fill non-beat gaps, matching the avoid-the-kick ethos). No Fable, no ultracode, nothing deferred. Deliverables: pyscripts/RumbleBassline.pyscript, pyscripts/tests/test_rumble.py, docs/W1-RUMBLE-BASSLINE.md, README Tools row, PRD §7 tick, CHECKPOINTS entry. NEXT §7 item: W2-BREAK-CHOP. | FL: open the Piano roll on a bass channel (with a 4-on-the-floor kick on another channel), Tools → Scripting → **Rumble Bassline**, set Root/Octave (F1 default), Scale, Pattern (try all four) + Bars, click OK — bass notes appear locked between the kicks (offbeats accented, on-beat notes ghosted). Select a timeline region first to place the pattern there; clear the roll (Ctrl+A, Del) before running for a clean pattern. Same Seed → identical pattern; bump Fill density for busier rolls, Humanize for feel.
2026-07-07 | ASCEND | SHIPPED (full, [x]): tension generator (MIDI/transport instrument; taste-tailored Phase 3; SPECS "ASCEND"). New crate `plugins/ascend`. Reads the host transport (`nih_plug` `Transport`: tempo + time_sig_num/den + `pos_beats()`), derives a fractional 4/4-aware bar position and a bars-per-sample advance, and COUNTS DOWN to the next **8/16/32/Custom**-bar boundary (**Sync Target** + **Custom Bars** 1–64). **ONE tension envelope** (phase `p` 0→1 over the window, shaped `p^γ` where **Curve** morphs γ: 0 exp / 0.5 lin / 1 log) drives, at once: an **SVF cutoff sweep** (`Filter Start`→`Filter End`, exponential interp, opening up), a **pitch rise** 0–24 st (`Pitch Rise`) on a tonal **root+fifth** stack (each osc **saw↔sine** = `Saw/Sine`, root from **Key**+**Octave**), a **width bloom** (mid/side, mono-compatible, `Width`·env), and a **volume swell** (5 % floor→full). Sources: **white↔pink** filtered noise (`Noise Color`, decorrelated L/R streams for the bloom) + the tonal stack, balanced by **Noise/Tone**. At the boundary: an embedded **impact** (IMPACT's `testsig::synth_kick` math at a low ~42 Hz pitch, synthesized ONCE at construction into a Vec and played back by index — alloc-free process; `Impact` on/off + `Impact Level`) fires, and **Auto-Cut** gates the SOURCES to silence (fast one-pole fade + ~45 ms hold, then re-arm) — the impact is summed AFTER the gate so the drop hits clean. **Downlifter** mode reverses the envelope (`env = shape(1−p)`: full at the boundary, falling away). **Free-run** (transport stopped): the momentary **Trigger** button (rising-edge detected) or a **MIDI note** runs the same envelope over **Free Length** s; **Key Track** makes the root follow the played note. Boundary detection is sample-accurate (the engine advances its own bar position per sample from the block-rate host position; a >0.5-bar discontinuity re-primes without a false impact). Lock-free `AtomicU32` publishes the bars-remaining COUNTDOWN to the GUI. DSP is local to the crate (an `AscendEngine` + a small local `Pink` per-sample filter); the only suite-core reuse is `dsp::{Svf, tape_soft, ScopedFtz, db_to_lin}` + `testsig::{synth_kick, KickSpec, Rng}` — **NOTHING added to suite-core ⇒ no suite-wide revalidate**. The DSP core takes a plain `TransportFrame { playing, bar_pos, bars_per_sample }` so it stays free of any host type and is driven in tests by a local fake 4/4 transport (the brief's "CLEAVE-style fake-transport in suite-core testsig" does NOT exist yet — CLEAVE is unbuilt; recorded, drove tests locally instead). 21 params, all rendered through `suite_core::ui::ScaledWindow` + `labeled_knob` (inherits the UI-CORE-FIX knob/type/scale interactions for free) grouped TARGET / SOURCES / TENSION SWEEP / IMPACT-MODE, with a live COUNTDOWN readout. Done-bars MET (no Fable, no ultracode, no descope): (1) render 8 bars @130 BPM, target=8 → windowed spectral centroid (suite STFT, 8 windows) rises monotonically (12 % dip tolerance) with ≥1.5× net rise over the countdown; (2) impact lands on the target bar within **±5 ms** — isolated by differencing an impact-on vs impact-off render (deterministic engine) and comparing the onset sample to the computed boundary sample — AND (auto-cut on, impact off) the source path falls to <5 % of its pre-boundary peak within 10–50 ms after the boundary; (3) downlifter mode → centroid falls monotonically after the boundary; + universal (finite / ≤0 dBFS / non-silent) on all 6 preset stereo renders → renders/ASCEND/. 10/10 crate tests green. **build.ps1 ascend GREEN**: clap-validator 16 pass / 0 fail / 4 skip; pluginval --strictness-level 8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread-safety + all buses); ascend.clap + ascend.vst3 installed to the CLAP dir + VST3 junction + dist/. One fix attempt: clap-validator's `text_to_value` all-or-none check failed because the **Custom Bars** display "N bar" broke a bare `parse::<i32>` — added a unit-stripping `string_to_value` (leading digits/sign), re-ran GREEN. 6 presets: Riser 8 Dark, Riser 16 Wide, Sub Boom Drop, Downlifter 8, Noise Swell Short, Melodic Fifth Rise. Deliverables: plugins/ascend/{Cargo.toml, src/lib.rs, src/dsp.rs, src/presets.rs, src/render_tests.rs}, workspace Cargo.toml member + Cargo.lock, docs/ASCEND.md, README row. **HARD CHECKPOINT due after ASCEND (PRD §1)** — CHECKPOINTS.md updated with the FL audition steps. | FL: Add "Qeynos ASCEND" as an instrument on a MIDI/pattern track. With the transport PLAYING it auto-arms — load "Riser 8 Dark" and the GUI shows a live bars-remaining countdown; over each 8 bars it sweeps up and drops an impact on the downbeat with an auto-cut (try "Riser 16 Wide" for a longer wide build, "Sub Boom Drop" for a big low drop, "Melodic Fifth Rise" for a tonal riser, "Noise Swell Short" for an impact-less atmos swell). Load "Downlifter 8" for the reversed after-the-drop fall. Transport STOPPED: click TRIGGER (or play a note; tick Key Track to pitch it) to run the riser over Free Length seconds standalone. Set Sync=Custom + Bars for an odd-length build.
2026-07-07 | UI-CORE-FIX | SHIPPED (full, [x]): suite-wide GUI interaction rework (SPECS "UI-CORE-FIX"; user complaints: sliders-not-knobs, clunky rescaling, dead click-to-type). All three fixes live in `suite_core::ui` so one change retrofits every editor. **(1) Rotary knob** replaces the labeled slider as the standard control: vertical drag (up=increase), **Ctrl/Shift fine drag ~10×**, **double-click reset to default**, **scroll-wheel steps** (one detent for stepped params), arc + tick-ring + needle painting, label above / live value below. Correct ParamSetter discipline: `begin_set_parameter` on drag-start, per-frame `set_parameter_normalized` (accumulated from a stored drag-start value so it never drifts), `end_set_parameter` on drag-stop; double-click/scroll each bracket their own begin/end. **Bool params dispatch to a toggle pill**; **Int/Enum → detented knob** (scroll = exactly one step, values snap via preview_plain). **(2) Real click-to-type**: value text is a plain `Label` when idle (kills the phantom caret) and swaps to a focused `TextEdit` on click; **Enter** commits through the param's own `string_to_normalized_value` (all params already have parsers — clap-validator enforced), **Esc** cancels, **click-away commits** (lost_focus). **(3) Uniform scaling**: `ScaledWindow` wraps nih_plug_egui's ResizableWindow and applies `ctx.set_zoom_factor(scale)` so the whole editor zooms as one unit (no reflow); scale derived from the persisted window logical width vs each plugin's base logical size, aspect-safe (width-master), snapped to **75/100/125/150 %** with a **corner size menu** (top-right, shows current %); `min_size = base` so nothing clips at 100 %; the effective size/scale persists via `EguiState` (the existing `#[persist="editor-state"]`). `labeled_slider`/`labeled_knob` now delegate to `param_widget`, so all 18 editors (17 plugins incl. OVERSEER's node+master GUIs + _template) needed only a 2-line mechanical swap (drop the ResizableWindow import; `ResizableWindow::new(id)` → `suite_core::ui::ScaledWindow::new(id, base)`). **git diff 3866eca..HEAD over every plugin lib.rs contains ONLY those two line-kinds — zero param rows touched ⇒ provably no param lost** (the param-count cross-check, done by diff not eyeball). EMBER factor-band + OVERSEER node-grid/analyzer custom widgets keep their internals (their params still render). DONE-BARS MET (no Fable, no ultracode): parse/commit path unit-tested (`string_to_value` round-trips on representative Float/Int/Bool params), scale mapping unit-tested (window-px→ppp lands on each snap; snap band + clamp) — 9 new `suite_core::ui` tests, 44 suite-core tests total green; `cargo test --workspace --release` green with NO DSP change (every plugin's existing tests unchanged & passing); **`build.ps1 -All` = all 17 crates + _template GREEN** (clap-validator 0 failed suite-wide, pluginval s8 0 failed suite-wide incl. Fuzz Parameters + thread-safety + all buses, CLAP+VST3 reinstalled). DEFERRED.md: nih_plug_egui exposes no public host-resize request from shared code (`EguiState::set_requested_size` is pub(crate)) → the size menu snaps the current window's zoom and the user's corner-drag grows the OS window (snaps onto the stops). CHECKPOINTS.md + docs/UI.md: pluginval runs `--skip-gui-tests`, so live window-open/typing is verified by hand in FL, and if FL swallows keystrokes flip the wrapper's "steal keyboard focus" (Typing-keyboard-to-piano) toggle. Deliverables: rewritten suite-core/src/ui.rs, all 17 plugin editors + _template, docs/UI.md (the manual "controls" boilerplate), README Controls section, DEFERRED + CHECKPOINTS entries. | FL: reopen ANY Qeynos plugin — params are now knobs: drag up/down, Ctrl-drag = fine, double-click = reset, scroll = step; click a value readout and type an exact number then Enter (if keys don't land, toggle the FL wrapper's keyboard-focus per CHECKPOINTS); drag the window's bottom-right corner and watch the whole UI zoom as a unit, snapping at 75/100/125/150 % (corner % menu), size restored on reopen.
2026-07-07 | VOXFIT | SHIPPED (full, [x]): vocal character conformer (VOX suite; SPECS "VOXFIT"), FINAL VOX item. Makes a ripped/foreign acapella SIT in a different production. Chain (per channel, stereo): **formant shift** (pitch-INDEPENDENT — reuses SEANCE's `suite_core::shift::ShiftEngine` with `set_pitch_ratio(1.0)` + `set_formant_ratio(2^(st/12))` + envelope-preserve ON; **Formant ±5 st**) → **de-esser** (complementary crossover @5 kHz: `low`=two cascaded SVF LP 24 dB/oct, sibilant `high = x−low`; fast peak `EnvFollower` keys `high`; infinite-ratio downward gain `gr=(thresh/env)^amount`; `out = low + gr·high` sums to identity so reduction can be TOTAL and <2 kHz vowel untouched; **Thresh/Amount/Listen** [Listen monitors the removed content]) → **harshness tamer** (dynamic bell cut 2–5 kHz: unity-peak RBJ bandpass @3.16 kHz feeds detector + subtract `tamed = x − k·bp`, `k` follows band energy over **Harsh Thresh** × **Harsh** up to 18 dB — gain-modulated subtract, no coeff clicks) → **tilt EQ** (complementary RBJ low+high shelves pivot 1 kHz, **Tilt ±6 dB**, <0 dark) → **proximity** (low-mid shelf ~300 Hz, ±6 dB) → **air** (high shelf 12 kHz, ±6 dB) → mix vs latency-matched dry → out trim → knee'd safety-clip. **SIT** macro (0–100%) folds a curated conform into the effective Settings in `Controls::resolve` (slight formant drop + mild de-ess + presence dip + dark tilt + touch of proximity); sit=0 = base untouched. Reports **2048-sample latency** (ShiftEngine FFT), dry `DelayLine`-matched so `mix=0` nulls EXACTLY (<1e-5); every post-shift stage is minimum-phase so no extra reported latency. Local RBJ **Biquad** copied from `overseer::eq` (+ a unity-peak `bandpass_0db`) — kept in-crate, **NOTHING added to suite-core ⇒ no suite-wide revalidate**. EQ coeffs recomputed at block rate from OnePole-smoothed dB; all scalar params per-sample smoothed; alloc-free process under ScopedFtz. Done-bars MET (no Fable, no ultracode, no descope): (1) Formant +3 st on a 145 Hz vocal → cepstral avg-log-envelope cross-correlation shift ratio ≈ 2^(3/12)=1.189× within ±10% while measured f0 within ±10 cents (reused the VOXKEY/shift.rs cepstral helpers, f0 low so n/16 lifter sits below the pitch quefrency); (2) HP-noise sibilant bursts on a 150 Hz vowel → 5–9 kHz burst-band energy reduced >3 dB and MORE with higher amount, while <2 kHz vowel within ±1 dB; (3) log-chirp at max-dark Tilt → low-minus-high spectral balance shifts >6 dB (spectrum tilts); + universal (finite/≤0 dBFS/non-silent) on all 6 preset renders + mix=0 exact null. 12/12 crate tests green; **build.ps1 voxfit GREEN** (clap-validator pass, pluginval s8 SUCCESS incl Fuzz Parameters + thread-safety + all buses; voxfit.clap + voxfit.vst3 installed + dist/). 12 params (formant, deess_thresh, deess, listen, harsh_thresh, harsh, tilt, prox, air, sit, mix, out), 6 presets (Sit In Dark Mix, De-Harsh Rip, Radio Ghost, Deeper Voice, Airy Feature, Neutral Cleanup), 7 renders in renders/VOXFIT/. GUI: all params grouped MACRO/DE-ESS/HARSH/TONE/OUT, suite theme, ResizableWindow. docs/VOXFIT.md + README row. First real change to the shared workspace Cargo.toml (added plugins/voxfit member) + Cargo.lock. | FL: Find more plugins → add "Qeynos VOXFIT" on a ripped/imported vocal (after VOXKEY for pitch); load "Sit In Dark Mix" and raise SIT until the vocal tucks into a dark track, "De-Harsh Rip" for bright/sibilant acapellas (tick De-Ess Listen to tune Thresh), "Deeper Voice"/"Radio Ghost" for character, "Airy Feature" for an up-front top. Host auto-delay-comps +2048 samples.
2026-07-07 | VOXKEY | SHIPPED (full, [x]): vocal retuner (VOX suite; SPECS "VOXKEY"), second VOX item, reuses the SEANCE-built `suite_core::shift::ShiftEngine`. Chain: mono sum → pitch detect → nearest Root+Scale tone (or held MIDI note) → correction ratio → TWO stereo ShiftEngines (envelope-preserve ON) → latency-matched dry/wet mix. **Root** (12 notes) + **Scale** (Chromatic/Major/Natural+Harmonic Minor/Phrygian/Dorian/Minor Pentatonic; semitone-class mask, ±3-semitone nearest-tone search covers pentatonic's widest gap). Correction = 1200·log2(target/detected) clamped ±1 octave, one-pole **glided in the cents (log) domain** by **Retune Speed** 0–400 ms (0 = coef 1 = hard snap = autotune stair-step), then × **Amount** 0–100 %, + **Humanize** (slow Rng ±cents random-walk on the target). **Confidence Gate**: below the gate (breaths/consonants/silence/noise) the correction glides to 1.0 → no retune, no artifacts. **Formant Offset** ±12 st via `set_formant_ratio` (preserve always on, independent of pitch). **MIDI Mode** (MidiConfig::Basic): held note = target (last-note-priority fixed stack, alloc-free), scale ignored while held. Two ShiftEngines for stereo; reports **2048-sample latency**, dry `DelayLine`-matched so `mix=0` nulls exactly (gated on the DRY path per SEANCE's honest-PV note — the wet identity only nulls ~−15 dB). Knee'd wet safety-clip (identity <0.9) keeps ≤0 dBFS without breaking the null. GUI: all params + live **IN→TGT** note/Hz read-out + confidence (lock-free AtomicU32 meter), suite theme. **DESIGN DECISION (PRD §0, recorded in DEFERRED.md):** detect with `suite_core::pitch::Mpm` **directly** (thin decimated no-hysteresis `RetunePitch` wrapper, median-3) instead of `suite_core::pitch::PitchTracker` — PitchTracker's ±35-cent re-lock hysteresis (tuned for TRACER crossover stability) reads up to 35c below true pitch right after a note change and STICKS, and since output = input×target/detected that bias lands the retuned note up to 35c off the tone (measured 60% on-scale). Hysteresis-free Mpm is accurate to a few cents → ≥80% on-scale (the done-bar). Same module, same MPM core. Done-bars MET (no Fable, no ultracode, no descope): (1) vibrato-free saw+formant vocal stepped across a fifth, root A / natural minor, retune 0, amount 100% → measured output f0 within ±15 cents of an A-minor tone for ≥80% of pitched frames (used the brief-sanctioned "saw+formant ramp" — synth_vocal's ±17c vibrato would pass through hard-snap as residual wobble); (2) forced +5 st (MIDI mode) on a fixed 150 Hz note → cepstral log-envelope cross-correlation shift ratio within ±8% of 1.0 (formants held) while f0 moves +5 st [used f0=150 so the n/16 cepstral lifter sits below the pitch-period quefrency — the shift.rs envelope-measure technique]; (3) white noise → pitch-shift ratio within ±6% of 1.0 (gate holds it, no pumping); + universal (finite/≤0 dBFS/non-silent) on all 7 preset renders + mix=0 exact null. 11/11 crate tests green; clap-validator 16 pass/0 fail/4 skip (1 benign scan warning); pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread-safety + all buses); voxkey.clap + voxkey.vst3 installed + dist/. Nothing added to suite-core ⇒ no suite-wide revalidate. 10 params (root/scale/retune/amount/humanize/formant/gate/midimode/mix/out), 7 presets (Hard Snap Am, Gentle Glide, Phrygian Dark, T-Pain Extreme, Subtle Live, MIDI Puppet, Doll Formant), 8 renders in renders/VOXKEY/. docs/VOXKEY.md + README row. | FL: Find more plugins → add "Qeynos VOXKEY" on a monophonic vocal/lead; set Root+Scale to your key; load "Hard Snap Am" for classic autotune (Retune 0), "Gentle Glide" for transparent correction, "Phrygian Dark"/"Doll Formant" for character (Formant Offset), "Subtle Live" for a light nudge; tick MIDI Mode + load "MIDI Puppet" and route a MIDI clip so held notes drive the pitch. Watch the IN→TGT read-out. Host auto-delay-comps +2048 samples.
2026-07-07 | W9-VOXRIP | SHIPPED (full, [x]): acapella extraction + key/tempo conforming Python tool (tools/voxrip/, first of the VOX suite). Pipeline: (1) SEPARATION via demucs htdemucs `--two-stems vocals` → vocals_raw.wav + instrumental.wav (accompaniment = mix minus vocals); runs OUT-OF-PROCESS in tools/voxrip/voxrip_separate.py whose PEP 723 `[tool.uv]` pins torch/torchaudio to the CPU wheel index (no CUDA) — keeps the main script + offline gate torch-free; torchaudio capped <2.8 (2.8+ needs torchcodec/FFmpeg). `--no-separate` skips it. (2) ANALYSIS: BPM (librosa beat_track on 256-hop onset env for fine tempogram resolution; confidence proxy + half/double alts) + key (chroma_cqt vs 24 Krumhansl-Schmuckler major/minor rotations, Pearson) of BOTH full song and vocal; low-confidence/ambiguity warnings. (3) CONFORM (when targets): rubberband CLI (portable Breakfast Quay Windows build, self-fetched into tools/bin/rubberband, `-F` formant-preserving, `--time`+`--pitch`, `-c 6`) time-stretch to --target-bpm + pitch-shift by minimal semitone move to --target-key; relative-mode-aware key logic (same mode→minimal signed root distance +octave-wrap alt; different mode→reinterpret source via its relative key into target's mode, e.g. Am→Cmaj=0st, Cm→Cmaj=−3st). librosa phase-vocoder fallback if rubberband can't be fetched/run after 3 attempts. (4) OUTPUT <out>/<stem>/ {vocals_raw,vocals_conformed,instrumental}.wav + REPORT.md (source bpm/key+confidence, target, transposition incl. relative alt, warnings). Offline gate 19/19 green via uv py3.12 (synthesized click-track BPM ±2%/alt + tonicised key-pad exact incl. Am-vs-Cmaj ambiguity; transposition fuzzed over all 24×24 key pairs; stretch-ratio; rubberband/demucs command builders; rubberband invocation mocked). BOTH live paths verified end-to-end on the build machine: demucs built the CPU-torch env, downloaded htdemucs weights, separated a track; rubberband-3.3.0 fetched+conformed C#m/99BPM→F#m/128BPM +5st with formant preservation. No fix-attempt descopes, no Fable escalation, no ultracode, nothing deferred. Deliverables: voxrip.py, voxrip_separate.py, test_voxrip.py, .claude/skills/voxrip/SKILL.md, docs/W9-VOXRIP.md, README Tools row. | NOT an FL plugin — run: `uv run --python 3.12 tools\voxrip\voxrip.py "song.mp3" --target-bpm 174 --target-key "F#m" --out .\ripped` (uv at %USERPROFILE%\.local\bin\uv.exe); audition ripped\<song>\vocals_conformed.wav over your target track, read REPORT.md. See CHECKPOINTS.
2026-07-07 | HARD-CHECKPOINT-1 | gates green, 7 confirmed findings fixed (comb/latency, smoothers, aliasing, loudness-rt, denormals) | re-test GRIT/TRACER parallel mix in FL
2026-07-07 | PLANNING | PRD v2 hardened via 3-agent adversarial review; repo, specs, loop contract, allowlist committed | n/a
2026-07-07 | BOOTSTRAP | GO: _template passes clap-validator + pluginval on windows-gnu | rescan plugins in FL, load "Qeynos Template"
2026-07-07 | GRIT | SHIPPED (degraded, [x]*): sidechained distortion, Modes A (Env-Drive) + B (Waveshape); Mode C (spectral STFT) deferred to DEFERRED.md. 4x oversampling + presets module added to suite-core (all-crates revalidated: _template green). clap-validator 14/0, pluginval s8 PASS, CLAP installed. Done-bar met: THD rises during SC pulses, auto-gain holds post-RMS within ±1 dB of pre. 5 presets, renders in renders/GRIT/. | FL: Find more plugins → add "Qeynos GRIT", route a kick to its sidechain, load "Kick Bass Grit", confirm it pumps with the kick (SC Listen to audition the focus band)
2026-07-07 | IMPACT | SHIPPED (full, [x]): kick drum synth (MIDI instrument). Mono last-note-priority voice: exponential pitch env f(t)=f_end+(f_start−f_end)e^(−t/τ_p) with curve morph → phase-continuous sine/tri body; band-passed noise click (SVF BP, own 5–50ms decay) + 3 embedded PCM transients (Tick/Snap/Knock, synthesized offline in build.rs as const arrays, windowed to zero) + sub osc (f_end×ratio); mix → waveshaper-bank drive (Tube/Tape/Fold/Hard, pre-amp-env) → exponential amp env (curve) → soft/hard clip. Length macro scales amp decay + pitch τ together; Key-track sets f_end from MIDI (A1=55Hz). Phase-continuous retrigger + 1.5ms declick ramp on amp env AND click/transient onset. New suite-core API `testsig::synth_kick`/`KickSpec` (IMPACT's own math, replaces the kick stub) — all crates revalidated green via build.ps1 -All (_template, grit, ember, impact). Done-bar met: STFT f0 starts within 10% of f_start & ends within 5% of f_end; mid-decay retrigger stays within declick bound vs no-retrigger. clap-validator 16/0 (was 15/1 — fixed IntParam text_to_value consistency by adding string_to_value to shape/trans), pluginval s8 SUCCESS, CLAP installed. 5 presets, renders in renders/IMPACT/. No Fable escalation, no descope. | FL: Find more plugins → add "Qeynos IMPACT" to a channel, play notes (each fires a kick); load "808 Long"/"House Punch"; enable Key Track to tune from the keyboard; fire rapid repeated notes to hear the declicked retrigger.
2026-07-07 | EMBER | SHIPPED (full, [x]): spectral fader / temporal smoother. Added alloc-free streaming STFT engine to suite-core (`suite_core::stft`, realfft 3.5) — all crates revalidated green (_template, grit). EMBER: per-bin state machine (coef=1-exp(-T/τ), 8-band log-freq attack/decay curves, decay to 60s), phase-vocoder tails (tonal ring), 1/3-oct fitting envelope, freeze (τ→∞), gate, tail gain, latency-aligned dry/wet. Reports 2048-sample latency. Done-bar met on FIRST attempt (no Fable escalation): τ=10s noise tail +2s > -40 dBFS & frame-RMS monotone↓; freeze tail flat ±1 dB over 5s; mix=0 nulls vs latency-delayed dry < -80 dB. clap-validator PASS, pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024), CLAP installed. 5 presets, renders in renders/EMBER/. | FL: Find more plugins → add "Qeynos EMBER", load "Bloom Pad" on a pad/vocal (notes bloom & sustain past release); play a sustained note, tick Freeze, stop input → spectrum holds as a drone. Host reports +2048-sample latency (auto delay-comp).

2026-07-07 | TRACER | SHIPPED (full, [x]): pitch-tracking multiband saturation. MPM pitch detector (new `suite_core::pitch`) on a mono-summed, anti-aliased, ~12 kHz-decimated stream (window 1024) → median-5 → ±35-cent hysteresis → Hz/ms slew → (f0, confidence). Time-varying LR4 crossover tree = cascaded 2nd-order Butterworth pairs built on the TPT SVF (stable under per-32-sample-block cutoff modulation), cutoffs = harmonic multiples of f0 (×1.5/×4/×8) × 2^SmartFreq, or per-crossover Fixed Hz; confidence < 0.6 freezes cutoffs; NaN/blow-up guard resets tree + 256-sample crossfade. Per band: drive → suite waveshaper bank → 2x OS → level, summed; optional constant-color drive (inverse ISO-226 11-pt LUT). MIDI mode (MidiConfig::Basic) replaces the detector with note-on f0. Done-bar met on FIRST attempt (no Fable escalation, no LR4 instability): (1) sliding-saw → band-1 centroid tracks f0 within ±1 semitone across the glide; (2) white noise → crossovers frozen (< 0.5 Hz drift over 1 s). Plus a param-fuzz stability test (48 dB drive, hard shaper, degenerate cutoffs → finite, ≤ 0 dBFS). clap-validator PASS, pluginval s8 SUCCESS across 44.1/48/96k blocks 64..1024 incl. Fuzz-parameters, CLAP installed. New suite-core APIs also: testsig::synth_vocal (replaces stub) + sliding_saw; all crates revalidated green via build.ps1 -All. 5 presets, 10 renders in renders/TRACER/. | FL: Find more plugins → add "Qeynos TRACER" on a monophonic pitched source (bass/808/vocal/lead); load "Sliding 808 Grit" and glide a note — the bands follow the pitch. For drums/bus set crossovers to Fixed or load "Fixed-Band Bus Saturator". Pitch Mode = MIDI to key the bands from notes.

2026-07-07 | OVERSEER | SHIPPED (full, [x]): mastering system — ONE crate/bundle exporting TWO plugins via multi-plugin `nih_export_clap!/vst3!` (PRD §3 tier 1). **Node** (per track): meter → 4-band EQ (RBJ LS/2×bell/HS biquads) → FF comp (10 ms RMS detector, soft knee, atk/rel, makeup) → level-preserving tanh sat → M/S width → trim → meter; text LABEL (persisted `RwLock<String>`, non-automatable); registers a slot on the same-DLL BUS. **Master**: 4-band EQ → 3-band multiband comp (LR4 = cascaded Butterworth TPT-SVF pairs) → lookahead limiter (2 ms delay + sliding-window max → anticipatory gain envelope, no attack overshoot; ceiling clamp guarantee; latency reported via set_latency_samples, dry path latency-matched) → BS.1770 LUFS meter (new reusable `suite_core::loudness`: sample-rate-correct K-weighting biquads, momentary 400 ms / short 3 s / gated integrated + reset) + 4x-OS true-peak-approx metering. BUS: `OnceLock<Bus>` registry, per-slot atomics only in process() (label mutex touched only from GUI/init), Master-written overrides (THRESH/RATIO/DRIVE/WIDTH/TRIM) win by timestamp, Node local touch steals back, GC via Arc strong-count (strictly stronger than heartbeat staleness; heartbeat still published for liveness). Master GUI = own chain + live NODES grid (label, PK/RMS/LUFS-M, 5 remote sliders + per-param release). Done-bar met on FIRST test run: (1) +6 dBFS sine, ceiling −1 → peak ≤ −0.9 dBFS (+ no-discontinuity click check); (2) LUFS: meter == own-K-filter analytic value ±0.5 LU (momentary + integrated), unweighted hook reads −20.0 ±0.1; (3) bus round-trip: override reaches Node next block, steal-back + GC asserted. One gate fix attempt: f32::clamp inverted-bounds PANIC in set_crossovers under pluginval param-fuzz at 44.1k (xo_low→20 kHz > 0.45·fs) — sanitized + regression test. clap-validator 26/0 (both plugins), pluginval s8 SUCCESS ×2 (44.1/48/96k, blocks 64..1024, incl. Fuzz), single overseer.clap installed. 6 presets (Node: Kick Strip/Vocal Strip/Bus Glue; Master: Techno/Gentle/Loud & Proud), 9 renders in renders/OVERSEER/. build.ps1 needed NO changes (already one-bundle-per-crate; both validators enumerate the two plugins inside it). All crates revalidated green (build.ps1 -All) after the suite-core loudness addition. No Fable escalation — the §8-eligible limiter passed first try. | FL: Find more plugins → add "Qeynos OVERSEER Node" on 2–3 tracks (set LABEL in each GUI), "Qeynos OVERSEER Master" on the master. Master GUI NODES grid lists them live; drag THRESH/DRIVE there → Node badges OVR + sound changes; touch the param on the Node → steals back. Keep "Make bridged" UNTICKED on these. Limiter: slam a hot mix, ceiling −1 dB → no peak past it; watch LUFS M/S/I + RESET LUFS.

2026-07-07 | W8-VITALGEN | SHIPPED (full, [x]): Claude-powered Vital 1.5.x preset generator (Python tool, `tools/vitalgen/vitalgen/`). Architecture per PRD: generation is a pure function of (schema, base template, description) — Claude fills a CONSTRAINED subset (osc levels/tuning/wave_frame/unison, filter routing/cutoff/res/model, env ADSR+curves, LFO shapes as point lists, FX on+amounts, macro names) via a tool-use schema; everything else copied verbatim from an embedded real 1.5.5 preset (`base_template.vital` = PAD_-_Miasma, guaranteed loadable). Never emits a full file. pydantic `PresetSpec` clamps continuous params to Vital ranges and REJECTS out-of-set enums (model/style/type/on/unison_voices). Embedded taste block (KAS:ST dark techno + Cynthoni/Sewerslvt grief pads/reeses/drowned leads) on every prompt. CLI: `generate`/`tweak`/`validate` (validate is offline). Model default claude-opus-4-8 (--model, reads ANTHROPIC_API_KEY), adaptive thinking. PEP 723 pinned py3.12 via uv (installed uv to %USERPROFILE%\.local\bin + `uv python install 3.12`). Schema ground truth: LOCAL 1.5.5 user presets (primary — found ~80 on-taste presets under Documents\Vital\User\Presets + dark-techno packs) + OSS synth_parameters.cpp; corrected 2 OSS ranges from real data (wave_frame 0..256 not 0..63; lfo frequency bipolar -7..7). Done-bar MET: 6/6 offline tests green via `uv run test_vitalgen.py` (base validates; valid fixture round-trips+writes loadable file; out-of-range clamped; enum-violation rejected with clear error; unknown-key rejected); fixture-built preset physically written to Documents\Vital\User\Qeynos\Drowned_Grief_Pad.vital and passes `validate` CLI (1.5.5, 781 settings). Live API smoke test SKIPPED (no ANTHROPIC_API_KEY) → CHECKPOINTS. GUI load in Vital → CHECKPOINTS (human-only). Serum 2 → DEFERRED.md (by spec). Deliverables: vitalgen.py, base_template.vital, 3 fixtures, test_vitalgen.py, `.claude/skills/vitalgen/SKILL.md`, docs/W8-VITALGEN.md, README Tools row. One fix attempt (2 OSS ranges vs real presets). No Fable escalation. | Set ANTHROPIC_API_KEY, then `uv run --python 3.12 tools\vitalgen\vitalgen\vitalgen.py generate "grief pad, slow attack, drowned" --bank Qeynos`; in Vital 1.5.5 open User → Qeynos → load the preset. uv at %USERPROFILE%\.local\bin\uv.exe.

2026-07-07 | W4-SESSION-BOOTSTRAP | SHIPPED (full, [x]): one-command FL Studio session template tool (`tools/session_bootstrap.py` + `tools/templates/`). A JSON template ({name, tempo?, mixer_tracks:[{index,name,color}], routing?:[{channel,mixer_track}], loop_mode?}) is compiled to an ordered op list and pushed to a running FL Studio through the user's FL Studio MCP controller over MIDI SysEx. Supported (ground truth = MCP server tools + `device_FLStudioMCP.py` dispatch, read this iteration): mixer_tracks.name→mixer.setTrackName, .color→mixer.setTrackColor (r/g/b, "#RRGGBB" or [r,g,b]), routing→channels.routeToMixer, loop_mode→transport.setLoopMode ("pattern"/"song"). UNSUPPORTED: tempo — the MCP server has NO BPM command (transport = start/stop/record/getStatus/setPosition/getLength/setLoopMode/setPlaybackSpeed), so tempo is printed as a skipped field, never applied, never fatal → DEFERRED.md. **Transport design decision: REIMPLEMENTED the ~60-line SysEx layer in-tool** (SYX_MFG 0x7D, CHUNK_SIZE 200, cmd 0x01/0x11, resp 0x02/0x12; wire-compatible, verified vs midi_connection.py) rather than importing the MCP package — keeps the tool one self-contained uv-runnable file with no path/venv dependency on the OneDrive MCP repo (which we must not modify). CLI: `apply <template> [--dry-run]` (non-interactive: default IS apply when connected; --dry-run previews the op list with no FL) + `list`. Idempotent (every op sets an absolute name/color/route/mode). Resilient: per-op failures (e.g. routing a not-yet-existing channel) are warnings, run continues; exit≠0 only if an op failed. Offline gate: **47/47 checks green** via `uv run --python 3.12 tools\test_session_bootstrap.py` — templates validate, op-lists compared to stored snapshots (`tools/tests/fixtures/{TECHNO,DNB}.ops.json`), transport MOCKED (command-stream/success/failure/idempotency), color-parse + validation-error coverage. Live smoke NOT run: fl_connection_status returned connected:true but every real command (fl_get_mixer_track_count/fl_get_transport_status) timed out → FL not actually running with the controller responding; exact live command recorded in CHECKPOINTS.md. 2 shipped templates: TECHNO (dark melodic techno, 11 mixer tracks KICK/RUMBLE/BASS/PERC/HATS/ATMOS/LEAD/CHORD/FX + REVERB/DELAY sends, dark scheme, tempo 132) + DNB (atmospheric dnb, 10 tracks KICK/SNARE/BREAKS/SUB/REESE/PADS/VOXFX/FX + REVERB/DELAY, cool scheme, tempo 174). Deliverables: session_bootstrap.py, templates/{TECHNO,DNB}.json, test_session_bootstrap.py + tests/fixtures/, docs/W4-SESSION-BOOTSTRAP.md, `.claude/skills/flsession/SKILL.md`, README Tools row. No fix attempts, no Fable escalation, no descope beyond the tempo server-limitation. | Start FL Studio with the FLStudioMCP controller enabled on the loopMIDI port (enabled in BOTH MIDI Input+Output, same port #), then: `uv run --python 3.12 tools\session_bootstrap.py apply TECHNO` (uv at %USERPROFILE%\.local\bin\uv.exe). Mixer tracks 1–11 get named+colored, loop→pattern, channels 0–8 routed to tracks 1–9. Preview without FL: `apply TECHNO --dry-run`; browse: `list`. Set tempo (132) by hand — no MCP command for it.

2026-07-07 | DRIFT | SHIPPED (full, [x]): infinity filter (Shepard-tone filter illusion, Sweep clone) — first Phase 2a clone. N (2..8, default 6) TPT/Cytomic **bell** filters evenly spaced by span/N octaves across a [Range Lo, Range Hi] log-freq window, all gliding together up/down at Rate (free Hz OR BPM-synced via host `context.transport().tempo` + Division 4bars..1/16), each center wrapping at the range edges; per-filter boost = Depth·raised-cosine(Hann) over its log-freq position ⇒ filters fade in silent at the bottom & out at the top ⇒ endless rise/fall. At a window edge gain→0 dB so the bell is a pass-through and the fc wrap carries no energy (click-free). Shared Resonance (Q), Stereo Offset 0..0.5 cyc on the R channel (note: offset = k/N maps R's filter set onto L by the Shepard self-similarity ⇒ no width; documented). Series bell cascade per channel; coeffs recompute per 32-sample control block from a smoothed phase + smoothed range/res/depth/offset with state preserved. **Pure minimum-phase IIR ⇒ latency 0, no FIR/OS, no dry-path compensation** (per build brief). Bell filter kept LOCAL to drift/dsp.rs (Cytomic peaking SVF) — NOT added to suite-core, so NO suite-wide rebuild/revalidate needed (only build.ps1 drift). Done-bar MET on first full test run (no Fable escalation, no descope): (1) white-noise, dir=up → dominant STFT peak (tracked via `suite_core::stft`) strictly advances + wraps, plus a companion test tracking a single filter center sweeping the FULL range and wrapping Range Hi→Lo; (2) periodicity: Welch-averaged output spectra at t and t+period/N correlate >0.9 and more strongly than at t+period/2N. Two done-bar tests initially failed as TEST-DESIGN issues (not DSP): stereo test used offset=1/N (degenerate, R≡L by design) → changed to 0.13; self-similarity test correlated raw noise periodograms (noise variance dominates) → switched to Welch-averaged + freq-smoothed PSD over `stft` frames. Also: mix=0 null <−80 dB, partial-mix single-coherent-peak regression (min-phase alignment), param-fuzz stability (36 dB depth, Q24, inverted range, 40 Hz rate → finite ≤0 dBFS), stereo decorrelation. IntParam Peaks has round-tripping string_to_value. 11/11 crate tests green; clap-validator PASS, pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters), drift.clap installed. 6 presets (Endless Riser, Slow Descent, Hypnotic Sweep 1/4, Wide Drift, Subtle Motion, Falling Half-Note), 12 renders in renders/DRIFT/. docs/DRIFT.md + README row. No Fable, no ultracode, nothing deferred. | FL: Find more plugins → add "Qeynos DRIFT" on a pad/drone/noise/drum-bus; load "Endless Riser" and let it run (motion never stops); tick Sync + pick a Division to lock to tempo; raise Resonance/Depth for an obvious vocal sweep, drop Mix for subtle motion, Stereo Offset widens it.

2026-07-07 | WIRE | SHIPPED (full, [x]): codec degradation (Codec clone) — a real Opus round-trip abused as an effect. **Codec Plan A LANDED** (no failed attempts, no Fable): pure-Rust `opus-rs` 0.1.23 (PRD wrote `opus_rs`; published crate is hyphenated; ZERO C/build deps). 10-line link-test binary (scratchpad/opustest) BEFORE any DSP: encode+decode band-limited noise at 48k, delay-compensated correlation MONOTONICALLY rises with bitrate in Music/Audio@48k (6k=0.36→12k=0.83→24k=0.96→96k=0.995) ⇒ done-bar(1) satisfied; VoIP@48k 0.20→0.995. Crate's internal SILK-resampler paths at 12k/24k are BUGGY (decode decorrelates to ~0.05) ⇒ DESIGN DECISION: always run Opus at 48k internal, realise "Bandwidth NB→FB" as a pre-codec low-pass (PRD §5 explicitly sanctions "approximate with bandwidth limiting and note it"), dodging every broken path. Latency deterministic+bitrate-independent (impulse peak-lag); codec's algo delay is absorbed inside the 20ms frame buffering ⇒ pipeline latency = FRAME(960)+WET_GUARD(64)+1 = 1025 @48k, reported==measured EXACTLY at 128k (done-bar(2)). CPU 62us/20ms-frame = 0.31% RT ⇒ codec runs IN the audio thread, frame work wrapped in `nih_plug::util::permit_alloc` for the workspace's assert_process_allocs (opus-rs allocates internally). Stereo = two independent mono codec instances (reliable across full 6-128kbps range; independent L/R quantisation = genuine codec-width). Pipeline: host→48k linear SRC (push-up + pull-down pair, exact passthrough at 48k) → per-sample regen feedback (tanh soft-limit + DC-block, PRD §3) → bandwidth LP → crunch (bit 16→5 + SR ÷1→÷24) → 20ms framing → encode → packet-loss frame-drop (seeded RNG, zero-fill + crossfade concealment since opus-rs decode() rejects empty input) → decode → width(M/S) → PDC-aligned dry/wet mix → out. Nothing added to suite-core (opus-rs + all WIRE DSP local to the crate) ⇒ NO suite-wide rebuild/revalidate. 11 params (Bitrate/Mode/Bandwidth/FEC/Packet Loss/Crunch/Regen Delay/Regen Amount/Width/Mix/Out), 6 presets (Discord Ghost, Dying Stream, Hold Music, Generation Loss, Subtle Digital, Bitcrushed Void). 8/8 crate tests green (both done-bars, mix=0 null −inf dB, extreme-degradation stability, packet-loss audibility, 44.1/48/96k SRC); clap-validator PASS, pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread safety + all buses), wire.clap + wire.vst3 installed. docs/WIRE.md + README row. Two documented approximations (bandwidth-as-LP, FEC no real decode-side recovery) → DEFERRED.md with resume steps. Renders in renders/WIRE/. | FL: Find more plugins → add "Qeynos WIRE" on any source; load "Discord Ghost" (VoIP grunge), "Generation Loss" (crank Regen Amount for tape-eating feedback), "Hold Music" (telephone), or "Bitcrushed Void" (everything). Sweep Bitrate 6→128k to hear fidelity trade; raise Packet Loss for stutters; Mix for parallel. Host auto-delay-comps the reported latency.

2026-07-07 | OUROBOROS | SHIPPED (full, [x]): recursive feedback processor (Recurse clone), Phase 2a. Feedback delay (1 ms–2 s, free or BPM-synced via `context.transport().tempo` × division) whose loop runs input+fb → **fractional smoothed delay** → **reorderable 3-slot chain** → in-loop `tanh` soft-limiter (unity) → DC blocker → output tap; feedback = tap × (fb 0–110% × decay), summed at input. Slots each selectable from 6 effects (all LOCAL to ouroboros/dsp.rs, nothing added to suite-core ⇒ no suite-wide revalidate): granular ±12 st pitch (two-tap half-grain crossfading ring reader), SVF LP/HP/BP (suite `Svf`; filter type folded into the slot-type enum), Hilbert **frequency shifter** (Niemitalo/Costello two-path 4-section z⁻² allpass network, one branch fed a 1-sample-delayed input ⇒ empirically ≈−19 dB SSB suppression 300 Hz–9 kHz — the delay placement was found by a scratch sweep; documented group-delay = negligible, un-reported), suite **saturator** bank (tanh↔fold blend), **reverse chunk** (fixed-size reversed-granule two-grain player), **bit crush** (bit + SR reduce, WIRE's crunch design). **Slot-order = single enum over the 6 permutations** (simpler/safer than 3 position IntParams — can't express a degenerate duplicate; documented). **Freeze** = smoothed input mute + fb pinned to 100% (click-free). **Zero reported latency** (delay line IS the effect, not fixed processing delay — only FIR/lookahead counts, and there is none); partial-mix single-coherent-peak (a lag-0 alignment check) DOESN'T APPLY to a time-delay effect ⇒ adapted to a **mix=0-nulls-vs-dry** assertion, documented. Done-bar MET first full run (no Fable, no descope): (1) 110% feedback + saturator+filter, 30 s render → peak ≤0 dBFS, zero NaN, last-5 s RMS stable (5×1 s windows, growth ≤1 dB, no collapse) — self-oscillation bounded by the tanh limiter; (2) delay time change 300→150 ms mid-render (block-by-block) → max sample-to-sample delta within 3× the steady-state render (fractional smoothed read, no hard click). Plus freeze-sustains-without-input, all-8-slot-types-finite-&-bounded @110% fb fuzz, Hilbert-quadrature sanity. Only fix cycle: Hilbert allpass sign/delay placement for the (non-gated) quadrature sanity test — resolved via a 4-config scratch sweep (form `a·(x+y2)−x2` + branch-A delay = min ripple 1.255). 8/8 crate tests green; clap-validator PASS, pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread safety + all buses), ouroboros.clap + ouroboros.vst3 installed. 18 params (delay/sync/division/feedback/decay/freeze/order + 3×{type,amount,param} + mix/out), 6 presets (Dub Tail, Shifter Spiral, Crushed Echoes, Frozen Drone, Reverse Cascade, Frequency Clang — none set freeze, which would render silent from scratch), 12 renders in renders/OUROBOROS/. docs/OUROBOROS.md + README row. No Fable escalation, no ultracode, nothing deferred (shipped full). | FL: Find more plugins → add "Qeynos OUROBOROS" on any source/bus; load "Dub Tail" over a rhythmic loop; push Feedback past 100% or load "Frozen Drone" for bounded self-oscillation; put Pitch Shift in Slot A + load "Shifter Spiral" for an endless riser; tick Sync + pick Division to lock to tempo; hit Freeze to hold the wash as an infinite pad; reorder slots (Order) to change how each repeat mutates. Zero latency; delay knob glides click-free.

2026-07-07 | SWARM | SHIPPED (full, [x]): mass granulator (Glow clone), Phase 2a. 10 s stereo circular capture buffer (allocated per-SR in initialize()), grain scheduler = poisson (exponential inter-arrival at `density` grains/s, 1..500) OR grid-sync (fixed division period from `context.transport().tempo`, cluster of round(density·period) grains/tick so density stays meaningful when synced). Per-grain randomised at spawn (suite testsig::Rng reused): position spray ±spray_ms around a read head 300 ms behind write, pitch scatter ±24 st (free or semitone-quantised), size 10–500 ms, Tukey(α=0.5) window, equal-power pan within width, reverse probability. Interpolated circular buffer reads (wrap = benign old-audio artefact, never NaN). Voice pool = fixed [Grain;128] preallocated, steal-oldest by monotonic serial (alloc-free process()). Density-normalised 1/√overlap. Shimmer = +12 st two-tap granular shifter on the summed cloud → in-loop tanh + DC blocker (PRD §3) → written back to buffer; a SHIMMER_DRIVE=6.0 makeup offsets the grain cloud's ~−10 dB round-trip loss so the feedback reaches ~unity loop gain at 100 % and self-oscillates BOUNDED past that (tanh caps it). Freeze locks the write head (input still monitored to dry per mix). Zero reported latency (time-smearing effect, per OUROBOROS) ⇒ mix=0-null assertion, NOT lag-0 coherent-peak. 13 params (density/size/spray/scatter/quantize/reverse/shimmer/freeze/sync/division/width/mix/out). Done-bar MET first full run (no Fable, no descope): (1) onset count strictly monotonic in density on an impulse-seeded frozen buffer at 5/50/200 gr/s — counting = relative-threshold (0.15·peak) envelope crossings w/ 3 ms refractory (relative ⇒ robust to density-normalisation scaling; sharp impulse clicks stay separable even overlapping); (2) freeze+silent-input 5 s RMS > −50 dBFS; (3) shimmer=1.1 over 30 s peak ≤ 0 dBFS + finite. Plus mix=0 null < −80 dB, voice-cap-never-exceeded (≤128, saturates >64 at 500 gr/s), extreme-macro finite/≤0 dBFS fuzz. One fix cycle: shimmer feedback initially decayed to silence (density-normalised decorrelated grains gave loop gain <1) → added SHIMMER_DRIVE makeup. 8/8 crate tests green; clap-validator PASS, pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread safety + all buses), swarm.clap + swarm.vst3 installed. 6 presets (Texture Bed, Frozen Cathedral, Shimmer Bloom, Rhythmic Dust, Reverse Swell, Granular Chaos — none set freeze, which renders silent from an empty buffer), 12 renders in renders/SWARM/. docs/SWARM.md + README row. Nothing added to suite-core (all SWARM DSP local to the crate) ⇒ no suite-wide revalidate. No Fable escalation, no ultracode, nothing deferred. | FL: Find more plugins → add "Qeynos SWARM" on a pad/vocal/loop/bus; load "Texture Bed" for an instant cloud, "Shimmer Bloom" for the +12 st ascending bloom; tick Grid Sync + Division with "Rhythmic Dust" for tempo-locked bursts; hit Freeze to lock 10 s of audio as an infinite evolving pad (dry still passes per Mix); Reverse flips grains, Width spreads the cloud. Zero latency.

2026-07-07 | SMUDGE | SHIPPED (full, [x]): spectral chaos (Smear clone), Phase 2a. Streaming STFT 2048/hop 512/Hann (reuses `suite_core::stft`, EMBER engine) → four per-frame ops in FIXED order 1→4, each with its own amount and EXACTLY bypassed at 0 (op skipped, spectrum untouched): (1) **scramble** — Fisher-Yates permute bins within ±N-bin neighbourhoods (N=range·48, DC/Nyquist fixed), perm redrawn every `Scramble Rate` frames (1=chaos, 4–8=musical), crossfaded by amount, permuted content ×2.0 makeup to restore the −6 dB overlap-decorrelation loss (√(fft/hop)); (2) **spectral delay** — 30 ~⅓-oct bands, per-band frame delay 1..32 from a tilt curve (lows-short/highs-long or inverse), additive echo `cur+amt·delayed` with soft-limited feedback (≤0.95, always decays), echo energy-normalised down to a ~1 s-release envelope of input frame-energy; (3) **blur** — per-bin one-pole magnitude averaging (τ per band from Blur Time+Tilt) + phase-vocoder advance blended along the shortest arc as blur dominates (>50%); (4) **smear/stretch** — bin-index remap ×0.5–2 (linear interp, energy-normalised), crossfaded by amount. **Chaos macro** = S&H (suite Rng) every `Chaos Rate` frames × `Chaos Depth`, modulating op params — amounts MULTIPLICATIVELY (never lifts a 0 ⇒ exact bypass survives chaos). Amounts/tilt/factor frame-rate one-pole smoothed (primed on first configure); mix per-sample smoothed. **Wet-path safety soft-clip = exact identity below 0.9** (tanh only above) is the hard ≤0 dBFS guarantee that does NOT break the identity/null done-bars (normal reconstruction stays under the knee; a global tanh would err ~−28 dB at 0.5). Reports 2048-sample latency; dry delay-matched. Stereo = L primary (advances all shared per-frame state) + R secondary (reuses it), hop-aligned. Done-bar MET first full run (no Fable, no descope, nothing to suite-core): (1) ALL amounts 0 → wet nulls vs latency-delayed dry <−60 dB (per-op exact bypass) + holds under full-depth/rate chaos; (2) scramble>0 → mean per-frame magnitude-spectrum correlation vs dry <0.9 (independent analysis STFT, 4-hop aligned) while total energy within ±3 dB. Plus mix=0 null <−80 dB, delay feedback @95%/amt100% bounded ≤0 dBFS over 30 s, everything-on+full-chaos finite/≤0 dBFS. 8/8 crate tests green; clap-validator PASS, pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread safety + all buses), smudge.clap + smudge.vst3 installed. 14 params (scramble/srange/srate, delay/dtilt/dfb, blur/btau/btilt, stretch/sfactor, crate/cdepth, mix), 6 presets (Gentle Haze, Frequency Rain, Time Smear, Chaos Engine, Frozen Blur, Spectral Echoes), 6 renders in renders/SMUDGE/. docs/SMUDGE.md + README row. Nothing added to suite-core ⇒ no suite-wide revalidate. No Fable escalation, no ultracode, nothing deferred. | FL: Find more plugins → add "Qeynos SMUDGE" on a pad/vocal/drum-bus/full mix; load "Gentle Haze" for a subtle veil, "Frequency Rain" for glitchy scattered delays, "Time Smear"/"Frozen Blur" to melt transients into clouds, "Spectral Echoes" for cascading band-delays (sweep Delay Tilt/Feedback), "Chaos Engine" to let it drift on its own (raise Chaos Depth). Stretch Factor off 1.0 = spectral pitch illusion. Host auto-delay-comps +2048 samples; Mix blends parallel.

2026-07-07 | MURMUR | SHIPPED (full, [x]): stochastic reverb (Hikari clone), Phase 2a. **NEW suite-core module `suite_core::fdn`** (the architectural obligation — UNDERTOW/SEANCE/CHAMBER reuse it): `Fdn8` = 8 delay lines, 8×8 Householder feedback `H=I−(2/N)vvᵀ` with v=ones implemented as the O(N) sum trick `(Hv)_i = v_i − (2/N)Σv` (orthogonal reflection ⇒ lossless apart from decay+damping), per-line one-pole damping (DC-unity LP; `set_damping(tilt)` bright→dark with a per-line color spread), per-line runtime delay lengths within preallocated max (`set_delays(&[usize;8])`, alloc-free), 4-allpass input diffusion per channel (`set_diffusion(coeff)`), stereo in/out taps (L→even lines, R→odd; even taps→L, odd→R w/ fixed sign patterns), decay via per-line `g_i=10^(−3·Lᵢ/(RT60·sr))` (`set_rt60`), `new(max_delay_samples, sr)`/`process(l,r)->(l,r)`/`reset()`, + `fdn::measure_rt60(ir,sr)` Schroeder-EDC helper. 5 suite-core fdn tests (impulse monotone decay; measured RT60 ±25% at 2 settings; energy bounded; reset clears; longer RT60 slower). MURMUR itself: band-energy-rise onset detector (fast/slow env on an 800 Hz HP, 120 ms refractory, `sensitivity` lowers ratio 3.0→1.2, zero-latency spectral-flux stand-in) + manual re-roll button (edge-detect on a BoolParam flip) trigger a fresh random draw (suite `Rng`): 8 delay lengths (nominal geo-spread 15–70 ms × Size, ±40%×Randomness deviation, then nudged mutually-prime-ish via odd+gcd bump), damping color, diffusion coeff — every hit a different room. **TWO Fdn8 ping-pong**, BOTH always process input (idle pre-warmed w/ its own room ⇒ already holds the incoming transient), 50 ms **equal-power (cos/sin) crossfade** on trigger; on completion swap + reset + redraw the now-idle FDN. Freeze = RT60→1e5 (line gain capped 0.99995 ⇒ sustains, never grows) + 30 ms input duck→0. Live: Decay/Freeze applied to both every block; Color live only on knob-change (per-draw color randomness survives a still knob); Size/Randomness shape the next draw. Width (M/S on wet) + wet DC-block + knee'd safety-clip (identity <0.9 ⇒ mix=0 uses dry, untouched) + per-sample-smoothed Mix. Zero reported latency (time-smearing effect, like OUROBOROS/SWARM) ⇒ mix=0-null not lag-0-coherent-peak. Done-bars MET first full run (no Fable, no descope): (1) two impulses 2 s apart @ randomness 1.0 → tail cross-corr 0.9-fail-safe <0.9 (+asserts ≥4 draws); (2) randomness 0 → measured RT60 within ±25% of Decay @ 1.5 s & 3.0 s; (3) freeze charge 1 s→5 s silence RMS > −50 dBFS; (4) re-roll max sample-delta ≤4× steady tail (click-free); (5) mix=0 nulls vs dry <−80 dB. 7 crate tests green. **BECAUSE suite-core touched: `cargo test -p suite-core --release` 31/31 green + `build.ps1 -All` ALL 12 crates GREEN** (clap-validator PASS, pluginval s8 SUCCESS incl Fuzz/thread-safety/all-buses, murmur.clap + murmur.vst3 installed, every prior plugin revalidated). 9 params (size/decay/color/randomness/sensitivity/freeze/reroll/width/mix), 6 presets (Never The Same Hall, Shifting Chamber, Grief Space, Percussion Rooms, Frozen Nave, Small Odd Room — none freeze), 6 renders in renders/MURMUR/. docs/MURMUR.md + README row. No Fable escalation, no ultracode, nothing deferred. | FL: Find more plugins → add "Qeynos MURMUR" on drums/vocal/keys/bus; load "Never The Same Hall" & play a phrase (each note = a slightly different room); "Percussion Rooms" for drums, "Grief Space" for dark pads, "Frozen Nave" for a cathedral; raise Randomness for wilder room-to-room variation, drop to 0 for a fixed reverb (RT60=Decay); hit RE-ROLL for a new room by hand, or FREEZE to hold the wash as an infinite pad. Zero latency; Size/Color/Width/Mix shape it.

2026-07-07 | UNDERTOW | SHIPPED (full, [x]): kick-to-rumble generator (Taste-tailored; hard/melodic techno low-end), Phase 2 taste-tailored. Sits ON the kick track — passes dry, adds a kick-derived kick-ducked sub-bass rumble. Chain (all local to the crate, NOTHING added to suite-core ⇒ no suite-wide revalidate): **transient strip** (fast/slow suite EnvFollower peak diff gates the attack down by `strip` so the rumble source is the kick BODY not its click) → **saturation** (`suite_core::dsp::Shaper::TubeTanh` @ **2× `Oversampler2x`**, pregain 1+9·drive) → **`suite_core::fdn::Fdn8` small/dark** (8 lines geo-spread **8–25 ms × Size** scale 0.6–1.7, `set_damping(0.6)` dark, `set_diffusion(0.6)`, `set_rt60(Decay)`; delays nudged mutually-prime-ish; only reconfigured on size/decay CHANGE via epsilon cache to avoid per-block churn; stereo pair summed → mono sub + kept side for width) → DC-block → **resonant LP** (suite `Svf` .lp, 90–250 Hz, res Q) → **key-lockable resonant tune peak** (second `Svf` .bp at the note fundamental, `tuned = low + amount·4·bp`; Note is an IntParam 0..35 → C0..B2, A1=idx21=55 Hz; note↔name formatter + string_to_value both ways) → **ducker** (fast-attack 1 ms peak EnvFollower on the DRY mono kick, normalised by an instant-attack/1500 ms-release peak-tracker so `sc_norm→1` at every onset regardless of kick level; reduction = depth·24 dB; release 80–300 ms) → rumble gain → **width** (side = 0.5·(fl−fr) steeply HP'd at 150 Hz ×2 SVF, so **everything <150 Hz is mono**; `rumble_l/r = safety_clip(sub ± side)`) → + dry. Audible scalars smoothed inside the core (OnePole `Smooth`, 12 ms). Gains are dB params (rumble −60..+12, dry −60..+6, trim ±24) converted dB→linear in snapshot(); the dB floor (≤−59.5) maps to TRUE 0 so "rumble muted → dry" nulls EXACTLY. **Zero reported latency** (reverb wet path = time-smearing ⇒ the null convention is rumble-muted-→-dry, NOT lag-0 coherent peak; the 2× OS group delay only affects the wet rumble which is not meant to phase-align with dry). Done-bars MET (no Fable, no descope, no ultracode): (1) **duck** — 4-on-floor `testsig::synth_kick` @130 BPM, rumble-only (dry=0): at each steady onset the rumble envelope dips ≥ the configured Duck Depth (dB) measured against a NO-DUCK reference render at the same instant (avoids the reverb-tail-decay confound of a naive onset-vs-interonset self-compare), AND swells back ≥6 dB between hits (breathing); (2) **tune** — Note=A1, Amount=1, broadband-noise excitation, rumble-only → 16384-pt STFT accumulated magnitude + quadratic peak interp lands **within ±3% of 55 Hz**; (3) **mono low-end** — Width=1, rumble-only stereo → 4th-order-LP'd L/R Pearson correlation below 150 Hz ≥ 0.9; (4) **30 s bounded** under a hot setting (drive 0.9, res 6, width 1) finite + peak <2.0; plus universal (finite/≤0 dBFS/non-silent) on all 6 preset renders + rumble-muted→dry exact null. Render kick pattern scaled ×0.22 for headroom (dry+rumble ≤ 0 dBFS artifact); the plugin itself intentionally can exceed 0 dBFS on a hot kick track (user gain-stages — it's an additive generator). 11/11 crate tests green; clap-validator 14 pass / 0 fail / 6 skipped (1 scan-time WARNING only, benign); pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread-safety + all buses); undertow.clap + undertow.vst3 installed + dist/. 14 params (strip/drive/size/decay/lpfreq/lpres/tunenote/tuneamt/duckdepth/duckrel/rumble/width/dry/trim), 6 presets (Warehouse Bed, Rolling Rumble, Tight Modern Techno, Cavern Floor, Hypnotic Wash Low, Distorted Drone Bed), 8 renders in renders/UNDERTOW/ (incl full_mix + rumble_only). docs/UNDERTOW.md + README row. No Fable escalation, no ultracode, nothing deferred. | FL: add "Qeynos UNDERTOW" **on the kick track** (Dry defaults to unity so the kick still plays); load "Tight Modern Techno" for a punchy ducked sub, "Rolling Rumble"/"Hypnotic Wash Low" for a tuned melodic hum (set Tune Note to your key, raise Tune Amount), "Cavern Floor"/"Distorted Drone Bed" for a huge saturated tail; Duck Depth/Release sets how the rumble breathes around the kick; Strip removes the click; Width only spreads the top of the rumble (low-end stays mono). Zero latency.

2026-07-07 | SNAP | SHIPPED (full, [x]): snare/clap generator (Taste-tailored; IMPACT-style MIDI instrument). Mono-ish last-note voice, stereo out, no audio in, MidiConfig::Basic — copied from IMPACT. Three engines crossfaded by a continuous **Mode** (equal-power cos/sin, Snare↔Hybrid↔Clap): **BODY** (phase-continuous sine/tri @ **Tune** 100–400 Hz, fast exp pitch env f_start=Tune×1.9→Tune, τ=8 ms·len — shell knock; amplitude governed by master amp env only, IMPACT-faithful ⇒ phase+level continuous retrigger), **RATTLE** (per-channel white noise → 3 parallel `Svf` BP formants 800/1.5k/3k, own env sped by **Snap**), **CLAP ENGINE** (at note-on schedule **Taps** 3–5 short noise slaps evenly spaced across **Spread** 8–30 ms + per-burst **Humanize** pre-delay jitter from a per-note-reseeded `testsig::Rng` [deterministic per seed]; +1 longer tail burst just past spread; all through `Svf` BP(**Tone** center 500 Hz–5 kHz log)→LP tone shaping; tap τ=2.5 ms·len, tail τ=90 ms·len). **Transient click** layer (high-BP noise, fast env, level=0.12+0.55·Snap) added post-blend every mode. **Body/Noise** balances body vs rattle in the snare engine. **Decay** (40–1200 ms) is the master amp-env exp decay AND a length macro scaling every sub-envelope (RT done-bar). Chain: sum per channel → **Drive** (suite `Shaper::TubeTanh` @ **2× `Oversampler2x`** per channel) → master amp env (1.5 ms from-current declick ramp + trig fade-in on the fast noise layers) → soft clip (knee'd identity <0.9) → **Level**. **Width** = decorrelated per-channel noise: n_L/R = (shared + k·diff_L/R)/√(1+k²), k=0.8·width ⇒ noise corr = 1/(1+k²) ≥ 0.61 at max (mono-compatible >0.5); tonal body stays mono; width=0 ⇒ L≡R exactly. **Key Track** off by default (ref note A2=MIDI 45 reproduces Tune). ALL DSP local to the crate (body osc, formant bank, clap scheduler, width) ⇒ **NOTHING added to suite-core ⇒ no suite-wide revalidate** (only build.ps1 snap). Done-bars MET (no Fable, no ultracode, no descope): (1) **clap onsets** — Mode=1, Humanize=0 → onset count == **Taps+1** (slaps + tail) via SWARM-style envelope-peak method (0.3 ms-atk/2 ms-rel peak follower → hysteresis gate set 0.40·peak / reset 0.22·peak, 4 ms refractory) over the spread+15 ms window; asserted Taps∈{3,4} (10 / 7.5 ms spacing); Taps=5 @ 30 ms = 6 ms spacing overlaps into fewer resolvable onsets [documented, not asserted]; (2) **tone→centroid** — Clap mode accumulated-magnitude STFT spectral centroid rises monotonically across Tone 0.1/0.5/0.9; (3) **retrigger click-free** — IMPACT's exact recipe (mid-decay retrigger worst sample-step ≤ 1.6× no-retrig baseline + 0.03); (4) **decay→RT** — measured −20 dB tail time scales with Decay (120 vs 700 ms, >1.5×); + **width** L/R Pearson >0.5 at max width; + universal (finite/≤0 dBFS/non-silent) per channel on all 6 preset renders. 10/10 crate tests green. ONE fix attempt (clap-validator convert-params): the continuous **Mode** FloatParam's bucketed value_to_string ("Snare"/"Clap") can't round-trip text_to_value → made it always emit the parseable "NN% Label" + added a matching string_to_value that reads the leading number (lesson: any custom value_to_string needs an inverse string_to_value, even on FloatParams — IMPACT/UNDERTOW's IntParam lesson generalises). Then clap-validator 16 pass / 0 fail / 4 skipped (1 benign 381 ms scan WARNING); pluginval s8 SUCCESS (44.1/48/96k, blocks 64..1024, incl Fuzz Parameters + thread-safety + all buses); snap.clap + snap.vst3 installed + dist/. 13 params (mode/tune/balance/snap/decay/taps/spread/humanize/tone/drive/width/level + keytrack), 6 presets (Rimshot Knock, Wet Techno Clap, DnB Crack, Gunshot Layer, 90s Machine Clap, Airy Top Snare), 6 stereo renders in renders/SNAP/. docs/SNAP.md + README row. No Fable escalation, no ultracode, nothing deferred. | FL: Find more plugins → add "Qeynos SNAP" to a channel, play notes (each fires a snare/clap); load "Rimshot Knock"/"Airy Top Snare" (turn Mode toward Snare) for snares, "Wet Techno Clap"/"90s Machine Clap" (Mode toward Clap) for claps, "DnB Crack"/"Gunshot Layer" for hybrids; Mode morphs snare↔clap, Taps/Spread/Humanize shape the clap, Tune/Body-Noise/Snap the snare, Decay the tail length, Width spreads the noise (stays mono-compatible), Drive dirties it; enable Key Track to tune from the keyboard; fire rapid repeats to hear the declicked retrigger.

2026-07-07 | SEANCE | SHIPPED (full, [x]): ethereal vocal machine (Taste-tailored; Cynthoni/Sewerslvt ghost vocals) — the VOX-suite keystone. **NEW suite-core module `suite_core::shift`** (VOXKEY/VOXFIT reuse it): `ShiftEngine` = streaming formant-preserving phase vocoder on `suite_core::stft` (2048/512 Hann WOLA). Per frame: analysis magnitude + **instantaneous frequency** (Laroche/Dolson/Bernsee phase-difference, osamp=N/hop) → **spectral envelope via CEPSTRAL LIFTERING** (real cepstrum of log|X|, keep low quefrency L=fft/16, exp) → flatten `exc=mag/env` → **pitch-shift the excitation** by bin-remap `round(k·pitch_ratio)` w/ PV freq scaling → **re-apply envelope remapped by its OWN formant_ratio** (`env(k/formant_ratio)`) ⇒ pitch & formants move INDEPENDENTLY → synthesis phase re-accumulated. **Phase PRIMING** (seed sum_phase = true analysis phase on the first frame after reset) is what makes the unity-ratio identity null usable. API: `new(fft,hop,sr)`/`default_geometry(sr)`, `set_pitch_ratio`/`set_formant_ratio`/`set_envelope_preserve(bool)`/`process(x)->f32`/`latency()` (==fft)/`reset()`. Mono; run two for stereo. **Honest quality (documented for VOXKEY/VOXFIT):** PV identity is a RESYNTHESIS not a passthrough — wet-vs-latency-delayed-dry nulls only to ≈ **−15 dB steady / −8 dB vibrato**, so retune plugins MUST gate mix=0 on the DRY path, never the wet null. 4 suite_core::shift unit tests: (1) +5 st preserve-on → measured f0 +5 st ±20 cents while envelope shift-ratio ≈1.0 ±8% (measured by log-envelope cross-correlation on a log-freq axis — robust to the stationary harmonic comb that biases naive peak-picking, DRIFT's Welch lesson); (2) formant ratio 1.26 pitch 1.0 → envelope moves ×1.26 ±10% while f0 ±15 cents (NOTE: build brief's "~+3 st" was approximate; the engine moves formants by exactly the requested ratio, 1.26≈+4 st — decided & documented); (3) identity residual < −12 dB on a steady tone; (4) no-preserve octave-up f0 check. SEANCE plugin chain: two main ShiftEngines (pitch ±12 st + formant, preserve on) → **BPM-synced chopper** (free-running phase over one Rate division 1/2..1/32; 4 shapes Square/Stutter/Ramp/Double + Random S&H; OnePole ~5 ms edge slew; Depth blends gate→unity; reads `context.transport().tempo`) → **shimmer FDN verb** (`suite_core::fdn::Fdn8` medium/large 20–75 ms×Size, RT60=Decay, +**a cheap mono +12 st ShiftEngine (1024/256, preserve off) IN THE FEEDBACK LOOP** w/ tanh soft-limit ×1.6 makeup + DC block, Shimmer scales the octave feedback ≤0.95) → **wash** (Svf LP 18k→2.5k + fractional-delay **wow** ~0.45 Hz, decorrelated L/R phase; amount 0 = full bypass, no added delay) → **drowned-vocal ducker** (level-normalised EnvFollower on the DRY key → INVERSE duck: wet pulled down while vocal active, SWELLS in the silence after over Duck Release) → **3 macros** (GHOST=formant+wash+mix-blend, DROWN=size+wet+duck, CHOP=chop depth; each folds into underlying params in `RawControls::resolve`) → linear Mix (0=latency-matched dry, exact) → knee'd safety-clip (identity <0.9 ⇒ mix=0 null survives) → Out. Reports **2048-sample latency**; dry path DelayLine-matched. Done-bars MET (no Fable, no ultracode, no descope): (1) **+12 st doubles f0** of synth_vocal within ±20 cents (MPM); (2) **chop gate periods match the division ±1 ms** @120 BPM (direct Chopper rising-edge spacing on 1/4·1/8·1/16 + full-core envelope-autocorrelation periodicity); (3) **drowned-vocal swell ≥6 dB** — burst-then-silence vocal, wet in the silence ≥6 dB above wet during the active burst (measured on the full core); + mix=0 == latency-delayed dry exact null + universal on all 6 presets + 2 showcase renders. 10/10 seance crate tests green. **BECAUSE suite-core touched: `cargo test -p suite-core --release` 35/35 + 2 doctests green + `build.ps1 -All` ALL 15 crates GREEN** (clap-validator SUCCESS, pluginval s8 SUCCESS incl Fuzz/thread-safety/all-buses, seance.clap+vst3 installed, every prior plugin revalidated). ONE bounded issue fixed inline (no attempt-count burn): a hot shimmer+wet render peaked +8.76 dBFS → added the knee'd output safety-clip (UNDERTOW/SMUDGE pattern). 18 params (pitch/formant/preserve/choppat/choprate/chopdepth/vsize/vdecay/vshimmer/vwet/wash/duckdepth/duckrel/ghost/drown/chopmac/mix/out), 6 presets (Grief Pad Vox, Drowned Lead, Whisper Choir, Formant Ghost, Chopped Ether, Sunken Chorus) + 2 showcase renders in renders/SEANCE/. docs/SEANCE.md + README row. No Fable escalation, no ultracode, nothing deferred. | FL: Find more plugins → add "Qeynos SEANCE" on a vocal/lead/pad; load "Grief Pad Vox"/"Drowned Lead" for underwater swells (raise DROWN), "Whisper Choir"/"Formant Ghost" for formant-lifted ghosts (raise GHOST), "Chopped Ether" for gated stutter (raise CHOP + pick Rate/Pattern), "Sunken Chorus" for octave shimmer; Pitch ±12 st + Formant move independently (Preserve on); Mix blends; host auto-delay-comps +2048 samples.

2026-07-07 | FREEZE-MIX | SHIPPED (user request mid-SEANCE, addressed immediately after SEANCE ship): added a **Freeze Mix** fader (0–100%, default 100% = unchanged) to every plugin with a Freeze toggle — **EMBER, MURMUR, OUROBOROS, SWARM** (TRACER's "freeze" is an internal crossover-confidence hold, not a user toggle, so untouched). Freeze stays a toggle; Freeze Mix blends the output between the live input (0) and the fully-frozen texture (1) while Freeze is engaged, so freezing is a smooth crossfade instead of a sudden jump. DESIGN DECISION (documented, PRD §0): implemented as a **safe output crossfade toward the live input** (`out = fm·frozen + (1−fm)·live`) rather than feeding the live signal back INTO the frozen feedback/RT60=∞ path — the latter would build up / run away (MURMUR FDN lossless under freeze, OUROBOROS 100% feedback). The frozen path is left exactly as-tuned; we only fade the live source back in at the output. EMBER blends toward its latency-matched dry (2048); the other three are zero-latency and blend toward the raw input. Each blend is a dedicated ~15 ms OnePole smoother (`fm`/`fm_s`) so fader moves and freeze toggles don't zipper. New param `freezemix` on each (FloatParam pct, default 1.0), wired through snapshot + presets `settings_from_preset` + GUI (a FREEZE MIX slider beside the FREEZE checkbox). All existing freeze done-bars still pass (default 1.0 preserves the classic hard freeze); added a `freeze_mix_zero_passes_live` test to each (freeze on + freeze_mix=0 → output nulls to the live input < −40 dB) covering all four architectures (spectral/reverb/feedback/granular). `build.ps1 -All` all 15 crates GREEN (clap-validator + pluginval s8 incl Fuzz-parameters exercising the new param; VST3+CLAP reinstalled). No suite-core change. No Fable, no ultracode, nothing deferred. | FL: on EMBER/MURMUR/OUROBOROS/SWARM, engage FREEZE then pull FREEZE MIX down from 100% to hear the live source blended under the frozen tail (100% = the old instant-freeze behavior).
2026-07-08 | HARD-CHECKPOINT-3 | gates green; 4 confirmed findings fixed (CLEAVE RT panic off-by-one [blocker], PLUCK RT alloc [major], PLUCK body IR spec compliance, reserved-name preset loss); 4 refuted | FL: rescan for FLYBY/CLEAVE/PLUCK/SHAPESHIFT/CHAMBER

## NOTES
- PRESET-EXPANSION-A (2026-07-08): **read this before Batch B.** PRESET-EXPANSION was split A/B; Batch A shipped 12
  plugins, Batch B owns the remaining ~16 (drift, ouroboros, swarm, smudge, murmur, flyby, cleave, pluck, shapeshift,
  chamber, carve, nerve, halt, bandaid, patina, chorale — split as you see fit). Do NOT tick the §7 PRESET-EXPANSION
  box until Batch B lands.
  (a) **The pattern is `plugins/grit/src/presets.rs`** — copy it verbatim structurally: each `PRESET_JSON` blob gains a
  `"category"` field (a String; serde pulls it into `Preset.category` because it is declared before the `#[serde(flatten)]`
  numeric `values` map — DO NOT put category in `values`, it is numeric-only). The `settings_from_preset` is unchanged.
  The test module gains a `count_diffs(a: &Settings, b: &Settings) -> usize` (compare enums/bools by `!=`, floats by
  `>1e-3`) and a `bank_meets_expansion_quality_gate` test asserting: `>=15` presets (>=12 for a genuinely simple utility),
  every preset categorised, `>=4` diffs vs `Settings::default()`, pairwise `>=2` across the whole bank, unique names.
  **Rule 4 (render passes universal assertions) is ALREADY enforced by each crate's `render_tests` loop over
  `PRESET_JSON`** — do NOT touch it or its `>=5`/`>=6` length check; it fails loudly if a preset renders silent / >0 dBFS,
  which is your signal to fix the PRESET (never the assertion). Keep existing presets; rename only on a rule violation.
  (b) **The preset-bar sections are already wired suite-wide** — `PresetBar::show` (suite-core/src/ui.rs, shipped in the
  grit commit) auto-groups any factory bank that carries categories into headed sections in first-appearance order, so
  Batch B needs ZERO suite-core changes; just tag the presets. `PresetBar::filter(Some(cat))` still exists for OVERSEER's
  type/theme banks and is untouched.
  (c) **Efficient method that worked for Batch A:** author one plugin yourself to re-confirm the pattern, then fan out
  parallel Opus subagents (one per plugin, each editing ONLY its own crate, each running `cargo test -p <crate> --release`
  to green, each NOT committing), then the orchestrator independently re-tests + does ONE commit per plugin. Subagents
  serialize naturally on the shared `C:\qvs-target` cargo build lock — no coordination needed. Watch for: a plugin whose
  preset test lives in `lib.rs`/`tests.rs` rather than `presets.rs` (grep `PRESET_JSON` + `assert_universal` to locate);
  instruments (pluck/chorale) warrant the high end (20-30); a preset that clips >0 dBFS needs its out-trim pulled.
  (d) **Naming taste:** dark techno / atmospheric dnb / Cynthoni-Sewerslvt — evocative + purpose-driven, never
  'Preset N' or a settings description. Cover each plugin's DSP range: subtle/utility -> genre-core -> extreme/sound-design.
  (e) After Batch B: `cargo test --workspace --release`, tick the §7 box, LOG line, push; then BUILT-IN-MANUALS.
- CHORALE (2026-07-08): **Phase 3 is COMPLETE** — the handoff is to Phase 4 (Python tools), not another plugin.
  (a) **NEXT §7 items are W5-PROJECT-JANITOR → W6-SAMPLE-LIBRARIAN → W7-REFERENCE-GAP** (PRD §7 "Phase 4 —
  remaining automations"), then the **POLISH** phase (PRESET-EXPANSION / BUILT-IN-MANUALS / PEDAL-UI) + the
  **FINAL CHECKPOINT**. Phase 4 tooling is **Python via uv** (PRD §8 notes: `uv python install 3.12`, PEP-723
  `requires-python=">=3.12,<3.13"`; librosa/numba on 3.14 are bleeding-edge). **W1–W7 need FL running with the
  MCP controller**: if `fl_connection_status` fails, build the tool against **recorded fixtures** and mark live
  verification in **CHECKPOINTS.md**, move on (PRD §8). W8 (VITALGEN) already validated against the INSTALLED
  Vital 1.5.x — reuse that pattern for any preset-schema work.
  (b) **Phase-3-wide flags for the FINAL CHECKPOINT** (all shipped, but worth an adversarial pass): (i) the
  **SpectrumPublisher retrofit was DEFERRED for OVERSEER / NERVE / _template** (DEFERRED.md) — those instances do
  NOT appear in X-RAY; decide if that's acceptable or finish them. (ii) **X-RAY's 25-plugin retrofit shipped but
  was left UNCOMMITTED by the X-RAY iteration** — recovered here as `wip(recovery)` (commit before start(CHORALE)).
  The retrofit compiles + the two-instance done-bar passed, but a full **`cargo test --workspace` + `build.ps1 -All`
  re-sweep has NOT been re-run since the recovery commit** — the FINAL CHECKPOINT ultracode sweep should confirm
  all 27 crates still green together (CHORALE alone was gated via `cargo test -p chorale` + `build.ps1 chorale`).
  (c) **CHORALE reuse patterns for any future resonator/comb work:** a KS/waveguide loop driven **continuously** at
  high feedback needs a **memoryless soft-clip (tanh) inside the loop** to self-limit — it bounds the energy without
  detuning (pitch is set by delay length, not amplitude). Spread a scale across a **bounded octave span with wrap+
  stack** (`offset[i%L] + 12·(⌊i/L⌋ % SPAN)`) rather than climbing indefinitely — keeps pitches musical/off-Nyquist
  and the wrapped duplicates detune into a chorus for free. For "gain ∝ input energy at a pitch", `SpectrumTap`'s
  constant-Q bands are ready-made and cheap; remember constant-Q reads **pink as flat / white as +3 dB/oct**, so
  design any band-energy test around white (or an explicitly-tilted) source, not pink.
- X-RAY (2026-07-08): reusable patterns + the handoff to CHORALE.
  (a) **NEXT §7 item is CHORALE** (resonator bank, last Phase 3 plugin) — a fresh crate from
  plugins/_template, no X-RAY coupling. Design (SPECS "CHORALE"): **12–24 KS waveguide
  resonators** (high-feedback damped loops) tuned to **held MIDI / scale+chord select /
  chromagram key-detect**; input excites all; **per-resonator gain = input band energy at its
  pitch**. Params: tuning source, decay, damp, spread (cents), stereo alternate, wet solo, mix.
  **Reuse (all in the tree):** PLUCK is the direct template — its `dsp::FracDelay` KS loop with
  the **cent-accurate tuning solve** (`frac_read + p_damp + p_ap == sr/f0`, all-pass low-freq
  phase delay subtracted so the fundamental stays in tune; verified ±10 cents) and its held-note
  bitmap MIDI voice-assign + chromagram key-detect (via `suite_core::stft`) port almost verbatim;
  done-bar (§4: "resonator peaks at tuned pitches ±10 cents") is PLUCK's test recipe re-run.
  For "per-resonator gain = input band energy at its pitch", the new
  **`suite_core::spectrum::SpectrumTap`** band energies (or per-resonator `Svf` bandpasses at
  exact pitches) are ready-made. Decay→feedback per-string from PLUCK; DC-block + soft-clip on
  feedback paths per §3 conventions.
  (b) **`suite_core::spectrum::SpectrumTap`** — reusable 32-band ⅓-oct constant-Q analyzer
  (alloc-free, <0.5 % core): any future metering/band-energy need (CHORALE band gains, dynamic
  EQ, W7-REFERENCE-GAP) should reuse it rather than an FFT unless bin-resolution demands one.
  (c) **`SpectrumPublisher` retrofit is proven suite-wide**: field + init + feed-loop + publish
  + Drop-release compiled clean on 25 heterogeneous crates (incl. KeepAlive-returning
  instruments ASCEND/IMPACT/PLUCK/SNAP and WIRE's permit_alloc closure) with zero borrow
  conflicts — the end-of-process `buffer.iter_samples()` reborrow is safe everywhere because
  earlier `as_slice` bindings are dead by then. Any new plugin copies the same 5 lines.
  (d) **Two-pass egui overlay+legend** (xray/src/lib.rs): to let legend hover/click affect the
  curves in the same frame, allocate the plot rect and paint the background/grid first, build
  the legend (collect hover/click), THEN paint the curves into the earlier rect via the saved
  painter. Avoids one-frame lag without egui memory tricks.
- BANDAID (2026-07-08): reusable patterns + the handoff to PATINA.
  (a) **NEXT §7 item is PATINA** (analog lo-fi character) — a fresh crate from plugins/_template,
  no BANDAID coupling. Design (SPECS "PATINA"): `in ─ wow/flutter (frac delay ← 0.4 Hz wow + 8 Hz
  flutter + random walk) ─ saturation ─ head-bump EQ (LS 60–120 Hz) ─ azimuth (L/R HF phase skew)
  ─ dropouts (random dips) ─ + noise (hiss/hum/crackle, keyed to input env) ─ age macro ─ mix`.
  Params: wow, flutter, sat, bump, azimuth, dropout, noise type/level/key, age, mix. Done-bar (§4):
  **wow → f0 modulation at ~0.4 Hz measurable** (reuse the FLYBY/SHAPESHIFT doppler-periodicity /
  Goertzel-THD-vs-time helper), **noise floor rises with input env** (env-keyed gate on the hiss).
  **Reuse (all already in the tree):** SEANCE's **wash/wow** core + WIRE's **character-stage** patterns
  (per the prompt); `dsp::FracDelay` (Catmull-Rom) is copy-pasted in **FLYBY/PLUCK/CHAMBER** for the
  wow/flutter fractional delay; `dsp::{Oversampler2x/4x + tape_soft/Shaper}` for the saturation
  (GRIT/SHAPESHIFT PDC recipe if a dry path is mixed at lag 0 — but keep wow zero-lag like FLYBY so
  mix=0 nulls); `dsp::{Svf, OnePole, EnvFollower}` for the LS head-bump, azimuth HF skew, and the
  env-keyed noise; `testsig` (synth_kick/pink/sine) + harness for the done-bars. Age = a macro that
  scales wow+flutter+dropout+noise together (BANDAID-solo/ SHAPESHIFT-orbit style single-knob→many).
  Add 2-3 modlisten targets (wow/noise/mix) via `ui::mod_section` + `modulated_float`.
  (b) **Parallel-delta reconstruction for any band/parallel processor that must null at neutral**
  (bandaid/dsp.rs): output `x + Σ (g_b−1)·band_b`, NOT `Σ g_b·band_b`. An LR4 split-sum is
  allpass-flat (phase-lagged), so the naïve sum can't null vs dry; adding only the per-band
  *difference* makes each term exactly 0 when that band is neutral (`g==1`) ⇒ bit-exact identity at
  neutral regardless of crossover phase. `db_to_lin(0.0)==1.0` exactly, and a OnePole gain smoother
  primed to 1.0 stays 1.0, so `(g−1)==0.0` to the bit. Any future multiband/parallel color box (a
  multiband saturator, X-RAY-driven dynamic EQ) uses this to satisfy the neutral-null done-bar.
  (c) **Peak envelope followers RIPPLE on low frequencies** — a fast-attack peak follower re-arms to
  the |signal| peak every half-cycle, so on low tones it sits well above a slower follower and a
  fast−slow "transient" detector *false-fires* through the whole note. Fix: give the fast follower a
  release LONGER than the lowest period (BANDAID uses 1 ms attack / **60 ms** release) so it holds
  through cycles; or detect on an RMS/mean envelope. Relevant to PATINA's env-keyed noise gate and
  any onset/transient detector on bass-heavy material.
  (d) **Measuring a per-band effect by re-filtering the output is phase-fragile.** The parallel-delta
  output's low band is already `g·band_low`; re-applying an LR4 low for measurement adds group delay,
  and subtracting that phase-shifted copy (for a *cut*) makes a **comb** whose per-sample PEAK is
  scrambled (can read *larger* than the input at notch-adjacent samples). Measure band level as a
  short-window **RMS/energy** (it averages the comb out and recovers the true gain scaling) rather
  than an instantaneous peak. Applies to any future done-bar that isolates a band from a summed output.
- HALT (2026-07-08): reusable patterns + the handoff to BANDAID.
  (a) **NEXT §7 item is BANDAID** (multiband transient designer) — a fresh crate from
  plugins/_template, no HALT coupling. Design (SPECS "BANDAID"): **LR4 3-band** split (2 crossovers);
  per band **transient = fast env (1 ms) − slow env (50 ms)**; **attack-region gain ±12 dB** +
  **sustain-region gain**, applied **5 ms smoothed**; params = 2 crossovers, per-band attack/sustain,
  output, per-band solo. Done-bar (PRD §4): *per-band transient peak ratio moves with attack gain*.
  **Reuse:** the LR4/TPT-SVF crossover **band-split tree is already built** — TRACER (time-varying LR4
  crossover tree) and OVERSEER Master (3-band LR4 multiband comp) are the precedent; copy their split +
  the **PDC-clean recombination** (an LR4 split-then-sum is allpass-flat ⇒ all-gains-0 / all-solo-off must
  null to the input, the natural done-bar). `suite_core::dsp::{Svf, EnvFollower, ScopedFtz, db_to_lin}` cover
  the envelopes/gain; the transient = fast−slow env difference is UNDERTOW/GRIT envelope-follower territory.
  Zero latency (minimum-phase LR4) unless a lookahead is added — keep it zero and null all-flat.
  (b) **Dual-reader equal-power crossfade for click-free STATE SWITCHES** (halt/dsp.rs): keep `cur` + `prev`
  readers; on ANY discontinuity (mode engage/disengage, loop retrigger, param jump) do `prev = cur` (it keeps
  advancing coherently), swap `cur`, and mix `sin/cos(t·π/2)` over 5 ms. A "dry/idle" state is itself a reader
  that returns the live input, so engage & disengage reuse one path. Any future momentary/retrigger effect
  (a gate, a re-clocker) copies this — MURMUR's FDN-swap crossfade is the block-level analogue.
  (c) **Decouple retrigger PERIOD from read RATE** for pitched loopers: retrigger on an OUTPUT-sample counter
  (`loop_pos += 1; if >= loop_len …`), NOT when the read head exits the slice. Compounding a per-repeat pitch
  ratio onto the read rate makes wraps accelerate geometrically → rate→∞ → NaN (found via the preset fuzz
  render). Clamp the pitched rate (±2 oct) and wrap the read *within* the slice. Any beat-repeat/glitch with a
  per-repeat pitch step must do this.
  (d) **Momentary "last-pressed wins" arbitration**: a per-target press-`serial` counter (rising edge →
  `serial = ++ctr`; release → 0); the held target with the max serial is active; release falls back to the
  next-most-recent. Alloc-free, fixed array. Fold host buttons + MIDI notes into one `[bool;N]` held set at
  block rate and feed one `set_held`. Reusable for any multi-momentary control surface.
  (e) **Bit-exact passthrough when idle**: `(1-mix)·dry+mix·wet` is NOT bit-exact for arbitrary mix
  (`0.7+0.3 != 1.0` in f32), so an "inactive" effect must **short-circuit to the raw input** (a core
  `is_idle()` the plugin checks per sample) to pass the `< −120 dB` / `to_bits()`-equal passthrough done-bar.
  `mix = 0` while active still nulls because `1.0·dry + 0.0·wet == dry` exactly.
- SHAPESHIFT (2026-07-08): reusable patterns + the handoff to CHAMBER (the last, hardest clone).
  (a) **NEXT §7 item is CHAMBER** (image-source room simulator, Eigen clone) — a fresh crate from
  plugins/_template, no SHAPESHIFT coupling. It is flagged the **hardest clone** and carries an
  explicit **§4 CPU descope ladder**: bench the mean `process()` per 512-sample block @48 kHz in
  release; if **>30% of the real-time budget** drop ER **reflection order 3 → 2 → 1 + a bigger late
  field**. Design (SPECS "CHAMBER"): shoebox **image-source** ER to order 3 (≈60 images) — per image
  a delay `r/c`, gain `1/r × absorption^bounces`, HF damp, azimuth pan — crossfaded after the ER
  window into a **late FDN with RT60 from Sabine**. Draggable source/listener; moving source =
  interpolated (doppler-free, clamped) delays. Reuse targets already in the tree: **`suite_core::fdn`
  (`Fdn8`, Householder, `set_rt60`/`set_damping`/`set_delays`/`set_diffusion` + `measure_rt60`)** for
  the late field — MURMUR already proved it; and PLUCK's NOTE (f): a **direct-FIR** IR is cheap and
  zero-latency only up to ~1024 taps — CHAMBER's ER cluster is short delays (fine as taps or a small
  multitap delay bank), but a long convolved late field would need **partitioned/FFT** conv, so the
  FDN late tail is the intended path. Done-bars (§4): **first reflection = direct `r/c` ±1 sample**;
  **late RT60 ±25% of the Sabine prediction**. This ER path reports **real latency** (the pre-delay /
  ER onset), so wire PDC like GRIT/SHAPESHIFT if a dry path is mixed at lag 0.
  (b) **Oversampled-shaper + PDC recipe (now used by GRIT + SHAPESHIFT):** wrap the nonlinearity in
  `Oversampler4x::process(x, |v| f(v))`, delay the dry/parallel path by `Oversampler4x::measure_group_delay()`
  (= 22 samp, SR-independent), and report it via `context.set_latency_samples`. Tests: `mix=0` nulls
  vs the latency-matched dry (< −80 dB) AND `assert_single_coherent_peak(out, 2, 0.5)` on an impulse
  at mix 0.5. Any future nonlinear stage (PATINA, BANDAID drive, etc.) copies this verbatim.
  (c) **Bilinear XY morph (`bilinear_weights` + `orbit_offset` in shapeshift/dsp.rs):** weights are a
  partition of unity so a blend of bounded curves is unconditionally bounded — a clean pattern for any
  "N-corner morph" (NERVE mod targets, a future multi-band morph). Smooth the X/Y **position** (OnePole,
  primed to target on first configure) rather than the weights, then compute the (cheap) bilinear
  weights per sample — keeps the corner-isolation exact so "position at corner K == shaper K alone"
  nulls. The FLYBY `xy_pad` widget generalised cleanly to a single draggable point + a live LFO dot.
  (d) **THD-vs-time done-bar helper (crate-local Goertzel `power`/`thd_ratio` in shapeshift/tests.rs):**
  to prove a modulator changes *spectral character* periodically, sample THD in short windows and
  autocorrelate the sequence (period-lag > half-period-lag), the same shape as FLYBY's doppler
  periodicity test. Reusable for PATINA wow/flutter or any LFO-character done-bar.
- PLUCK (2026-07-08): reusable patterns for the remaining Phase 2b/3 clones.
  (a) **NEXT §7 item is SHAPESHIFT** (XY-morphing distortion, Teuri clone) — a fresh crate
  from plugins/_template, no PLUCK coupling. The brief says reuse the **suite shaper bank**
  and that **GRIT's oversampled shaper path is the reference**: GRIT wires
  `suite_core::dsp::Oversampler2x`/`Oversampler4x` (polyphase halfband FIR, alloc-free
  `process(x,|v| f(v))`) around its waveshaper and delays the dry/parallel path by
  `dsp::tests::measure_group_delay()` (== the analytic `oversampler_group_delay()`), reporting
  it via `context.set_latency_samples(...)` so partial mix doesn't comb-filter. SHAPESHIFT's
  XY-morph blends ≥2 shaper curves; its `mix=0` / corner-A-nulls-shaper-A done-bar needs the
  same PDC discipline. Read GRIT's dsp.rs + lib.rs first.
  (b) **KS / resonator tuning trick (cent-accurate):** to hit ±10 cents in a feedback loop,
  compute the *whole* loop delay `sr/f0` and subtract every element's low-freq phase delay
  (the 2-tap damp ≈ its coefficient `s`; the first-order all-pass ≈ `(1-c)/(1+c)`), reading the
  remainder from a **continuous Catmull-Rom `FracDelay`** — the interpolator absorbs the
  fractional part with <1-cent error at low fundamentals. A first-order all-pass is phase-only,
  so it never moves the measured *magnitude* peak → it can't break a tuning done-bar. CHORALE
  (resonator-peaks-at-tuned-pitches ±10 cents) can reuse this exact recipe.
  (c) **Effect-WITH-MIDI pattern:** `MidiConfig::Basic` + stereo AUDIO_IO_LAYOUTS, drain
  `context.next_event()` into a `[bool;128]` held-note bitmap at the top of process (alloc-free),
  snapshot it into the block Settings. CHORALE / any keyed effect reuses this.
  (d) **Forward exciter capture:** grabbing input *at* an onset means the recent-past ring is
  mostly pre-onset silence — capture the **next** N samples forward from the onset instead
  (Hann-windowed), then fire once full. Cost is ~N-sample latency between onset and strum
  (musical, = pick attack). Any input-excited/onset-triggered design (PATINA noise-key, etc.)
  should note this.
  (e) **Peak-tuning test helper (crate-local in pluck/src/tests.rs):** `mag_at(sig,f,sr)` =
  windowed single-frequency DFT magnitude; `peak_cents_error` scans a ±span-cent grid and
  returns the argmax's cents error. Cleaner than an FFT bin search for "is there a peak within
  ±X cents of f" — copy it for CHORALE/other tuned-resonator done-bars.
  (f) A **direct-FIR body IR at 1024 taps** (sum of decaying modal resonances, generated at
  init, L2-normalized, causal from tap 0) is cheap enough for a per-sample stereo convolution
  and adds **zero latency** (doesn't touch the mix=0 null). No FFT conv needed at this length —
  SPECS' "bench and pick" resolved to direct FIR. CHAMBER's longer IRs will need partitioned/FFT.
- CLEAVE (2026-07-08): reusable patterns + the new shared transport helper.
  (a) **`suite_core::testsig::TransportFrame` + `FakeTransport`** now exist (PRD §4's
  "fake-transport struct"). `TransportFrame { playing, tempo, ppq_pos, bar_pos,
  bars_per_sample, beats_per_bar }` is a plain host-type-free struct; fill one from
  `nih_plug` in the plugin (`bars_per_sample = (tempo/60/sr)/beats_per_bar`,
  `bar_pos = pos_beats/beats_per_bar`) and from `FakeTransport::new(sr, bpm)[.with_time_sig(n,d)]`
  in tests (`advance(n)` per block, `frame()`/`frame_stopped()`). **HALT** (Phase 3 performance
  buffer FX) should drive its tape-stop / stutter timing off this exact helper. **ASCEND** still
  has its own crate-local `TransportFrame` (I did NOT touch its crate — it could migrate onto the
  shared one at PEDAL-UI time; both are identical in spirit).
  (b) **Transport-locked pattern position pattern:** advance an internal `pattern_pos` (bars,
  mod pattern length) per sample from `bars_per_sample`, and re-sync to `frame.bar_pos` ONLY on a
  discontinuity (a seek, >~0.05 bar) — resyncing every block from `bar_pos` jitters onsets at
  block boundaries. Detect the wrap in the per-sample advance (not the block resync) to avoid
  double-latching. Free-run when `!playing` by just keeping the advance (internal clock).
  (c) **Buffer-slicer "latch at wrap" trick:** a rolling capture ring + a snapshot latched at the
  pattern boundary keeps slice starts on the musical grid (the current cycle plays the PREVIOUS
  2 bars — the cycle's own audio isn't fully recorded yet). This is the clean way to get
  "onsets-on-grid ±5 ms" testable. Grain reads use a 5 ms raised-cosine fade (no clicks); a
  linear interp read is enough for slice playback (FLYBY's Catmull-Rom `FracDelay` is overkill
  here). Transient slicing: preallocate the `stft::Stft` + flux/prev-mag scratch in `new()` and
  `reset()`+reuse at the wrap so `process()` stays alloc-free under `assert_process_allocs`.
  (d) **Per-step lanes = persisted `#[persist] RwLock<StepGrid>` (serde), NOT params.** 16–64
  steps × 8 lanes is far too many for the automation tree / pluginval fuzzer. Read it into the
  audio core each block via `try_read()` → copy into a preallocated scratch → keep the last
  snapshot on contention (RT-safe, no lock held in `process`). GUI (GUI-thread) edits it via
  `write()`. Presets carry a compact pattern-archetype id expanded into the grid on load (like
  FLYBY's shape→nodes), so factory JSON stays numeric/flat.
  (e) **Null contract for a re-timing effect:** keep the dry path a zero-latency direct copy,
  `out=(1-mix)·dry+mix·wet`, ceiling with a plain `clamp(-1,1)` (the UNIQUE continuous,
  null-preserving, ≤0 dBFS bound — a soft saturator would break the `mix=0` null because it isn't
  identity in-range). Prime the mix/out smoothers on the first `configure()` (snap to target) so a
  fresh instance at `mix=0` is an exact passthrough from sample 0.
  (f) **NEXT §7 item is PLUCK** (Karplus-Strong strummer, Strum clone): delay line + one-pole
  damp + allpass fine-tune, 6 strings tuned to chord-select / MIDI-held / **`suite_core::pitch`**
  chromagram key-detect; strum = staggered excitation (5–80 ms stride, up/down/alt); exciter =
  500-sample burst of input audio; embedded 2048-tap body IR. Fresh crate from _template, no
  CLEAVE coupling. FLYBY's `dsp::FracDelay` (Catmull-Rom) is copy-pasteable for the KS line's
  fractional tuning.
- FLYBY (2026-07-08): reusable patterns for the remaining Phase 2b clones.
  (a) **Stereo-out DSP + offline harness:** the mono `suite_core::harness::render_offline` can't
  render a stereo effect. Follow the OUROBOROS/FLYBY pattern instead — build a `Core` with
  `process_sample(l,r)->(l,r)` + `process_stereo(&[f32])->(Vec,Vec)` and drive it directly in
  `tests.rs`; use `render_path`/`write_wav` on the L channel for the artifact. **CLEAVE** (stereo
  slicer) should do the same.
  (b) **Transport-locked phase:** FLYBY reads `context.transport().tempo` per block and derives a
  phase increment; **CLEAVE needs the PLAYHEAD too** (bar/beat position), not just tempo — see
  ASCEND's `TransportFrame` pattern (it already wraps `transport().pos_beats()`/bar math and, per
  ASCEND's STATUS notes, suggests promoting that to a shared `suite_core` testsig/transport helper
  so the step sequencer can be tested offline against a synthetic playhead). Strongly consider
  building that shared fake-transport (PRD §4 lists a "fake-transport struct (tempo, playhead, bar
  pos) for ASCEND/CLEAVE/HALT") **before** CLEAVE's step sequencer so onsets-on-grid (±5 ms) is
  testable headless.
  (c) **Custom draggable widget:** the FLYBY XY pad (`xy_pad` in lib.rs) is a clean template for any
  custom editor widget — allocate one `Sense::click_and_drag` rect, store the grabbed index in
  `ui.memory`, map screen↔data with a pair of helpers, and bracket every drag in
  begin/set/end_set_parameter. A phase/position value from the audio thread rides an
  `Arc<AtomicF32>` (published only while `editor_state.is_open()`), read in the editor with
  `ui.ctx().request_repaint()` to animate. CLEAVE's step-grid UI can reuse this shape.
  (d) **Fractional delay w/ Catmull-Rom** (`dsp::FracDelay` in flyby, private) is copy-pasteable for
  any pitch/grain read (CLEAVE slice reads, PLUCK KS line). It's local to the crate per the DSP-
  local convention; if a 3rd plugin needs it, promote to suite-core then.
- OVERSEER-ENRICH (2026-07-08): reusable machinery for later work.
  (a) **`suite_core::classify`** is general — `FeatureExtractor` (allocation-free, feed stereo
  blocks; `summary()` for continuous, `begin_capture(n)`/`take_capture()` for exact-N LEARN) +
  `classify(&FeatureSummary)` + `infer_theme(reports, mix)`. Any future analyzer/visualizer
  (e.g. X-RAY) can reuse it. Tuning tip: onset detection is a rising-edge fast-vs-25 ms-ref
  ratio; it over-fires on pitch-rate ripple, so onset-driven classes are gated on `le(pitched)`.
  Sustain uses a smoothed-envelope windowed mean/peak (raw peak/mean reads transient on ripply
  sustained tones — don't revert it).
  (b) **Assist null invariant:** `enrich::apply_assist` MUST early-return `*base` bit-exact at
  strength ≤ 0 (the audio-path null done-bar). Any future assist target added must keep that.
  (c) **`Preset.category`** + **`PresetBar::filter(Option)`** now exist suite-wide (serde
  default, back-compat) — PRESET-EXPANSION can tag its deep banks by category for free.
  (d) The Node publishes an *effective type override* + a full `FeatureSummary` per block to the
  bus `Slot`; the Master reads them lock-free. Theme inference + assist targets are computed on
  the GUI/bus tick (spec-sanctioned) and stored in `MasterShared` for the audio thread — so
  assist is only live while the Master editor is open (documented; headless = frozen last value).
  (e) NEXT §7 item is **FLYBY** (Phase 2b doppler spatializer) — a fresh plugin crate from
  plugins/_template, no OVERSEER coupling.
- PRESET-SYSTEM (2026-07-08): **read this before OVERSEER-ENRICH v2 and PRESET-EXPANSION.**
  (a) **Preset bar API** (`suite_core::ui`): `PresetBar::new(plugin_id: &str, factory: &[Preset])
  .show(ui, params: &dyn Params, setter: &ParamSetter, apply_factory: impl Fn(&ParamSetter,&Preset))`.
  `plugin_id` is BOTH the egui id salt AND the on-disk folder slug. Generic helpers you can reuse
  anywhere: `snapshot_params(&dyn Params) -> BTreeMap<String,f32>` (param-id → plain value) and
  `apply_values(&dyn Params,&ParamSetter,&map)` (inverse). Disk tier: `presets::{list_user,save_user,
  load_user,delete_user,sanitize_name}` (all `Result`, GUI-thread only).
  (b) **OVERSEER-ENRICH needs TYPE-FILTERED banks.** The current `PresetBar` shows ONE flat FACTORY
  list + the USER list; it does NOT yet filter by instrument/theme type. To add type-filtered banks,
  extend `PresetBar` (don't fork it): the cleanest hook is a new builder method e.g.
  `.filter(Option<&str>)` or `.sections(&[(&str, &[Preset])])` that pre-partitions/labels the factory
  slice into named sub-sections in the dropdown — the widget already renders section headings
  (FACTORY/USER), so add per-category headings the same way. Tag factory presets with a `"category"`
  or `"type"` field: **the flat `Preset.values: BTreeMap<String,f32>` is NUMERIC-ONLY**, so a string
  category can't ride in `values` — either (i) add an optional `pub category: Option<String>` to the
  `Preset` struct (serde `#[serde(default)]`, back-compat since factory JSON just omits it), or
  (ii) key banks by a naming convention. Option (i) is the intended path and is a small suite-core
  change (API rule → rebuild-all, which is cheap and already automated).
  (c) **OVERSEER editors**: ONE crate/bundle exports TWO plugins (Node struct + Master struct), each
  with its OWN editor, `factory_presets`, and `apply_*_preset`. They already carry SEPARATE preset
  bars with distinct slugs **overseer-node** and **overseer-master** (separate `Documents\Qeynos\
  Presets\overseer-node|overseer-master\` folders). The Node editor's preset bar sits directly under
  the LABEL text field (I split the old shared `ui.horizontal`); the Master bar sits above a small
  RESET LUFS row. When OVERSEER-ENRICH adds the auto-classified Instrument Type, filter the Node bar
  by the guessed/pinned type, and the Master bar by the guessed THEME. NodeParams also holds a
  non-param `label: RwLock<String>` — it is NOT in `param_map`, so it is (correctly) NOT saved into
  presets; keep type/theme as real `#[id]` params if you want them in presets, else publish via the Bus.
  (d) **User presets are keyed by nih-plug param id** (e.g. grit `autogain`), factory by pretty JSON
  keys (e.g. `auto_gain`) — they differ on purpose; both are "same flat JSON". PRESET-EXPANSION authors
  factory presets in the pretty-key space that each plugin's `settings_from_preset` + `apply_preset`
  already read, so nothing changes there.
- W1-RUMBLE-BASSLINE (2026-07-07): reusable patterns for the FL piano-roll scripts (W2-BREAK-CHOP, W3-DARK-PROGRESSION).
  (a) **A `.pyscript` cannot be imported by name** — the extension isn't a recognized source
  suffix, so `importlib.util.spec_from_file_location` returns a spec with no loader. Load it with
  an explicit `importlib.machinery.SourceFileLoader("mod", path)` + `spec_from_loader` (see
  `pyscripts/tests/test_rumble.py::load_module`). (b) **Mock `flpianoroll`** (Note class w/
  number/time/length/velocity/pan/selected/muted; `score` = indexable list w/ addNote/getNote/
  deleteNote/clearNotes/noteCount/PPQ/getTimelineSelection; `ScriptDialog` stub collecting
  AddInput*/GetInputValue, Execute→True) lives in test_rumble.py and is **built to be reused by
  W2** — W2 operates on SELECTED notes, so seed its mock `score` with pre-made notes and set
  `.selected=True` on the ones to chop/reorder/reverse. Inject the mock into `sys.modules` BEFORE
  loading the script. (c) **FL script structure that stays testable:** keep ALL logic in a PURE
  function returning plain dicts (no flpianoroll types), guard `import flpianoroll` (→None headless),
  and let FL's `createDialog()`/`apply(form)` be the only flp-touching code. FL calls createDialog
  then apply(form) on OK — no module-level side effects needed (unlike ComposeWithLLM which ran at
  import because it had no dialog). (d) **Kick grid math:** 4/4 bar = 4·PPQ ticks; kicks on beats
  (offset%PPQ==0 ⟺ 16th idx%4==0); a 16th = PPQ//4 (PPQ always ÷4: 96/192/384/960). W2 slice
  reorder/reverse should preserve total selected duration and keep pieces on the 16th grid.
  (e) **PPQ comes from `flp.score.PPQ`** (default 96 on this machine's projects) — pass it into the
  pure function so tests can vary it. Install target for all W-piano-roll scripts:
  `[MyDocuments]\Image-Line\FL Studio\Settings\Piano roll scripts\` (found on disk; MyDocuments is
  OneDrive-redirected). Live run = CHECKPOINTS (FL scripts can't run headless).
- VOXKEY (2026-07-07): reusable patterns for the VOX retune/conform plugins (VOXFIT next).
  (a) **Formant-preserving retune** = drive `suite_core::shift::ShiftEngine` (preserve ON) from a
  detected f0; gate `mix=0` on the DRY (latency-matched `DelayLine`) path, never the wet PV
  identity (only nulls ~−15 dB — SEANCE's honest note). Report latency == `ShiftEngine::latency()`
  (2048). (b) **Detector choice:** `suite_core::pitch::PitchTracker`'s ±35-cent hysteresis biases
  a retune ratio by up to 35c and STICKS after a note change → use `suite_core::pitch::Mpm`
  directly (decimated ~12 kHz, 1024 win, median-3, NO hysteresis) when you need cents-accurate f0.
  VOXKEY's private `RetunePitch` (plugins/voxkey/src/dsp.rs) is copy-pasteable. (c) **Test signals:**
  `testsig::synth_vocal` carries ±17c (±1%) 5 Hz vibrato that passes through a hard-snap retune as
  residual wobble — use a **vibrato-free saw+formant ramp** (brief-sanctioned) for a ±15c retune
  done-bar. For a **formant-envelope** measure, reuse the shift.rs cepstral `avg_log_env` +
  log-freq cross-correlation `formant_shift_ratio`, and keep the test f0 LOW ENOUGH that the
  n/16 cepstral lifter cutoff sits BELOW the pitch-period quefrency sr/f0 (e.g. f0≤150 at n=4096),
  else the lifter keeps the harmonic comb and you measure the pitch move, not the envelope.
  **VOXFIT** (next): formant/character conformer — reuses ShiftEngine in **formant-only** mode
  (`set_pitch_ratio(1.0)` + `set_formant_ratio(2^(±st/12))`, preserve ON) + needs a de-esser;
  suite-core already has `dsp::EnvFollower` + `dsp::Svf` for the split-band (5–9 kHz) de-ess work.
- FREEZE-MIX (2026-07-07): pattern for any future freeze/hold control — keep the freeze toggle, add a `freeze_mix` (default 1.0) that does a SAFE OUTPUT crossfade `out = fm·frozen + (1−fm)·live` ONLY while frozen; never feed live audio back into a frozen feedback/∞-RT60 loop (runaway). Smooth `fm` with a ~15 ms OnePole. For a latency-reporting plugin (EMBER) blend toward the latency-matched dry, not the raw input, so the blend stays phase-aligned. Remember the 4 touch-points per plugin: Settings field + Default, DSP smoother (field/init/set_time/reset/process), lib param + snapshot, presets `settings_from_preset` literal, and the GUI row — plus any `Settings {…}` literals in tests.rs.
- SEANCE / `suite_core::shift` (2026-07-07): **the shared formant-preserving PV shifter — VOXKEY + VOXFIT build directly on this.** EXACT API (`suite-core/src/shift.rs`):
  * `ShiftEngine::new(fft: usize, hop: usize, sr: f32)` (house geometry 2048/512; `hop` divides `fft`) or `ShiftEngine::default_geometry(sr)`.
  * `set_pitch_ratio(f32)` — 2.0 = +1 oct, `2^(st/12)` for semitones (clamped ≥1e-3).
  * `set_formant_ratio(f32)` — independent formant move, only active when preserve is ON; >1 = formants up.
  * `set_envelope_preserve(bool)` — ON (default) = formants stay put as pitch moves (cepstral flatten + re-apply); OFF = raw-magnitude shift (chipmunk; formant knob ignored).
  * `process(x: f32) -> f32` streaming, delayed by `latency()`; `latency() -> usize` (== fft_size); `reset()` (clears STFT history + phase accumulators + re-primes).
  * `pub const DEFAULT_FFT=2048`, `DEFAULT_HOP=512`.
  Reuse guidance: **VOXKEY** (retune) drives `set_pitch_ratio` from `suite_core::pitch::PitchTracker` f0 → nearest scale note ratio, preserve ON + a formant-offset knob → `set_formant_ratio`; gate its **mix=0 on the DRY path** (the wet identity only nulls ~−15 dB — do NOT expect a tight wet null; the plugin's latency-matched dry is the clean path). **VOXFIT** (character) runs it with `set_pitch_ratio(1.0)` + a `set_formant_ratio(2^(±st/12))` knob for pitch-independent formant conforming, preserve ON. Cepstral lifter cutoff = fft/16 (≈128 @2048) assumes f0 below ~sr/(2·L)≈187 Hz for cleanest harmonic/formant separation; higher voices still get a usable envelope. Integer bin-remap (round) is clean at octave/fifth, mild partial-quantization elsewhere (inaudible in textural use; f0 tracks ratio within a few cents). CPU: one engine = 1 STFT + 2 N-point cepstrum FFTs per frame; SEANCE runs 2 main (2048) + 1 shimmer (1024) and still passes pluginval s8 at 96k/1024 within the 120 s timeout.
- SNAP (2026-07-07): reusable patterns for percussive MIDI synths & any noise-based generator
  (SEANCE noise/air, PLUCK exciter, future drum synths). (a) **Mono-compatible decorrelated-noise
  width**: per channel feed n_L/R = (shared + k·diff_L/R)/√(1+k²) with THREE `Rng`s (shared,
  diffL, diffR) and k = K_MAX·width; identical LTI filters both channels ⇒ output corr = input
  corr = 1/(1+k²). Pick K_MAX = 0.8 so max-width corr ≥ 0.61 > 0.5 (stays mono-compatible), and
  keep the tonal/low content mono (only decorrelate the noise) so the total corr is even higher.
  width=0 ⇒ k=0 ⇒ L≡R exactly (mono null). The √(1+k²) normaliser keeps noise energy constant
  across width. (b) **Testable clap/burst scheduler**: lay N bursts at note-on at (j/N)·spread
  with `humanize·Rng.bipolar()·jitter_max` added, RNG **reseeded to a fixed value each note-on**
  (deterministic per seed — humanize=0 ⇒ exactly-even, exactly-N). Each burst = a `Burst{start,
  attack,inv_tau,amp}` evaluated analytically per sample (env = ramp then exp); iterate ≤6 slots
  (alloc-free). Short tap τ (~2.5 ms) keeps overlapping slaps separable for the onset done-bar.
  (c) **Onset-count done-bar recipe (à la SWARM), tuned**: raw per-frame RMS flux is too noisy
  (false counts) AND bare level-crossing double-counts a slow tail — use a peak-follower-smoothed
  envelope (0.3 ms atk / 2 ms rel) + a **hysteresis** gate (set 0.40·peak, reset 0.22·peak) + a
  refractory, and **window the analysis to the spread window** (+15 ms) so the long tail's slow
  decay past it can't re-arm into a spurious onset. Choose burst spacing ≥ ~7 ms (Taps 3–4 at
  30 ms spread) for reliable resolution; tighter spacing overlaps by design. (d) **Custom
  value_to_string ⇒ mandatory string_to_value even on FloatParams**: a bucketed display label
  ("Snare"/"Clap") breaks clap-validator's convert-params round-trip; keep a parseable number in
  the string ("NN% Label") and add a string_to_value that reads the leading digits. (e) **IMPACT
  declick generalises to noise layers**: master amp env ramps from-current-to-peak (1.5 ms) +
  fade-in the FAST layers (rattle/clap/click, decayed by mid-note) over the same window; keep the
  tonal osc phase-continuous with amplitude from the master env only (no independent body env that
  would jump on retrigger). SNAP added NOTHING to suite-core (all DSP local) ⇒ no suite-wide revalidate.
- UNDERTOW (2026-07-07): reusable patterns for the remaining `suite_core::fdn::Fdn8` consumers
  (SEANCE shimmer verb, CHAMBER) and for any kick/sidechain-ducked generator (W1-RUMBLE-BASSLINE).
  (a) **Fdn8 "small/dark" recipe that works**: 8 lines geo-spread 8–25 ms × a 0.6–1.7 Size scale,
  `set_damping(0.6)`, `set_diffusion(0.6)`, `set_rt60(decay)`, delays nudged mutually-prime-ish
  (odd + gcd bump, same helper as MURMUR). Summing the stereo pair → mono gives a solid sub; keep
  `0.5·(fl−fr)` as a side signal for width. It's provably bounded (orthogonal H) so no in-loop
  tanh needed — but DC-block the mono sum and safety-clip each output channel (identity <0.9 so
  the dry null survives). (b) **Reconfigure Fdn8 only on CHANGE** (epsilon-cache size/decay) — a
  set_delays every block is wasteful and the un-crossfaded length change can click; UNDERTOW isn't
  ping-pong (unlike MURMUR) because Size/Decay are set-and-forget, so change-gating is enough.
  (c) **Adaptive sidechain normaliser for a level-independent ducker**: a fast-attack peak env
  divided by an instant-attack/slow-release (~1.5 s) peak-tracker gives `sc_norm→1` at every onset
  regardless of input amplitude, so `duck_reduction = depth_dB·sc_norm` hits full depth on every
  kick. (d) **Duck done-bar without the tail-decay confound**: the naive "onset env vs inter-onset
  peak" self-compare UNDER-reads the dip because a reverb bed is highest right at the onset (fresh
  energy) — render a NO-DUCK reference with identical settings and measure the notch depth of the
  ducked render against the reference at the same instant; that's the true, faithful duck depth.
  (e) **Mono-below-Fc**: HP the width side-signal steeply (2× cascaded SVF `.hp`) at the crossover
  and add ±side only to the >Fc band; the sub stays perfectly correlated L=R below Fc. (f) **dB
  param → true-0 floor**: map the bottom of a dB FloatParam range (≤ −59.5 dB) to linear 0.0 in
  the snapshot so a "muted" gain nulls EXACTLY (db_to_gain(−60)=0.001 would sit above a −80 dB
  bar). (g) **Note IntParam**: 0..35 → MIDI 12..47 (C0..B2), needs BOTH value_to_string (note name)
  and string_to_value (parse "A1"/int) or clap-validator param-conversions fails (IMPACT's lesson).
- MURMUR (2026-07-07): **the reusable FDN core is `suite_core::fdn::Fdn8`** — UNDERTOW (next: needs a
  small/dark FDN preset), SEANCE, CHAMBER reuse it. EXACT API:
  * `Fdn8::new(max_delay_samples: usize, sr: f32) -> Fdn8` (preallocates 8 lines at max; call set_delays after)
  * `set_delays(&mut self, &[usize; 8])` — per-line lengths, clamped [1,max], recomputes decay gains, NO alloc
  * `set_rt60(&mut self, rt60_s: f32)` — per-line g_i=10^(−3·Lᵢ/(RT60·sr)); big RT60 ⇒ freeze (gain capped 0.99995)
  * `set_damping(&mut self, tilt: f32)` — −1 bright / 0 mild / +1 dark, per-line color spread; DC passes at unity
  * `set_diffusion(&mut self, coeff: f32)` — input-allpass g (both channels), clamped [0,0.9]
  * `set_output_gain(&mut self, g: f32)` (default 0.4); `max_delay() -> usize`
  * `process(&mut self, l: f32, r: f32) -> (f32, f32)`; `reset(&mut self)`
  * `pub const N: usize = 8`; free fn `measure_rt60(ir: &[f32], sr: f32) -> Option<f32>` (Schroeder EDC, −5..−35 dB fit)
  Reuse tips for UNDERTOW's "small/dark FDN preset": short delays (e.g. 8–25 ms via small Size-equiv scaling),
  high damping tilt (~+0.6), moderate RT60 (0.4–1.5 s), high diffusion (~0.6). The core is stereo (odd/even taps);
  for a mono rumble sum the pair or use one channel. It's provably bounded (orthogonal H, g≤1, DC-unity LP), so
  no in-loop tanh is needed — but add a wet-side DC-block + knee'd safety-clip if you feed it hot (MURMUR does).
- MURMUR reverb-plugin patterns (2026-07-07): (a) **Ping-pong two-instance crossfade for click-free
  re-config**: run BOTH instances on the input at all times so the idle one is pre-warmed and already
  contains the transient that will trigger the swap; on trigger do a 50 ms equal-power (cos/sin) blend
  cur→idle, and only AFTER it completes reset+reconfigure the now-idle (silent-in-the-blend) instance.
  This is what makes "reconfigure delay lengths without a click" work — the reconfig happens at 0 gain.
  (b) **Randomness=0 must collapse to a deterministic nominal config** so an RT60/decay done-bar is
  measurable; layer per-draw deviation as `nominal·(1+r·dev·rand_bipolar)` so r=0 ⇒ nominal exactly.
  (c) **Mutually-prime-ish delay nudging** (force odd, then +2 until gcd=1 with all earlier lines,
  bounded) kills commensurate-delay flutter; cheap, deterministic, alloc-free, runs only on re-roll.
  (d) **Onset detector = fast/slow envelope ratio on a high-passed copy** (spectral-flux stand-in) is
  zero-latency and fires reliably on an impulse (fast≫slow); a refractory gap stops double-triggers.
  (e) Apply live-tweakable params (decay/freeze) to both instances every block, but apply a param that
  ALSO carries per-draw randomness (color) only on knob-CHANGE, else every block overwrites the draw's
  randomness. (f) Same zero-latency + mix=0-null convention as OUROBOROS/SWARM (time-smearing effect).
- SMUDGE (2026-07-07): reusable patterns for STFT per-frame spectral effects (SEANCE, CARVE,
  any bin-domain op). (a) **Exact per-op bypass** = skip the whole op when its smoothed amount
  ≤ 1e-5 (leave the complex spectrum untouched); with all ops skipped the wet path is the
  `suite_core::stft` identity reconstruction, which nulls against the FFT_SIZE-delayed dry
  **< −60 dB** (much tighter than mix=0). Prime the amount smoothers to their target on the
  first `configure` (a `primed` flag) so a base amount of exactly 0 stays exactly 0. (b) **Bin
  permutation drops level by exactly −6 dB** (= √(fft/hop) = 2 for 2048/512): shuffling bins
  DECORRELATES overlapping WOLA frames, so their overlap-add sums in power (×√overlap) not
  coherently (×overlap). This is INDEPENDENT of redraw rate (a fixed perm loses the same 6 dB).
  Fix = pre-multiply the permuted content by √(fft/hop). Keep DC + Nyquist fixed in the perm
  (STFT zeroes their imag on inverse; moving a big bin into DC loses energy). (c) **A bin-domain
  op cannot be energy/peak-bounded in the spectral domain alone** — an additive/feedback echo
  matched to input ENERGY still doubles the TIME-DOMAIN peak (crest factor). Two-layer guard:
  energy-normalise the echo DOWN to a decaying envelope of input frame-energy (attenuate-only,
  no ducking while input present, ~1 s tail release) for musical level control, PLUS a wet-path
  **safety soft-clip that is EXACT identity below 0.9** (tanh only above). The identity region is
  the trick: normal reconstruction sits well under 0.9, so the null/identity done-bars are
  untouched while loud resonant peaks are bounded < 1.0. A global tanh would break the −60 dB
  null (tanh(0.5) errs ~−28 dB); the knee'd clip does not. (d) **Chaos S&H must modulate amounts
  MULTIPLICATIVELY** (random gain ≤ 1), never additively — that keeps a base-0 amount at 0 so
  exact bypass survives full-depth chaos (verified as a done-bar). (e) Stereo: run L as the
  "primary" channel that advances all shared per-frame state (chaos draw, perm redraw, smoothed
  eff params, per-band tables) and R as "secondary" reusing it — L and R STFTs are hop-aligned
  (same sample count) so both fire on the same frame; borrow-split the core's fields (stft_l vs
  shared/chan_l/cfg are disjoint) so each channel's `stft.process(|spec| frame(...))` closure
  compiles under NLL. (f) Blur phase = EMBER's phase-vocoder advance, but blended along the
  SHORTEST ARC by blur dominance: `phase_out = wrap(ph + va·wrap(advanced − ph))`, va ramps 0→1
  as amount 0.5→1 — phase-continuous, no switch click. Nothing added to suite-core (all SMUDGE
  DSP local to the crate, reusing stft/OnePole/ScopedFtz/Rng) ⇒ no suite-wide revalidate.
- SWARM (2026-07-07): reusable patterns for granular / capture-buffer plugins (CLEAVE, SEANCE,
  PLUCK exciter, any grain cloud). (a) **Grain pool = fixed `[Grain; N]` with an `active` flag +
  monotonic `serial`**; spawn scans for a free slot then steals min-serial — alloc-free under
  assert_process_allocs; the only big buffers (10 s stereo capture + shimmer ring) are allocated
  in `set_sample_rate`/`initialize`, never in process(). (b) **Poisson scheduler**: per sample
  `countdown -= 1`; while `countdown <= 0` spawn + `countdown += -ln(u)/density·sr` (exp
  inter-arrival); cap the while-loop with a budget so a degenerate tiny interval can't hang.
  **Grid scheduler**: interval = `division.beats()·60/bpm·sr`, spawn `round(density·period)`
  grains/tick so a single `density` knob means the same thing free or synced. (c) **Density
  done-bar counting**: seed the frozen buffer with ONE impulse at the read-head centre — each
  grain that reads across it emits one sharp click, so onsets are countable even when grains
  overlap (impulse clicks are 1–2 samples wide and land at distinct times). Count = relative
  threshold (0.15·peak) crossings + refractory gap; use a RELATIVE threshold because the grain
  amplitude is density-normalised (`1/√overlap`) so absolute spike height shrinks as density
  rises. (d) **Granular feedback (shimmer)** loses ~10 dB round-tripping through a
  density-normalised decorrelated grain cloud, so a bare +12 st send DECAYS — add a makeup drive
  (~6×) into the in-loop `tanh` to reach unity loop gain (blooms) while the tanh still bounds
  self-oscillation past unity (OUROBOROS §3 convention). (e) Reads **wrap circularly** over the
  whole capture buffer — a grain crossing the write head reads old audio, not garbage; document
  it as a benign artefact rather than adding offset-headroom bookkeeping. (f) Zero latency +
  mix=0-null (NOT lag-0 coherent-peak), same as OUROBOROS — a granulator is a time-smearing
  effect.
- OUROBOROS (2026-07-07): reusable patterns for the remaining feedback/delay clones
  (MURMUR, HALT, and any regen effect). (a) **In-loop stability = WIRE's convention**:
  `tanh` soft-limiter at unity AFTER the (gain-boosting) processing chain, then a one-pole
  DC blocker, then tap the output and feed back `fb×tap`. At fb>100% this self-oscillates
  into a *stable, bounded, non-silent* limit cycle — the done-bar asserts stability (windowed
  last-5 s RMS: growth ≤1 dB, not collapsing), NOT silence. (b) **Click-free delay
  modulation**: a fractional delay read with a ~40 ms one-pole glide on the *delay length* +
  linear interpolation. Done-bar it by rendering a steady tone at a fixed delay vs. a
  block-by-block render that steps the delay mid-way, and asserting the max sample-to-sample
  delta near the change ≤ ~3× the steady-state max (a hard tap-switch is 10–100×). (c) For a
  **time-delay effect the lag-0 `assert_single_coherent_peak` partial-mix check does not
  apply** (no lag-0 wet to align) — assert `mix=0` nulls vs dry instead, and set_latency=0
  (the delay is the effect, not processing latency; only FIR/lookahead counts). (d) A **Niemitalo
  two-path IIR Hilbert** (coeffs A=[.6924,.9361,.9882,.9987], B=[.4022,.8562,.9723,.9953],
  section `y=a·(x+y[n-2])−x[n-2]`) needs a **1-sample delay on the A branch** (NOT B) for the
  best ~90° match with these coeffs — verify empirically (envelope-flatness sweep), the delay
  side is not obvious. (e) Slots that own state for ALL effect types and dispatch on the
  current type (rather than reallocating on type-switch) keep `process()` allocation-free under
  assert_process_allocs; the two big buffers (pitch/reverse grain rings, 8 k each) are the only
  cost. (f) **Slot-order as a permutation enum** beats N position-IntParams (no degenerate
  duplicate states) and is trivially exhaustive.
- WIRE (2026-07-07): reusable pattern for any codec/heavy-DSP-in-the-audio-thread plugin.
  (a) A real third-party codec (opus-rs) can run in `process()` if a full block's work
  fits the RT budget (bench it — 20ms Opus frame = 0.3% RT) AND you wrap the call in
  `nih_plug::util::permit_alloc(|| …)` so the workspace's assert_process_allocs tolerates
  the codec's internal Vec churn. Offline harness tests call the core directly (no alloc
  guard active), so they need no wrapper — only the plugin `process()` does.
  (b) Frame-rate ↔ block-rate adapter: fixed-capacity ring FIFOs (a `SampleFifo` that never
  reallocs) accumulate host samples into 20ms frames and buffer decoded output; seed the
  out-FIFO with one frame so the streaming reader never underflows. The codec's own
  algorithmic delay is absorbed inside the frame buffer ⇒ measure total latency with an
  impulse and REPORT the measured value (don't try to sum sub-latencies analytically).
  (c) Arbitrary host rate → fixed codec rate: a push-based linear upsampler (host→48k, emits
  0+ samples per push) + a pull-based linear downsampler (48k→host, consumes a fractional
  count per output) — both exact passthrough when ratio≈1 so latency stays clean at 48k.
  Linear interp is the documented quality choice (cheap, ~1-sample delay, mild aliasing is
  on-brand for lo-fi). (d) opus-rs API: `OpusEncoder::new(rate_i32, ch, Application)` +
  public fields `bitrate_bps/use_cbr/complexity/use_inband_fec/packet_loss_perc`,
  `encode(&[f32], frame, &mut [u8])->usize`; `OpusDecoder::new(rate, ch)` +
  `decode(&[u8], frame, &mut [f32])->usize`. Application is FIXED at construction ⇒ rebuild
  the pair to switch Voice/Music. Only 8/12/16/24/48k rates; **12k & 24k SILK paths are
  buggy in 0.1.23** — run at 48k and band-limit ahead of it. decode() errors on empty input
  (no PLC entry) ⇒ synthesise concealment yourself.
- DRIFT (2026-07-07): reusable pattern for the remaining Phase-2a clones. (a) Shepard-filter
  design: N filters at log positions u_i = frac(phase + i/N) across span octaves, raised-cosine
  gain window over u ⇒ silent edge wraps; even span/N spacing gives the t+period/N self-similarity.
  (b) BPM sync reads `context.transport().tempo` (Option<f64>, falls back to 120) once per block →
  Settings.tempo_bpm; free-running phase accumulator, NOT locked to song position (click-free, robust).
  (c) A Cytomic **bell/peaking** SVF (k=1/(Q·A), out = v0 + k·(A²−1)·bp; pass-through at 0 dB) was kept
  LOCAL to the plugin crate rather than added to suite-core, to avoid the suite-wide rebuild/revalidate
  the API rule mandates — do this for any small DSP block only one plugin needs. (d) Spectral done-bar
  tests over noise MUST Welch-average (+freq-smooth) the periodogram, else the noise realization
  (not the filter) dominates the correlation. (e) A stereo phase offset equal to k/N is degenerate for
  any Shepard-style effect (maps one channel onto the other) — pick non-commensurate test values.
- HARD-CHECKPOINT-1 new suite-core APIs (2026-07-07), reuse in every later plugin:
  * `dsp::Oversampler2x/4x::group_delay_samples()` (analytic base-rate group delay from
    the halfband FIR tap count, 15.0 / 22.5) + `measure_group_delay()` (empirical integer
    peak-lag, 14 / 22 — verified equal within 1 sample by
    `dsp::tests::oversampler_group_delay_matches_analytic`). ANY plugin that runs a wet
    stage through an oversampler MUST delay its dry/parallel path by `measure_group_delay()`
    and report it via `context.set_latency_samples(...)`, else partial mix comb-filters.
  * `dsp::DelayLine::new(max)/set_delay/process` — alloc-free integer dry-delay line.
  * `dsp::ScopedFtz::enable()` — RAII FTZ+DAZ (MXCSR bit15/bit6) denormal guard; put
    `let _ftz = suite_core::dsp::ScopedFtz::enable();` as the FIRST line of every plugin
    `process()` (now in _template so copies inherit it). No-op off x86_64.
  * `harness::assert_single_coherent_peak(out, cluster, frac)` — the partial-mix alignment
    regression assertion (impulse @ mix=0.5, near-identity wet → ONE peak, not two). Wire
    it into any new dry/wet-mix plugin's tests (done for GRIT + TRACER).
  * `loudness::LoudnessMeter::new_momentary(fs, ch)` — momentary-only meter (no integrated
    gating work in the callback) for consumers that only read momentary (OVERSEER Node).
    The integrated path is now an O(bins) `PowerHistogram` (0.05 dB bins) instead of an
    O(elapsed-time) rescan of a growing Vec.
  * OVERSEER param-zipper fix pattern: the DSP CORE owns the smoothers (suite `OnePole`),
    targets latched in `configure()`, per-sample smoothing for directly-applied scalar
    gains (drive/width/trim/mix) and 32-sample-block coefficient smoothing for EQ gains +
    comp threshold/makeup + ceiling. This makes smoothing offline-testable
    (`node_param_step_is_smoothed_no_zipper`) rather than living untested in the nih-plug
    layer. Snap-on-first-configure (`primed`) avoids a startup glide from 0.
- New tool (W4-SESSION-BOOTSTRAP, 2026-07-07): `tools/session_bootstrap.py`. Second
  Phase-4 Python tool. Talks to the FL Studio MCP controller over MIDI SysEx by
  REIMPLEMENTING that server's thin protocol in-tool (JSON `{action,params}` →
  `F0 7D <type> <ascii-json> F7`, type 0x11 final cmd / 0x12 final resp, chunk 200;
  response envelope `{"success":bool,...}`). Reusable pattern for later FL MCP tools
  (W5 PROJECT-JANITOR): make the transport an injectable interface with a real
  `MidiTransport` + a `RecordingTransport` mock, generate an op list purely from
  input (snapshot-testable), then `apply_ops` tolerant of per-op failures. mido is
  imported LAZILY inside MidiTransport so offline/mocked tests need no MIDI backend
  (test file's PEP 723 deps = []). FL MCP server exposes NO tempo command — any tool
  needing BPM must skip+report, not fail. Live FL was unreachable (port opens, FL
  not responding) exactly like W8's API-key gap → build+test offline, defer live to
  CHECKPOINTS.
- New tool (W8-VITALGEN, 2026-07-07): `tools/vitalgen/vitalgen/`. First Phase-4 Python
  tool. Pattern for later W-tools: PEP 723 inline metadata pinned `requires-python =
  ">=3.12,<3.13"`, run via `uv run --python 3.12 <script.py>` (uv at
  %USERPROFILE%\.local\bin\uv.exe, NOT on PATH). Constrained-schema pattern (LLM fills a
  bounded pydantic subset merged onto a known-good base; clamp continuous, reject enums)
  is reusable for any "LLM emits structured config" tool. Vital schema helpers live only
  in vitalgen.py (PARAM_SPEC). ANTHROPIC_API_KEY was unset on the build machine → any
  W-tool needing live Claude calls should build+test against fixtures and defer the live
  call to CHECKPOINTS, exactly as W8 did.
- New suite-core API (OVERSEER, 2026-07-07): `loudness` module — ITU-R BS.1770.
  `loudness::LoudnessMeter::new(fs, channels)`; `push(&[f32])` per sample frame;
  `momentary_lufs()` (400 ms), `short_lufs()` (3 s), `integrated_lufs()` (two-stage
  gating: abs −70 LUFS then rel −10 LU, 400 ms blocks @ 75% overlap, ~60 min
  pre-reserved so push never allocates in process), `reset()`,
  `set_kweighting(bool)` (test hook: disables K-filters AND the −0.691 offset →
  reading becomes plain 10·log10(mean-square) ≈ dBFS-RMS). Also public:
  `KWeight` (per-channel shelf→RLB cascade), `shelf_biquad(fs)`/`highpass_biquad(fs)`
  (bilinear-derived, correct at any fs — not just the 48k table), `k_response(freq, fs)`
  / `k_response_db` (analytic magnitude — what the OVERSEER meter assertion checks
  against), `Biquad` (f64 TDF-II with `magnitude(w)`), `LUFS_OFFSET`. W7 REFERENCE-GAP
  reuses this. Gotcha fixed this iteration: `f32::clamp` panics on inverted bounds —
  pluginval fuzzes freq params to 20 kHz where `0.45·fs` at 44.1k is lower; order your
  clamp bounds for ALL inputs (see overseer dynamics::set_crossovers + regression test).
- OVERSEER architecture note: same-DLL bus lives in `plugins/overseer/src/bus.rs`
  (`OnceLock<Bus>`, slots = atomics + one label mutex never touched in process();
  GC = Arc strong-count on registry access). If FL "Make bridged" is ticked the two
  plugins land in different processes and the link silently degrades (audio fine,
  grid empty) — tier-2 memmap2 fallback deferred to NERVE/X-RAY (DEFERRED.md).
- New suite-core API (TRACER, 2026-07-07): `pitch` module. `pitch::Mpm::new(window, sr,
  f0_min, f0_max)` + `analyze(&[f32]) -> PitchResult{ f0_hz, confidence }` — allocation-free
  McLeod Pitch Method (NSDF type-II ACF, key-maximum peak pick @ k=0.85, parabolic interp;
  confidence = interpolated NSDF peak height 0..1). `pitch::PitchTracker::new(sample_rate,
  default_f0)` — streaming sample-in tracker: anti-aliased decimation to ~12 kHz, window
  1024/hop 256, median-of-5 + ±35-cent hysteresis + Hz/ms slew; `push(x)`, `f0()`,
  `confidence()`, `set_slew`, `set_confidence_gate`, `set_midi_note(Option<f32>)` (MIDI
  bypass), `reset()`. `default_f0` is the frozen pitch used before the first confident
  detect and whenever confidence < gate (0.6) — that freeze is what keeps crossovers still
  on noise. `pitch::cents(a,b)` helper. PLUCK and CHORALE reuse this (chromagram/MIDI/held
  tuning). Also new: `testsig::synth_vocal(freq, len, sr)` — saw + 5 Hz vibrato through 3
  formant band-passes (F1 700 / F2 1220 / F3 2600, /a/-like), peak-normalized to 0.7;
  REPLACES the old `synth_vocal_stub` (kept, now delegates). SEANCE reuses it.
  `testsig::sliding_saw(f_start, f_end, amp, len, sr)` (exponential glissando 808 stand-in)
  + `testsig::sliding_saw_f0(f_start, f_end, n, len)` for exact instantaneous-f0 in tests.
  TRACER's time-varying LR4 was built on the TPT SVF (unconditionally stable under cutoff
  modulation) rather than direct-form biquads — both done-bar tests + pluginval Fuzz passed
  on the FIRST attempt, so the §8 Fable escalation valve was never triggered.
- New suite-core API (IMPACT, 2026-07-07): `testsig::synth_kick(&KickSpec, len, sr) -> Vec<f32>`
  and `testsig::KickSpec { f_start, f_end, pitch_decay_s, amp_decay_s, click, sub_level,
  sub_ratio, drive }` (Default = 180→55 Hz, ~0.5 s tail, light click). This is IMPACT's own
  kick math (exp pitch env → phase-continuous sine → band-passed noise click → tanh drive →
  exp amp env w/ 1.5 ms attack → soft clip; deterministic, peak-bounded < 0 dBFS). It REPLACES
  the old decaying-sine stub; `testsig::synth_kick_stub(len, sr)` is kept and now delegates to
  `synth_kick(&KickSpec::default(), …)`. UNDERTOW (kick-duck test), SEANCE, and any later
  plugin needing a synthetic kick should use this. IMPACT's per-plugin f0 measurement pattern:
  streaming `stft::Stft` (fft 4096/hop 1024) + quadratic peak interpolation over the low band.
- New suite-core API (EMBER, 2026-07-07): `stft::Stft` — streaming alloc-free STFT.
  `Stft::new(fft_size, hop)` (periodic Hann, COLA-normalized WOLA); `process(x, &mut cb)`
  where `cb: FnMut(&mut [Complex<f32>])` mutates the length-`num_bins()` complex spectrum
  per frame (DC/Nyquist imag auto-zeroed for a valid real inverse); returns one output
  sample delayed by `latency()` (== fft_size). Also `reset()`, `fft_size()`, `hop()`,
  `num_bins()` (= fft_size/2+1), `bin_freq(k, sr)`. `Complex` is re-exported from
  `suite_core::stft`. Backed by `realfft` (workspace dep, pure Rust, windows-gnu clean).
  SMUDGE/SEANCE/CARVE/DRIFT reuse this. To build a spectral effect: keep per-bin state,
  in the callback read mag=`b.norm()`/phase=`b.arg()`, rewrite `b = Complex::from_polar`.
  Report `set_latency_samples(stft.latency())` and delay the dry path by the same for a
  clean mix=0 null.
- New suite-core APIs (GRIT, 2026-07-07): `dsp::Oversampler2x` / `dsp::Oversampler4x`
  (polyphase halfband FIR, alloc-free `process(x, |v| f(v))`; reset()); `presets`
  module (`Preset{name, values}`, `Preset::parse`, `load_all(&[&str])` — flat embedded
  JSON via serde_json, now a suite-core dep). Any later plugin needing oversampling or
  factory presets should reuse these. suite-core API rule honored: workspace rebuilt,
  _template revalidated green.
- Fixed a latent pre-existing bug: `dsp::tests::env_follower_tracks_level` asserted RMS
  (0.354) but used fast-attack/slow-release times (which peak-track ~0.5). Made the test
  times symmetric so it measures level. (Never gated anything — build.ps1 tests per
  plugin crate, not `-p suite-core`.)
- Toolchain gap fixed: rustup's x86_64-pc-windows-gnu ships dlltool but NO assembler,
  so raw-dylib import-lib generation (windows-sys, parking_lot_core) fails with
  "dlltool could not create import library ... CreateProcess". Fix = portable
  MinGW-w64 binutils (winlibs 16.1.0 ucrt) extracted to tools/bin/mingw64 (gitignored)
  and prepended to PATH. build.ps1 does this automatically. Any fresh shell that builds
  cargo directly (not via build.ps1) MUST prepend tools\bin\mingw64\bin to PATH.
- nih-plug pinned rev: f36931f7af4646065488a9845d8f8c2f95252c23 (master @ 2026-07-07).
- clap-validator: 14 passed / 0 failed / 6 skipped / 1 warning (scan-time 363ms, cosmetic).
- pluginval strictness 8 (--skip-gui-tests): SUCCESS across 44.1/48/96k, blocks 64..1024.

2026-07-08 | SUITE-TRIAGE (fix program) | SHIPPED — all P0 + P1 findings from docs/TRIAGE-2026-07-08.md fixed, 0 refuted, 0 descoped; P2 tier appended verbatim-in-substance to DEFERRED.md ("TRIAGE-P2 backlog"). Fixes (file:line ≈ at commit time; every fix has a named regression test):
  WAVE 1 (suite-core + clamp policy, commit "wave 1"):
  - shift.rs synthesis phase accumulators wrapped mod 2π (suite-core/src/shift.rs:246+265 via wrap_pi) — SEANCE/VOXKEY/VOXFIT long-session wet degradation; test shift::synthesis_phase_stays_bounded.
  - bus.rs file-global instance-id allocator in the header (VERSION 1→2, alloc_instance_id; new_instance_id draws from the mapped file, pid+counter only as no-bus fallback) — NERVE cross-DLL id collision; test bus::instance_ids_unique_across_two_dlls.
  - bus.rs bounded seqlock reader spin (SEQ_SPIN_LIMIT=64 in read_slot + read_mod_fast) — wedged-writer livelock; test bus::permanently_odd_seq_does_not_livelock.
  - ui.rs MOD picker filtered to PluginKind::Nerve slots (suite-core/src/ui.rs mod_section).
  - Clamp policy: final ±0.999 clamp → ±8.0 runaway/NaN guard in GRIT/TRACER/DRIFT/FLYBY/CHAMBER/BANDAID/PATINA/CHORALE/SWARM + siblings WIRE/OUROBOROS/SHAPESHIFT; sanctioned fuzz-test updates to finite && ≤ +18 dBFS; synth ceilings (IMPACT/SNAP/ASCEND/PLUCK/UNDERTOW generators) intentionally kept — not in the flagged dry-path class. FALLOUT FIXED: 51 factory presets (GRIT 10, DRIFT 12, SWARM 6, TRACER 15, WIRE 8) were >0 dBFS under the honest guard (the old clamp was digitally clipping them); out-trims lowered to ~-0.5 dBFS, universal preset bar (peak ≤ 0 dBFS) unchanged.
  WAVE 2 (per-crate batches, one commit each):
  - GRIT/TRACER/CARVE/HALT dead MOD routes: block-rate plain delta (modulated_float − value) added onto the per-sample smoother via dsp::apply_mod_delta (grit lib.rs ~493-551, tracer lib.rs ~652-698, carve lib.rs ~393-435, halt lib.rs ~504-587); bus round-trip tests live_route_shifts_value_dead_route_is_identity (+ per-crate variants). GRIT auto-gain toggle 20 ms crossfade (test autogain_toggle_is_smoothed_not_stepped); TRACER guard 16→64 (dsp.rs:437, test hot_multiband_does_not_trip_instability_guard); HALT intra-period stutter wrap crossfade (test stutter_pitch_up_wrap_is_click_free), OUT applies to wet only = no idle step (test out_trim_no_step_between_idle_and_active_dry), same-block MIDI on+off latch (test same_block_note_on_off_still_triggers).
  - IMPACT/SNAP/PLUCK configure-clobber (class 3), design (b) reapply-on-change: impact dsp.rs:238 (last_f_end_snap), snap dsp.rs:293 (last_tune_snap), pluck dsp.rs:743 (last_damp_snap); tests keytrack/vel_bright_survives_reconfigure_same_settings + knob_change_*_midnote per crate. SNAP humanize seed ^ per-hit counter (dsp.rs:344, test humanize_varies_hit_to_hit_but_is_deterministic).
  - CLEAVE: host-loop wrap latch via raw bar-delta backward-jump detection (dsp.rs ~370/418, latch_snapshot extracted) — 1-bar FL loops now sound at mix=1 (test one_bar_host_loop_latches_and_refreshes); transport tracking frozen while !playing (dsp.rs ~393) — true stopped free-run (test stopped_transport_free_runs_at_tempo).
  - VOXFIT: tone OnePoles re-derived against block rate (configure(block_size), dsp.rs ~462-490) — knobs settle ~12-30 ms (test tone_smoother_settles_in_block_time); PV bypassed at unity formant via latency-matched dry + 15 ms crossfade, engine kept warm (tests unity_formant_bypasses_pv_and_nulls_dry < −40 dB, pv_bypass_toggle_is_click_free). VOXKEY confidence median-of-3 + hysteresis (ConfGate, dsp.rs ~440-490; tests conf_gate_*).
  - SEANCE: chopper phase-locked to the grid via pos_beats (per-sample beat advance; free-run only stopped) (dsp.rs ~301/335/734, lib.rs ~424; tests chop_phase_locks_to_transport_grid + chop_free_runs_when_stopped); wash buffer written during bypass (dsp.rs ~560, test wash_bypass_reengage_plays_fresh_not_stale); dual-FDN 60 ms crossfade on SIZE (MURMUR pattern, dsp.rs ~328, test size_sweep_no_click).
  - SHAPESHIFT: XY corner labels un-mirrored in GUI paint only (lib.rs ~681-703; DSP + presets untouched; manual already correct); ORBIT PHASE applied live outside the primed gate (dsp.rs; test orbit_phase_applies_live_after_prime).
  - EMBER/OUROBOROS/SWARM/MURMUR: freeze blend one-poled ~20 ms (class 5) (ember dsp.rs ~396, ouroboros dsp.rs ~928, swarm dsp.rs ~594, murmur dsp.rs ~455; test freeze_toggle_blend_is_click_free ×4); EMBER lib.rs:229 + OUROBOROS lib.rs:401 apply_preset now set freeze_mix (class 8).
  - UNDERTOW DC blocker sr-independent ~15 Hz corner (DcBlock::new(sr), dsp.rs; test dc_blocker_passes_sub_and_kills_dc); CHORALE 25 ms retune glide on ringing tails (Resonator delay_target slew; test live_retune_glides_without_click); OVERSEER ENRICH assist targets computed block-rate on the audio thread (master.rs update_assist + alloc-free bus try_node_reports; editor displays only; test master_assist_targets_computed_on_audio_thread) and drive-floor exact sat bypass (node.rs latency-matched tap + primed crossfade; test node_drive_floor_bypasses_saturation_exactly, nulls −98 dB).
  REFUTED: none — every audited claim checked against code held up.
  GATES: cargo test --workspace --release GREEN after wave 1 and again after wave 2 (63 test binaries, 0 failures); build.ps1 -All full sweep run as the final gate (see next entry if split).
  HOW-TO-TEST-IN-FL: route a NERVE LFO to GRIT DRIVE — it audibly moves now; play a 1-bar pattern loop into CLEAVE at mix=1 — it chops instead of staying silent; key-track IMPACT/SNAP — pitch holds for the whole note; VOXFIT default (formant 0) is now transparent and TILT/PROX/AIR respond instantly; SEANCE chop lands on the grid on every bounce; toggle FREEZE on EMBER/OUROBOROS/SWARM/MURMUR — no click; hot FL buses no longer get clipped by any suite plugin at mix=0.
